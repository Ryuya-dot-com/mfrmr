% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-plotting.R
\name{plot_qc_pipeline}
\alias{plot_qc_pipeline}
\title{Plot QC pipeline results}
\usage{
plot_qc_pipeline(x, type = c("traffic_light", "detail"), draw = TRUE, ...)
}
\arguments{
\item{x}{Output from \code{\link[=run_qc_pipeline]{run_qc_pipeline()}}.}

\item{type}{Plot type: \code{"traffic_light"} (default) or \code{"detail"}.}

\item{draw}{If \code{FALSE}, return plot data invisibly without drawing.}

\item{...}{Additional graphical parameters passed to plotting functions.}
}
\value{
Invisible verdicts tibble from the QC pipeline.
}
\description{
Visualizes the output from \code{\link[=run_qc_pipeline]{run_qc_pipeline()}} as either a traffic-light
bar chart or a detail panel showing values versus thresholds.
}
\details{
Two plot types are provided for visual triage of QC results:
\itemize{
\item \strong{\code{"traffic_light"}} (default): A horizontal bar chart with one row
per QC check.  Bars are coloured green (Pass), amber (Warn), or red
(Fail).  Provides an at-a-glance summary of overall model quality.
\item \strong{\code{"detail"}}: A panel showing each check's observed value and its
pass/warn/fail thresholds.  Useful for understanding how close a
borderline result is to the next verdict level.
}
}
\section{Interpreting plots}{

\itemize{
\item Green bars indicate checks that meet the threshold profile criteria.
\item Amber bars signal areas that may need attention but are not critical.
\item Red bars require investigation before the calibration is used
operationally.
\item The detail view shows numeric values, making it easy to communicate
exact results to stakeholders.
}
}

\examples{
toy <- load_mfrmr_data("study1")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 25)
qc <- run_qc_pipeline(fit)
plot_qc_pipeline(qc, draw = FALSE)
}
\seealso{
\code{\link[=run_qc_pipeline]{run_qc_pipeline()}}, \code{\link[=plot_qc_dashboard]{plot_qc_dashboard()}},
\code{\link[=build_visual_summaries]{build_visual_summaries()}}
}
