% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-extended-calibration-intervals.R
\name{confint.mfrm_testlet}
\alias{confint.mfrm_testlet}
\title{Explicit normal-approximation intervals for testlet calibration}
\usage{
\method{confint}{mfrm_testlet}(object, parm = "calibration", level = 0.95, ...)
}
\arguments{
\item{object}{A result from \code{\link[=fit_mfrm_testlet]{fit_mfrm_testlet()}}.}

\item{parm}{\code{"calibration"} returns all fixed-facet and step intervals.
Variance components and Person abilities are not included.}

\item{level}{Nominal confidence level between zero and one; default 0.95.}

\item{...}{Unused.}
}
\value{
A matrix with \code{Lower} and \code{Upper}, one row per fixed-facet level or
step, and attributes \code{level}, \code{method}, \code{target} and \code{note}.
}
\description{
Explicit normal-approximation intervals for testlet calibration
}
\details{
The explicit request computes estimate plus/minus a normal quantile
times the saved observed-information SE. It needs no fitting, scoring or
live optimizer and does not change the source fit. Nominal finite-sample
coverage is not established. Estimated variance boundaries, unresolved
numerical/information checks and invalid SEs retain missing bounds.
No regular variance interval, simultaneous comparison or automatic rater
classification is supplied. Default fit tables, summaries and plots omit
these bounds. Use \code{summary(fit, calibration_intervals = "normal")},
\code{plot(fit, intervals = "normal")}, or
\code{mfrm_results(fit, calibration_intervals = "normal")} to select the same
approximation in those outputs; their level arguments retain its nominal
interpretation. \code{\link[=predict.mfrm_testlet]{predict.mfrm_testlet()}} supplies the separate conditional
Person-scoring intervals.
}
\seealso{
\code{\link[=confint.mfrm_random_rater]{confint.mfrm_random_rater()}}, \code{\link[=mfrmr_interval_guide]{mfrmr_interval_guide()}}
}
