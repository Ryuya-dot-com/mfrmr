% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-generalizability.R
\name{plot.mfrm_d_study}
\alias{plot.mfrm_d_study}
\title{Plot design comparisons from a main-effects D-study}
\usage{
\method{plot}{mfrm_d_study}(
  x,
  y = NULL,
  type = c("coefficients", "error_variance", "heatmap", "contour", "surface3d"),
  x_var = NULL,
  y_var = NULL,
  group_var = NULL,
  panel_by = NULL,
  panel_grid = NULL,
  metric = NULL,
  draw = TRUE,
  main = NULL,
  palette = NULL,
  preset = c("standard", "publication", "compact", "monochrome"),
  ...
)
}
\arguments{
\item{x}{An \code{\link[=mfrm_d_study]{mfrm_d_study()}} result.}

\item{y}{Reserved for method compatibility.}

\item{type}{\code{"coefficients"} for G/Phi curves, \code{"error_variance"} for error
variance curves, or \code{"heatmap"}, \code{"contour"}, or \code{"surface3d"} for one
metric over two facet counts. Error variance is in squared score units,
not SEM. Lower error variance is better.}

\item{x_var, y_var}{Planned-count columns such as \code{"n_Rater"}. The default
horizontal axis is the first count column; surface plots use the next
column on the other axis. The two axes must differ.}

\item{group_var}{Optional additional column distinguishing curves. All
non-horizontal facet counts and residual assumptions remain separate
within each panel, including when \code{group_var} is supplied.}

\item{panel_by}{One column defining panels, or \code{NULL}.}

\item{panel_grid}{One or two columns defining panels. Use this or
\code{panel_by}, not both. Surface plots require every other facet count and
residual assumption to be constant within each panel. Subset the result
or add panels when they vary; they cannot be silently averaged or overlaid.}

\item{metric}{Optional selection from \code{"G"}, \code{"Phi"},
\code{"RelativeErrorVariance"}, or \code{"AbsoluteErrorVariance"}, compatible with
\code{type}. Surface plots require one metric and default to \code{"Phi"}.}

\item{draw}{Draw when \code{TRUE}; \code{FALSE} only returns plot data.}

\item{main}{Optional plot title.}

\item{palette}{Optional colors.}

\item{preset}{Plot style: \code{"standard"}, \code{"publication"}, \code{"compact"}, or
\code{"monochrome"}.}

\item{...}{Reserved for method compatibility.}
}
\value{
Invisibly, an \code{mfrm_plot_data} object with the scenario \code{table},
metric \code{series}, axis/group/panel settings, and labels. Use \code{\link[=plot_data]{plot_data()}}
for custom graphics. Automatic \code{\link[=as_ggplot]{as_ggplot()}} conversion is not provided
for this class; the base plots preserve the chosen comparisons.
}
\description{
Compare planned facet counts using an existing \code{\link[=mfrm_d_study]{mfrm_d_study()}} result.
G concerns relative ordering; Phi also includes shifts in absolute score
levels. Larger coefficients mean greater dependability under the selected
model and residual assumption, not proven pass/fail accuracy.
}
\details{
Points represent requested scenarios; connecting lines and
contours are visual guides. Missing estimates remain missing and break
curves. If none are available, inspect the D-study table and source
variance components. Coefficient curves show 0.70 and 0.80 reference
lines; these are not universal acceptance criteria.
Heatmaps include a numeric color key; exact values remain in the table.

The main-effects G-study combines unmodeled interactions in its residual.
Different residual-scaling curves describe assumptions, not confidence
bounds. All projections hold estimated components fixed. Check source
fit warnings before interpreting even large coefficients. For supported
designs with separately estimated interactions, including a single score,
use \code{\link[=mfrm_multivariate_gstudy]{mfrm_multivariate_gstudy()}} and \code{\link[=mfrm_multivariate_d_study]{mfrm_multivariate_d_study()}}.
}
\examples{
# After creating ds with mfrm_d_study():
# plot(ds, x_var = "n_Rater", panel_grid = c("Metric", "ResidualScaling"))
# For Rater x Task x Occasion scenarios, separate occasions explicitly:
# plot(ds, type = "heatmap", x_var = "n_Rater", y_var = "n_Task",
#      metric = "Phi", panel_by = "n_Occasion")
# With residual_scaling = "sensitivity", use
# panel_grid = c("n_Occasion", "ResidualScaling") instead.
}
\seealso{
\code{\link[=mfrm_d_study]{mfrm_d_study()}}, \code{\link[=plot.mfrm_multivariate_d_study]{plot.mfrm_multivariate_d_study()}}
}
