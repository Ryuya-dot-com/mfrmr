% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-estimation.R
\name{compare_mfrm}
\alias{compare_mfrm}
\title{Compare two or more fitted MFRM models}
\usage{
compare_mfrm(..., labels = NULL, warn_constraints = TRUE)
}
\arguments{
\item{...}{Two or more \code{mfrm_fit} objects to compare.}

\item{labels}{Optional character vector of labels for each model.
If \code{NULL}, labels are generated from model/method combinations.}

\item{warn_constraints}{Logical. If \code{TRUE} (the default), emit a warning
when models use different centering constraints (\code{noncenter_facet} or
\code{dummy_facets}), which can make information-criterion comparisons
misleading.}
}
\value{
An object of class \code{mfrm_comparison} (named list) with:
\itemize{
\item \code{table}: data.frame of model-level statistics (LogLik, AIC, BIC,
Delta_AIC, AkaikeWeight, Delta_BIC, BICWeight, npar, nobs, Model,
Method, Converged).
\item \code{lrt}: data.frame with likelihood-ratio test result (only when two models
are supplied). Contains \code{ChiSq}, \code{df}, \code{p_value}.
\item \code{evidence_ratios}: data.frame of pairwise Akaike-weight ratios (Model1,
Model2, EvidenceRatio). \code{NULL} when weights cannot be computed.
\item \code{preferred}: named list with the preferred model label by each criterion.
}
}
\description{
Produce a side-by-side comparison of multiple \code{\link[=fit_mfrm]{fit_mfrm()}} results using
information criteria, log-likelihood, and parameter counts. When exactly
two nested models are supplied, a likelihood-ratio test is included.
}
\details{
Models should be fit to the \strong{same data} (same rows, same person/facet
columns) for the comparison to be meaningful. The function checks that
observation counts match and warns otherwise.

The likelihood-ratio test (LRT) is computed only when exactly two models
are supplied and the difference in the number of parameters is positive.
The LRT is most meaningful when the two models are nested (e.g., RSM vs
PCM on the same facets).
}
\section{Information-criterion diagnostics}{

In addition to raw AIC and BIC values, the function computes:
\itemize{
\item \strong{Delta_AIC / Delta_BIC} -- difference from the best (minimum) value.
\item \strong{AkaikeWeight / BICWeight} -- model probabilities derived from
\code{exp(-0.5 * Delta)}, normalised across the candidate set.
\item \strong{Evidence ratios} -- pairwise ratios of Akaike weights, quantifying
the relative evidence for one model over another.
}
}

\section{Interpreting output}{

\itemize{
\item Lower AIC/BIC values indicate better parsimony-accuracy trade-off.
\item A significant LRT p-value suggests the more complex model provides a
meaningfully better fit.
\item \code{preferred} indicates the model preferred by each criterion.
\item \code{evidence_ratios} gives pairwise Akaike-weight ratios (returned only
when Akaike weights can be computed for at least two models).
}
}

\section{Typical workflow}{

\enumerate{
\item Fit two models with \code{\link[=fit_mfrm]{fit_mfrm()}} (e.g., RSM and PCM).
\item Compare with \code{compare_mfrm(fit_rsm, fit_pcm)}.
\item Inspect \code{summary(comparison)} for AIC/BIC/LRT and evidence ratios.
}
}

\examples{
toy <- expand.grid(
  Person = paste0("P", 1:4),
  Rater = paste0("R", 1:2),
  Criterion = c("Content", "Organization", "Language"),
  stringsAsFactors = FALSE
)
toy$Score <- (
  as.integer(factor(toy$Person)) +
  2 * as.integer(factor(toy$Rater)) +
  as.integer(factor(toy$Criterion))
) \%\% 3

fit_jml <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                     method = "JML", model = "RSM", maxit = 25)
fit_mml <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                     method = "MML", model = "RSM", maxit = 25)
comp <- compare_mfrm(fit_jml, fit_mml, labels = c("JML", "MML"))
comp$table
comp$evidence_ratios
}
\seealso{
\code{\link[=fit_mfrm]{fit_mfrm()}}, \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}
}
