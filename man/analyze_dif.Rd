% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-advanced.R
\name{analyze_dif}
\alias{analyze_dif}
\title{Differential item/facet functioning analysis}
\usage{
analyze_dif(
  fit,
  diagnostics,
  facet,
  group,
  data = NULL,
  focal = NULL,
  method = c("residual", "refit"),
  min_obs = 10,
  p_adjust = "holm"
)
}
\arguments{
\item{fit}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{diagnostics}{Output from \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}.}

\item{facet}{Character scalar naming the facet whose elements are tested
for DIF (e.g., \code{"Criterion"} or \code{"Rater"}).}

\item{group}{Character scalar naming the column in the data that
defines the grouping variable (e.g., \code{"Gender"}, \code{"Site"}).}

\item{data}{Optional data frame containing at least the group column
and the same person/facet/score columns used to fit the model. If
\code{NULL} (default), the data stored in \code{fit$prep$data} is used.}

\item{focal}{Optional character vector of group levels to treat as focal.
If \code{NULL} (default), all pairwise group comparisons are performed.}

\item{method}{Analysis method: \code{"residual"} (default) uses the fitted
model's residuals without re-estimation; \code{"refit"} re-estimates the
model within each group subset. The residual method is faster and
avoids convergence issues with small subsets.}

\item{min_obs}{Minimum number of observations per cell (facet-level x
group). Cells below this threshold are flagged as sparse and their
statistics set to \code{NA}. Default \code{10}.}

\item{p_adjust}{Method for multiple-comparison adjustment, passed to
\code{\link[stats:p.adjust]{stats::p.adjust()}}. Default is \code{"holm"}.}
}
\value{
An object of class \code{mfrm_dif} (named list) with:
\itemize{
\item \code{dif_table}: data.frame of DIF contrasts.
\item \code{cell_table}: (residual method) per-cell detail table.
\item \code{summary}: counts by ETS classification.
\item \code{group_fits}: (refit method) per-group facet estimates.
\item \code{config}: list with facet, group, method, min_obs, p_adjust settings.
}
}
\description{
Tests whether the difficulty of facet levels differs across a grouping
variable (e.g., whether rater severity differs for male vs. female
examinees, or whether item difficulty differs across rater subgroups).
}
\details{
Two methods are available:

\strong{Residual method} (\code{method = "residual"}): Uses the existing fitted
model's observation-level residuals from \code{compute_obs_table()}. For each
facet-level x group cell, the observed and expected score sums are
aggregated and a standardized residual is computed as:
\deqn{z = \frac{\sum Obs - \sum Exp}{\sqrt{\sum Var}}}
Pairwise contrasts between groups compare the mean observed-minus-expected
difference for each facet level.

\strong{Refit method} (\code{method = "refit"}): Subsets the data by group, refits
the MFRM model within each subset, and compares the resulting facet-level
estimates using a Welch t-statistic:
\deqn{t = \frac{\hat{\delta}_1 - \hat{\delta}_2}{\sqrt{SE_1^2 + SE_2^2}}}

Effect size is classified following ETS guidelines: negligible (A: < 0.43
logits), moderate (B: 0.43--0.64), or large (C: >= 0.64).
}
\section{Interpreting output}{

\itemize{
\item \verb{$dif_table}: one row per facet-level x group-pair with contrast,
SE, t-statistic, p-value, adjusted p-value, effect size, and
ETS classification. Includes \code{Method}, \code{N_Group1}, \code{N_Group2},
and \code{sparse} columns.
\item \verb{$cell_table}: (residual method only) per-cell detail with N,
ObsScore, ExpScore, ObsExpAvg, StdResidual.
\item \verb{$summary}: counts of negligible/moderate/large DIF items.
\item \verb{$group_fits}: (refit method only) list of per-group facet estimates.
}
}

\section{Typical workflow}{

\enumerate{
\item Fit a model with \code{\link[=fit_mfrm]{fit_mfrm()}}.
\item Run \code{analyze_dif(fit, diagnostics, facet = "Criterion", group = "Gender", data = my_data)}.
\item Inspect \verb{$dif_table} for flagged levels and \verb{$summary} for counts.
\item Use \code{\link[=dif_interaction_table]{dif_interaction_table()}} for a detailed cell-level breakdown.
}
}

\examples{
set.seed(42)
toy <- expand.grid(
  Person = paste0("P", 1:8),
  Rater = paste0("R", 1:3),
  Criterion = c("Content", "Organization"),
  stringsAsFactors = FALSE
)
toy$Score <- sample(0:2, nrow(toy), replace = TRUE)
toy$Group <- ifelse(as.integer(factor(toy$Person)) <= 4, "A", "B")

fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                 method = "JML", model = "RSM", maxit = 25)
diag <- diagnose_mfrm(fit, residual_pca = "none")
dif <- analyze_dif(fit, diag, facet = "Rater", group = "Group", data = toy)
dif$dif_table
}
\seealso{
\code{\link[=fit_mfrm]{fit_mfrm()}}, \code{\link[=estimate_bias]{estimate_bias()}}, \code{\link[=compare_mfrm]{compare_mfrm()}},
\code{\link[=dif_interaction_table]{dif_interaction_table()}}, \code{\link[=plot_dif_heatmap]{plot_dif_heatmap()}}, \code{\link[=dif_report]{dif_report()}}
}
