% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-plotting-response-diagnostics.R
\name{plot.mfrm_response_diagnostics}
\alias{plot.mfrm_response_diagnostics}
\title{Plot descriptive posterior predictive residual summaries}
\usage{
\method{plot}{mfrm_response_diagnostics}(
  x,
  facet = NULL,
  style = c("paired", "scatter"),
  draw = TRUE,
  palette = c("accessible", "mono"),
  title = NULL,
  caption = NULL,
  show_title = TRUE,
  show_notes = TRUE,
  show_labels = TRUE,
  text_scale = 1,
  point_size = 2.5,
  ...
)
}
\arguments{
\item{x}{Saved \code{\link[=mfrm_response_diagnostics]{mfrm_response_diagnostics()}} output.}

\item{facet}{One recorded grouping column; default is the first.}

\item{style}{\code{"paired"} connects each group's Infit and Outfit on a common
axis. \code{"scatter"} plots Infit against Outfit; labels identify groups.}

\item{draw}{\code{FALSE} returns plot data without opening a device.}

\item{palette}{\code{"accessible"} uses blue circles and orange triangles;
\code{"mono"} retains distinct symbols in black. No red/green warning scale.}

\item{title, caption}{\code{NULL} uses the default text; a string replaces it;
\code{""} removes it. Interpretation notes remain in the saved plot data.}

\item{show_title, show_notes}{Show the title and caption, respectively.
\code{FALSE} hides text without deleting its metadata.}

\item{show_labels}{Show group labels. Unavailable groups keep their labels
in the paired view; both views retain all rows in \code{\link[=plot_data]{plot_data()}}.}

\item{text_scale}{Positive multiplier for text size.}

\item{point_size}{Positive point size in ggplot millimetres; base graphics
use the corresponding relative size (2.5 is the default).}

\item{...}{Unused; ordinary-model threshold arguments are not accepted.}
}
\value{
Invisibly, \code{mfrm_plot_data} with selected measures, display settings,
notes and alternative text. \code{\link[=as_ggplot]{as_ggplot()}} converts either view.
}
\description{
Compare residual summaries across selected groups without reference cutoffs.
}
\details{
The response probabilities integrate the latent effects conditional
on the same observed responses and fixed calibration. These are descriptive
summaries, not ordinary plug-in Infit/Outfit. Neither an expectation of
one nor a universal acceptable range is established. No interval, warning
region or statistical test is drawn. Hiding annotations changes only the
display; \code{\link[=plot_data]{plot_data()}} retains the probability definition and limitations.
}
