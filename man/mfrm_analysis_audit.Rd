% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-analysis-audit.R
\name{mfrm_analysis_audit}
\alias{mfrm_analysis_audit}
\title{Build a cross-analysis audit for an MFRM workflow}
\usage{
mfrm_analysis_audit(
  fit,
  diagnostics = NULL,
  generalizability = NULL,
  generalizability_bootstrap = NULL,
  comparison = NULL,
  d_study = NULL,
  bias_results = NULL,
  resamples = NULL,
  hierarchical_structure = NULL,
  run_diagnostics = FALSE
)
}
\arguments{
\item{fit}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{diagnostics}{Optional output from \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}.}

\item{generalizability}{Optional output from \code{\link[=mfrm_generalizability]{mfrm_generalizability()}}.}

\item{generalizability_bootstrap}{Optional output from
\code{\link[=bootstrap_mfrm_generalizability]{bootstrap_mfrm_generalizability()}}.}

\item{comparison}{Optional output from \code{\link[=compare_mfrm_generalizability]{compare_mfrm_generalizability()}}.}

\item{d_study}{Optional output from \code{\link[=mfrm_d_study]{mfrm_d_study()}}.}

\item{bias_results}{Optional bias/DIF object or list, for example from
\code{\link[=estimate_bias]{estimate_bias()}}, \code{\link[=analyze_dff]{analyze_dff()}}, or \code{\link[=analyze_dif_mh]{analyze_dif_mh()}}.}

\item{resamples}{Optional output from \code{\link[=draw_mfrm_resamples]{draw_mfrm_resamples()}}.}

\item{hierarchical_structure}{Optional output from
\code{\link[=analyze_hierarchical_structure]{analyze_hierarchical_structure()}}.}

\item{run_diagnostics}{Logical. If \code{TRUE} and \code{diagnostics = NULL}, compute
\code{\link[=diagnose_mfrm]{diagnose_mfrm()}} with \code{residual_pca = "none"}. The default \code{FALSE}
avoids surprising long-running work.}
}
\value{
An object of class \code{mfrm_analysis_audit}, a list with \code{overview},
\code{checkpoints}, \code{next_actions}, and \code{components}.
}
\description{
\code{mfrm_analysis_audit()} is a lightweight review layer over existing analysis
objects. It does not fit new models by default and it does not turn evidence
into a single pass/fail decision. Instead, it records which parts of a
defensible MFRM workflow are present, which raise review signals, and which
remain unrun.
When a \code{\link[=mfrm_generalizability]{mfrm_generalizability()}} object carries its design check, the
review adds a \code{g_study_interaction_identifiability} checkpoint for
interaction and highest-order cell review.
}
\examples{
\dontrun{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 30)
diag <- diagnose_mfrm(fit, residual_pca = "none")
analysis_review <- mfrm_analysis_audit(fit, diagnostics = diag)
analysis_review$checkpoints
}
}
