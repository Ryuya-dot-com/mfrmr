% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-simulation-misfit.R
\name{plot_simulation_misfit_rates}
\alias{plot_simulation_misfit_rates}
\title{Plot simulation underfit and overfit rates}
\usage{
plot_simulation_misfit_rates(
  x,
  facet = NULL,
  x_var = NULL,
  group_var = NULL,
  directions = c("underfit", "overfit", "mixed"),
  draw = TRUE
)
}
\arguments{
\item{x}{Output from \code{\link[=evaluate_mfrm_design]{evaluate_mfrm_design()}}, \code{summary()}, or
\code{\link[=summarize_simulation_misfit]{summarize_simulation_misfit()}}.}

\item{facet}{Optional facet filter.}

\item{x_var}{Design variable for the x-axis. When \code{NULL}, the first varying
design variable is used; if none varies, \code{Facet} is used.}

\item{group_var}{Optional design variable used in labels.}

\item{directions}{Direction rates to include. Use \code{mnsq_any} for all
MnSq-band flags and \code{zstd_any} for the legacy \eqn{|ZSTD| > 2} rate.}

\item{draw}{If \code{TRUE}, draw a stacked base-R bar chart.}
}
\value{
An \code{mfrm_plot_data} object.
}
\description{
Plot directional misfit rates from \code{\link[=summarize_simulation_misfit]{summarize_simulation_misfit()}}. With
\code{draw = FALSE}, the function returns a tidy plotting payload for custom
graphics.
}
\examples{
\donttest{
sim_eval <- suppressWarnings(evaluate_mfrm_design(
  n_person = c(20, 30),
  n_rater = 3,
  n_criterion = 2,
  raters_per_person = 2,
  reps = 1,
  maxit = 10,
  seed = 42
))
plot_simulation_misfit_rates(sim_eval, draw = FALSE)
}
}
\seealso{
\code{\link[=summarize_simulation_misfit]{summarize_simulation_misfit()}}, \code{\link[=evaluate_mfrm_design]{evaluate_mfrm_design()}},
\code{\link[=plot_fit_direction_summary]{plot_fit_direction_summary()}}
}
