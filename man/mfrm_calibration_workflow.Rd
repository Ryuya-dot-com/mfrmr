% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-calibration.R
\name{mfrm_calibration_workflow}
\alias{mfrm_calibration_workflow}
\alias{extract_mfrm_calibration}
\alias{review_mfrm_calibration}
\alias{validate_mfrm_calibration}
\alias{freeze_mfrm_calibration}
\alias{supersede_mfrm_calibration}
\alias{retire_mfrm_calibration}
\alias{save_mfrm_calibration}
\alias{load_mfrm_calibration}
\alias{score_mfrm_calibration}
\title{Create and use a portable fixed calibration}
\usage{
extract_mfrm_calibration(
  fit,
  calibration_id = NULL,
  source_fit_id = NULL,
  created_at_utc = NULL,
  scoring_quad_points = 31L
)

review_mfrm_calibration(calibration)

validate_mfrm_calibration(calibration, validated_at_utc = NULL)

freeze_mfrm_calibration(calibration, frozen_at_utc = NULL)

supersede_mfrm_calibration(calibration, record_id, superseded_at_utc = NULL)

retire_mfrm_calibration(calibration, record_id, retired_at_utc = NULL)

save_mfrm_calibration(calibration, file, overwrite = FALSE)

load_mfrm_calibration(file)

score_mfrm_calibration(
  calibration,
  new_data,
  person = NULL,
  facets = NULL,
  score = NULL,
  weight = NULL,
  interval_level = 0.95,
  missing_response = "error",
  event_id = NULL
)
}
\arguments{
\item{fit}{An eligible \code{mfrm_fit} produced by \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{calibration_id}{Optional nonempty calibration identifier.}

\item{source_fit_id}{Optional nonempty source-fit identifier.}

\item{created_at_utc}{Optional RFC3339 UTC timestamp. Omit it to use the
current time.}

\item{scoring_quad_points}{Integer quadrature order of at least 2 used for
later artifact scoring. It is independent of the fit-time quadrature and
defaults to 31.}

\item{calibration}{An \code{mfrm_calibration} object.}

\item{validated_at_utc, frozen_at_utc}{Optional RFC3339 UTC timestamps for
reproducible lifecycle records. Omit them to use the current time.}

\item{record_id}{A nonempty identifier for a terminal superseded or retired
record. It must differ from the frozen parent identifier.}

\item{superseded_at_utc, retired_at_utc}{Optional RFC3339 UTC timestamps.}

\item{file}{A path ending in \code{.rds}.}

\item{overwrite}{Whether an existing persistence target may be replaced.}

\item{new_data}{A data frame of response rows for new Persons.}

\item{person, score, weight, event_id}{Optional input-column names. Stored
source-column names are used when applicable. \code{event_id} distinguishes
otherwise duplicate response events.}

\item{facets}{Optional named character vector mapping stored facet names to
input columns.}

\item{interval_level}{Central posterior interval level, strictly between 0
and 1.}

\item{missing_response}{Either \code{"error"} or \code{"omit"}.}
}
\value{
\code{extract_mfrm_calibration()}, \code{validate_mfrm_calibration()},
\code{freeze_mfrm_calibration()}, \code{supersede_mfrm_calibration()},
\code{retire_mfrm_calibration()}, and \code{load_mfrm_calibration()} return an
\code{mfrm_calibration}. \code{review_mfrm_calibration()} returns a data frame of
structured refusals, with zero rows for an acceptable object.
\code{save_mfrm_calibration()} invisibly returns the normalized path.
\code{score_mfrm_calibration()} returns an \code{mfrm_calibration_score} containing
estimates plus row and Person dispositions and scoring identities.
}
\description{
These functions implement a strict lifecycle for a saved, versioned
calibration. \code{\link[=extract_mfrm_calibration]{extract_mfrm_calibration()}} creates a draft from an eligible
\code{RSM} or \code{PCM} MML fit under the fixed standard-normal scoring basis.
\code{\link[=validate_mfrm_calibration]{validate_mfrm_calibration()}} and \code{\link[=freeze_mfrm_calibration]{freeze_mfrm_calibration()}} are separate,
fail-closed transitions. Only a frozen artifact can be passed to
\code{\link[=score_mfrm_calibration]{score_mfrm_calibration()}}.
}
\details{
The portable 0.2.4 workflow supports one observed score scale, one latent
dimension, known non-Person facet levels, and stored two-way facet
interactions. Estimated-population or latent-regression MML, JML, and
bounded \code{GPCM} remain available only through their fitted-object routes; see
\code{\link[=mfrm_calibration_capabilities]{mfrm_calibration_capabilities()}}.

Posterior EAP estimates, posterior standard deviations, and intervals are
conditional on the frozen point calibration and its recorded fixed
standard-normal prior. They do not include calibration-parameter
uncertainty. Loading validates structure and semantic consistency, but does
not authenticate an artifact from an untrusted source.
}
\seealso{
\link{mfrm_calibration_score_methods} for concise review and
visualization of returned score batches.
}
