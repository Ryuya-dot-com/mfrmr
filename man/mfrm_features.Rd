% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-feature-clustering.R
\name{mfrm_features}
\alias{mfrm_features}
\alias{print.mfrm_features}
\alias{summary.mfrm_features}
\title{Prepare external features for exploratory grouping}
\usage{
mfrm_features(data, id, features, missing_reasons = NULL)

\method{print}{mfrm_features}(x, ...)

\method{summary}{mfrm_features}(object, ...)
}
\arguments{
\item{data}{A data frame with one row per entity. Repeated rating rows must
be summarized or joined to an entity-level table before calling this function.}

\item{id}{Name of the unique, nonmissing identifier column. Character, factor,
or finite numeric identifiers are stored as character strings without trimming.}

\item{features}{Explicit character vector of feature column names, excluding
the identifier. Numeric, character, factor, ordered factor, and logical
columns are supported. Character columns become nominal factors; logical
columns represent symmetric binary features. Ordered factor levels retain
their declared order. Dates, lists, and matrices must be converted explicitly.}

\item{missing_reasons}{Optional data frame with columns \code{ID}, \code{Feature}, and
\code{Reason}, one row per annotated missing cell. IDs refer to \code{id}; features
must be selected columns. Reasons for observed or unknown cells are refused.
Unannotated missing cells are labelled "Not supplied". Reasons are supplied
by the user, not inferred missing-data mechanisms.}

\item{x, object}{An object returned by the corresponding function.}

\item{...}{Reserved for method compatibility.}
}
\value{
An \code{mfrm_features} object containing \code{data}, \code{id}, \code{features}, a
\code{feature_summary}, a \code{row_summary}, and \code{missing} (all missing cells and
their reasons). No values are imputed or rows discarded.
}
\description{
Keep one row per Person, rater, or other entity, review feature availability,
and optionally attach user-supplied reasons for missing values.
}
\details{
Numeric \code{NA} and \code{NaN} are missing. Infinite numeric values and blank
categorical labels are refused; replace missing markers with \code{NA} explicitly.
Constant and entirely missing features remain available for review but
cannot be used by \code{\link[=mfrm_cluster]{mfrm_cluster()}}. IDs and unselected columns do not enter
distances. These functions are intended for external attributes such as
training, experience, or specialization. They do not propagate uncertainty
from estimated ability, severity, or fit statistics.
See \code{vignette("mfrmr-external-features")} for a complete rater-attribute
example including missingness review and multiple imputation.
}
\examples{
# Fictional rater attributes; experience is measured in completed years.
raters <- data.frame(
  Rater = paste0("R", 1:6),
  ExperienceYears = c(1, NA, 3, 12, 13, 14),
  Specialty = c("Language", "Language", "Language", "Science", "Science", "Science")
)
reasons <- data.frame(ID = "R2", Feature = "ExperienceYears", Reason = "Not recorded")
features <- mfrm_features(raters, "Rater", c("ExperienceYears", "Specialty"),
                          missing_reasons = reasons)
summary(features)
features$missing
features$row_summary
}
\seealso{
\code{\link[=mfrm_cluster]{mfrm_cluster()}}, \code{\link[=mfrm_cluster_imputed]{mfrm_cluster_imputed()}}, \code{\link[=mfrm_cluster_compare]{mfrm_cluster_compare()}}
}
