% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{specifications_report}
\alias{specifications_report}
\title{Build a specification summary report (preferred alias)}
\usage{
specifications_report(
  fit,
  title = NULL,
  data_file = NULL,
  output_file = NULL,
  include_fixed = FALSE
)
}
\arguments{
\item{fit}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{title}{Optional analysis title.}

\item{data_file}{Optional data-file label (for reporting only).}

\item{output_file}{Optional output-file label (for reporting only).}

\item{include_fixed}{If \code{TRUE}, include a FACETS-style fixed-width text block.}
}
\value{
A named list with specification-report components. Class:
\code{mfrm_specifications}.
}
\description{
Build a specification summary report (preferred alias)
}
\details{
\code{summary(out)} is supported through \code{summary()}.
\code{plot(out)} is dispatched through \code{plot()} for class
\code{mfrm_specifications} (\code{type = "facet_elements"},
\code{"anchor_constraints"}, \code{"convergence"}).
}
\section{Interpreting output}{

\itemize{
\item \code{header} / \code{data_spec}: run identity and model settings.
\item \code{facet_labels}: facet sizes and labels.
\item \code{convergence_control}: optimizer configuration and status.
}
}

\section{Typical workflow}{

\enumerate{
\item Generate \code{specifications_report(fit)}.
\item Verify model settings and convergence metadata.
\item Use as Table 1-style documentation in reports.
}
}

\examples{
toy <- expand.grid(
  Person = paste0("P", 1:4),
  Rater = paste0("R", 1:2),
  Criterion = c("Content", "Organization", "Language"),
  stringsAsFactors = FALSE
)
toy$Score <- (
  as.integer(factor(toy$Person)) +
  2 * as.integer(factor(toy$Rater)) +
  as.integer(factor(toy$Criterion))
) \%\% 3
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score", method = "JML", maxit = 25)
out <- specifications_report(fit, title = "Toy run")
summary(out)
p_spec <- plot(out, draw = FALSE)
class(p_spec)
}
\seealso{
\code{\link[=fit_mfrm]{fit_mfrm()}}, \code{\link[=data_quality_report]{data_quality_report()}}, \code{\link[=estimation_iteration_report]{estimation_iteration_report()}}
}
