% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-residual-dimensionality.R
\name{check_residual_dimensionality}
\alias{check_residual_dimensionality}
\title{Check residual dimensionality with parallel-analysis thresholds}
\usage{
check_residual_dimensionality(
  x,
  mode = c("both", "overall", "facet"),
  facets = NULL,
  method = c("residual_normal", "permutation", "parametric"),
  reps = 100L,
  quantile = 0.95,
  pca_max_factors = 10L,
  seed = NULL
)
}
\arguments{
\item{x}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}}, \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}, or
\code{\link[=analyze_residual_pca]{analyze_residual_pca()}}.}

\item{mode}{Residual matrix scope: \code{"both"}, \code{"overall"}, or \code{"facet"}.}

\item{facets}{Optional facet subset for facet-level matrices. When supplied,
the same subset is also used to form the overall combined-facet residual
matrix.}

\item{method}{Null-generation method. \code{"residual_normal"} simulates
independent standard-normal residual matrices with the same missingness
pattern; \code{"permutation"} permutes observed standardized residuals within
each residual column; \code{"parametric"} simulates responses from the fitted
model and is available only when \code{x} is an \code{mfrm_fit}.}

\item{reps}{Number of null replications.}

\item{quantile}{Parallel-analysis quantile used as the decision threshold.
The default \code{.95} follows the conservative Glorfeld-style convention.}

\item{pca_max_factors}{Maximum number of components retained in output, or
\code{"auto"} to use the matrix rank cap used by \code{\link[=analyze_residual_pca]{analyze_residual_pca()}}.}

\item{seed}{Optional random seed for reproducible null simulations.}
}
\value{
An object of class \code{mfrm_residual_dimensionality}, containing:
\itemize{
\item \code{observed}: observed residual PCA eigenvalue table
\item \code{null_distribution}: replication-level null eigenvalues
\item \code{comparison}: observed eigenvalues joined to null mean, SD, and quantile
\item \code{settings}: method, repetitions, quantile, and PCA settings
}
}
\description{
Check residual dimensionality with parallel-analysis thresholds
}
\details{
This function adds a simulation-calibrated layer to the residual PCA tools.
It compares each observed residual eigenvalue with eigenvalues obtained
under a unidimensional null reference.

The three null methods answer different questions:
\itemize{
\item \code{"residual_normal"} is Horn-style parallel analysis on independent
normal residual matrices with the observed matrix shape and missingness.
\item \code{"permutation"} preserves the empirical column distributions of the
standardized residual matrix, while removing cross-column association.
\item \code{"parametric"} samples categorical responses from the fitted \code{mfrmr}
model, then recomputes standardized residual matrices using the fitted
expected scores and variances. It preserves the observed design and the
fitted category-response model, but it does not refit the model in each
replication.
}

The procedure is exploratory. It is not FACETS ZSTD, TAM itemfit ZSTD,
or mirt's S-X2 item-fit statistic. It is a residual-structure diagnostic
for deciding whether residual components are larger than expected under a
chosen null reference.
}
\section{Mathematical definition}{

For a standardized residual matrix \code{R}, \code{mfrmr} computes the eigenvalues
of the positive-definite adjusted residual correlation matrix. In parallel
analysis, the observed eigenvalue \code{lambda_j} is compared with a null
threshold \code{q_j}, the selected quantile of simulated null eigenvalues for
component \code{j}. The component is flagged when \code{lambda_j > q_j}.
}

\section{References}{

\itemize{
\item Horn, J. L. (1965). A rationale and test for the number of factors in
factor analysis. \emph{Psychometrika}, 30, 179-185.
\item Glorfeld, L. W. (1995). An improvement on Horn's parallel analysis
methodology. \emph{Educational and Psychological Measurement}, 55, 377-393.
\item Linacre, J. M. (1998). Structure in Rasch residuals: Why principal
components analysis (PCA)? \emph{Rasch Measurement Transactions}, 12(2), 636.
\item Linacre, J. M. (1998). Detecting multidimensionality: Which residual
data-type works best? \emph{Journal of Outcome Measurement}, 2(3), 266-283.
\item Chou, Y.-T., & Wang, W.-C. (2010). Checking dimensionality in item
response models with principal component analysis on standardized
residuals. \emph{Educational and Psychological Measurement}, 70, 717-731.
\item Timmerman, M. E., & Lorenzo-Seva, U. (2011). Dimensionality assessment of
ordered polytomous items with parallel analysis. \emph{Psychological Methods},
16, 209-220.
}
}

\examples{
\donttest{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 20)
dim_check <- check_residual_dimensionality(
  fit,
  mode = "overall",
  method = "parametric",
  reps = 5,
  seed = 123
)
dim_check
head(as.data.frame(dim_check))
plot_residual_dimensionality(dim_check, draw = FALSE)$data$data
}
}
\seealso{
\code{\link[=analyze_residual_pca]{analyze_residual_pca()}}, \code{\link[=plot_residual_dimensionality]{plot_residual_dimensionality()}},
\code{\link[=plot_residual_pca]{plot_residual_pca()}}
}
