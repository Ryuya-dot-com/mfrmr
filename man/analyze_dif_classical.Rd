% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-classical-dif.R
\name{analyze_dif_classical}
\alias{analyze_dif_classical}
\title{Classical DIF screening for long-format many-facet data}
\usage{
analyze_dif_classical(
  x,
  facet,
  group,
  data = NULL,
  score = NULL,
  person = NULL,
  methods = c("mantel_haenszel", "logistic"),
  focal = NULL,
  min_obs = 10L,
  match_bins = 5L,
  p_adjust = "holm",
  logistic_threshold = NULL
)
}
\arguments{
\item{x}{An \code{mfrm_fit} or a data frame.}

\item{facet}{Facet/item column to screen level by level.}

\item{group}{Grouping column.}

\item{data}{Optional original data when \code{x} is a fit and \code{group} is not in
\code{fit$prep$data}.}

\item{score}{Score column. Inferred from \code{x} when \code{x} is an \code{mfrm_fit};
defaults to \code{"Score"} for data frames.}

\item{person}{Person/respondent column. Inferred from \code{x} when possible;
defaults to \code{"Person"} for data frames.}

\item{methods}{One or more of \code{"mantel_haenszel"} and \code{"logistic"}.}

\item{focal}{Optional focal group level(s). If omitted, all group pairs are
compared.}

\item{min_obs}{Minimum person-level observations per group and facet level.}

\item{match_bins}{Number of total-score strata used by the generalized
Mantel-Haenszel route when the matching score has many distinct values.}

\item{p_adjust}{Adjustment method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{logistic_threshold}{Numeric threshold for binary logistic DIF on
polytomous scores. Scores greater than or equal to this value are coded 1.}
}
\value{
An object of class \code{mfrm_dff} / \code{mfrm_dif} with \code{dif_table},
\code{cell_table}, \code{summary}, and \code{config} fields.
}
\description{
Runs limited classical DIF screens on a long-format score table. The
Mantel-Haenszel route uses a generalized Cochran-Mantel-Haenszel test over
ordered score categories and total-score strata. The logistic route is a
binary logistic-regression screen; for polytomous data it runs only when
\code{logistic_threshold} is supplied, making the dichotomization explicit.
}
\details{
Rows are first collapsed to one mean score per \code{person} x \code{group} x
\code{facet} level. The matching variable for a target level is the person's
total observed score across the screened facet minus the target-level
score, so the target response is not used to condition on itself. When the
matching score has more distinct values than \code{match_bins}, quantile bins are
used as score strata.

The Mantel-Haenszel option forms a
\eqn{Group \times Score \times MatchStratum} table and calls
\code{\link[stats:mantelhaen.test]{stats::mantelhaen.test()}} without continuity correction. This is a
generalized Cochran-Mantel-Haenszel screening p value for ordered score
categories. The reported \code{Contrast} remains the simple Group2-minus-Group1
mean score difference for direction; it is not a Mantel-Haenszel common odds
ratio.

The logistic option fits binary models
\deqn{Y \sim MatchScore,}
\deqn{Y \sim MatchScore + Group,}
\deqn{Y \sim MatchScore * Group.}
The \code{logistic_uniform} row is the likelihood-ratio comparison of the first
two models. The \code{logistic_nonuniform} row is the likelihood-ratio comparison
of the second and third models. For polytomous scores, \code{Y} is defined as
\eqn{1(Score \ge logistic_threshold)}; no implicit dichotomization is used.
}
\section{Scope}{

This is a classical screening helper, not a replacement for
\code{\link[=analyze_dff]{analyze_dff()}} or SIBTEST. It does not estimate MFRM subgroup parameters,
does not use anchors, and does not claim ETS A/B/C classifications.
}

\section{References}{

\itemize{
\item Mantel, N., & Haenszel, W. (1959). Statistical aspects of the analysis of
data from retrospective studies of disease. \emph{Journal of the National
Cancer Institute, 22}, 719-748.
\item Holland, P. W., & Thayer, D. T. (1988). Differential item performance and
the Mantel-Haenszel procedure. In H. Wainer & H. I. Braun (Eds.),
\emph{Test validity}.
\item Swaminathan, H., & Rogers, H. J. (1990). Detecting differential item
functioning using logistic regression procedures. \emph{Journal of Educational
Measurement, 27}(4), 361-370.
}
}

\examples{
toy <- load_mfrmr_data("example_bias")
cls <- analyze_dif_classical(
  toy, facet = "Criterion", group = "Group",
  person = "Person", score = "Score",
  methods = "mantel_haenszel"
)
cls$summary
}
