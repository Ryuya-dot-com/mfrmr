% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-advanced.R
\name{detect_anchor_drift}
\alias{detect_anchor_drift}
\alias{print.mfrm_anchor_drift}
\alias{summary.mfrm_anchor_drift}
\alias{print.summary.mfrm_anchor_drift}
\title{Detect anchor drift across multiple calibrations}
\usage{
detect_anchor_drift(
  fits,
  facets = NULL,
  drift_threshold = 0.5,
  flag_se_ratio = 2,
  reference = 1L,
  include_person = FALSE
)

\method{print}{mfrm_anchor_drift}(x, ...)

\method{summary}{mfrm_anchor_drift}(object, ...)

\method{print}{summary.mfrm_anchor_drift}(x, ...)
}
\arguments{
\item{fits}{Named list of \code{mfrm_fit} objects (e.g.,
\code{list(Year1 = fit1, Year2 = fit2)}).}

\item{facets}{Character vector of facets to compare (default: all
non-Person facets).}

\item{drift_threshold}{Absolute drift threshold for flagging (logits,
default 0.5).}

\item{flag_se_ratio}{Drift/SE ratio threshold for flagging (default 2.0).}

\item{reference}{Index or name of the reference fit (default: first).}

\item{include_person}{Include person estimates in comparison.}

\item{x}{An \code{mfrm_anchor_drift} object.}

\item{...}{Ignored.}

\item{object}{An \code{mfrm_anchor_drift} object (for \code{summary}).}
}
\value{
Object of class \code{mfrm_anchor_drift} with components:
\describe{
\item{drift_table}{Tibble of element-level drift statistics.}
\item{summary}{Drift summary aggregated by facet and wave.}
\item{common_elements}{Pairwise counts of common elements.}
\item{config}{List of analysis configuration.}
}
}
\description{
Compares facet estimates across two or more calibration waves to identify
elements whose difficulty/severity has shifted beyond acceptable thresholds.
Useful for monitoring rater drift over time or checking the stability of
item banks.
}
\details{
For each non-reference wave, the function extracts facet-level estimates
using \code{\link[=make_anchor_table]{make_anchor_table()}} and computes the element-by-element difference
against the reference wave.  Standard errors are obtained from
\code{\link[=diagnose_mfrm]{diagnose_mfrm()}} applied to each fit.  Only elements common to both the
reference and a comparison wave are included.

An element is \strong{flagged} when either condition is met:
\deqn{|\Delta_e| > \texttt{drift\_threshold}}
\deqn{|\Delta_e / SE_e| > \texttt{flag\_se\_ratio}}
The dual-criterion approach guards against flagging elements with large
but imprecise estimates, and against missing small but precisely estimated
shifts.

When \code{facets} is \code{NULL}, all non-Person facets are compared.  Providing a
subset (e.g., \code{facets = "Criterion"}) restricts comparison to those facets
only.
}
\section{Interpreting output}{

\itemize{
\item \verb{$drift_table}: one row per element x wave combination, with columns
\code{Facet}, \code{Level}, \code{Wave}, \code{Ref_Est}, \code{Wave_Est}, \code{Drift}, \code{SE},
\code{Drift_SE_Ratio}, and \code{Flag}.  Large drift signals instability.
\item \verb{$summary}: aggregated statistics by facet and wave: number of elements,
mean/max absolute drift, and count of flagged elements.
\item \verb{$common_elements}: matrix of pairwise common element counts.  Small
overlap weakens the comparison and results should be interpreted
cautiously.
\item \verb{$config}: records the analysis parameters for reproducibility.
}
}

\section{Typical workflow}{

\enumerate{
\item Fit separate models for each administration wave.
\item Combine into a named list: \code{fits <- list(Spring = fit_s, Fall = fit_f)}.
\item Call \code{drift <- detect_anchor_drift(fits)}.
\item Review \code{summary(drift)} and \code{plot_anchor_drift(drift)}.
\item Flagged elements may need to be removed from anchor sets or
investigated for substantive causes (e.g., rater re-training).
}
}

\examples{
d1 <- load_mfrmr_data("study1")
d2 <- load_mfrmr_data("study2")
fit1 <- fit_mfrm(d1, "Person", c("Rater", "Criterion"), "Score",
                 method = "JML", maxit = 25)
fit2 <- fit_mfrm(d2, "Person", c("Rater", "Criterion"), "Score",
                 method = "JML", maxit = 25)
drift <- detect_anchor_drift(list(Wave1 = fit1, Wave2 = fit2))
summary(drift)
drift$drift_table
}
\seealso{
\code{\link[=anchor_to_baseline]{anchor_to_baseline()}}, \code{\link[=build_equating_chain]{build_equating_chain()}},
\code{\link[=make_anchor_table]{make_anchor_table()}}, \code{\link[=plot_anchor_drift]{plot_anchor_drift()}}
}
