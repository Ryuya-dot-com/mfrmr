% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-plotting-screening.R
\name{plot_rater_agreement_heatmap}
\alias{plot_rater_agreement_heatmap}
\title{Pairwise rater-agreement heatmap}
\usage{
plot_rater_agreement_heatmap(
  fit,
  diagnostics = NULL,
  rater_facet = "Rater",
  metric = c("exact", "correlation"),
  preset = c("standard", "publication", "compact", "monochrome"),
  draw = TRUE
)
}
\arguments{
\item{fit}{An \code{mfrm_fit}.}

\item{diagnostics}{Optional \code{\link[=diagnose_mfrm]{diagnose_mfrm()}} output; piped through
to \code{\link[=interrater_agreement_table]{interrater_agreement_table()}} when supplied.}

\item{rater_facet}{Name of the rater facet (default \code{"Rater"}).}

\item{metric}{Column to colour by: \code{"exact"} (default) or
\code{"correlation"}. Quadratic-weighted kappa is not currently
computed by \code{\link[=interrater_agreement_table]{interrater_agreement_table()}} and is therefore not
offered here.}

\item{preset}{Visual preset.}

\item{draw}{If \code{TRUE}, draw with base graphics.}
}
\value{
An \code{mfrm_plot_data} object whose \code{data} slot bundles the
rater x rater matrix and the raw pairwise rows.
}
\description{
Summarizes inter-rater agreement as a symmetric rater x rater
heatmap. Cells are coloured by the chosen agreement metric: exact
agreement proportion by default, or the Pearson-style \code{Corr} column
from \code{\link[=interrater_agreement_table]{interrater_agreement_table()}} when \code{metric = "correlation"}.
The plot is a compact alternative to \code{\link[=plot_interrater_agreement]{plot_interrater_agreement()}}'s
bar chart when the rater count exceeds ~6 pairs.
}
\section{Session plot defaults}{

Set \code{options(mfrmr.plot_preset = "publication")} to choose a session default
for plotting functions that expose the common \code{preset} argument. The
supported values are \code{"standard"}, \code{"publication"}, \code{"compact"} and
\code{"monochrome"}. Precedence is an explicit call argument, then the session
option, then \code{"standard"}. For example, \code{preset = "standard"} overrides
a session set to \code{"monochrome"}. Explicit \code{preset = NULL} retains the
earlier package-default behavior; it does not read the session option.
Invalid session values cause an error only when that option is needed.

The category-curve, data-quality, fit-review, connectivity and network
routes of \code{plot()} for report bundles use the same option through \code{...}.
Plots without a common \code{preset} argument, including extended-model plots
with their own \code{palette} controls, keep their own settings. This option
selects a preset, not a universal theme or a guarantee that all renderers
implement every appearance control identically.

New plot payloads retain the resolved preset for supported saved-data
rendering. Converting an existing payload with \code{\link[=as_ggplot]{as_ggplot()}} uses its saved
appearance, even after the session option changes. A call that creates a
new plot from a fit or statistical result uses the current default.
For a reproducible script, supply \code{preset} explicitly or set the option in
that script. Saving only the fitted model does not save a session option.
No global ggplot theme is changed.

Restore previous settings with \code{old <- options(mfrmr.plot_preset = "monochrome")} followed by \code{options(old)}. Use
\code{options(mfrmr.plot_preset = NULL)} to remove the option. The preset changes
appearance, not estimates, confidence levels or diagnostic thresholds.
}

\examples{
\donttest{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 300)
p <- plot_rater_agreement_heatmap(fit, draw = FALSE)
dim(p$data$matrix)
# Look for (default `metric = "exact"`):
# - Off-diagonal cells close to the corresponding entry of
#   `summary(diag)$interrater$ExactAgreement` indicate consistent
#   pair behaviour; cells well below the average mark a pair
#   that disagrees more than the rest.
# - With `metric = "correlation"` the colour scale switches to
#   `[-1, 1]`; positive cells = pairs agree on relative ordering,
#   negative cells = pairs systematically rank persons in opposite
#   directions and are the highest-priority review cases.
}
}
\seealso{
\code{\link[=interrater_agreement_table]{interrater_agreement_table()}} for the underlying numeric
table; \code{\link[=plot_guttman_scalogram]{plot_guttman_scalogram()}} for a complementary
person-by-element view of residual structure;
\code{\link[=diagnose_mfrm]{diagnose_mfrm()}} for the diagnostics bundle the heatmap
reads from.
}
