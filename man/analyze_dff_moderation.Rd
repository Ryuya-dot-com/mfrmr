% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-advanced.R
\name{analyze_dff_moderation}
\alias{analyze_dff_moderation}
\alias{analyze_dif_moderation}
\title{Continuous-covariate differential-functioning moderation screen}
\usage{
analyze_dff_moderation(
  fit,
  facet,
  covariate,
  data = NULL,
  min_obs = 10,
  p_adjust = "holm",
  standardize = TRUE
)

analyze_dif_moderation(...)
}
\arguments{
\item{fit}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{facet}{Character scalar naming the facet whose elements are screened.}

\item{covariate}{Character scalar naming a numeric person-level covariate in
\code{data} or \code{fit$prep$data}. The value must be stable within person; if a
person has multiple finite covariate values, the function stops rather
than silently averaging them.}

\item{data}{Optional original data frame. Use this when the covariate was
not included in the columns retained by \code{fit_mfrm()}.}

\item{min_obs}{Minimum number of usable observations per facet level.
Default \code{10}.}

\item{p_adjust}{Method for multiple-comparison adjustment, passed to
\code{\link[stats:p.adjust]{stats::p.adjust()}}. Default \code{"holm"}.}

\item{standardize}{Logical. If \code{TRUE} (default), the reported \code{Slope} and
\code{SE} are per one sample SD of the covariate. \code{RawSlope} and \code{RawSE} are
always reported on the covariate's original unit.}

\item{...}{Passed directly to \code{analyze_dff_moderation()}.}
}
\value{
Object of class \code{mfrm_dff_moderation} with:
\itemize{
\item \code{moderation_table}: one row per facet level, with slope, SE, z statistic,
adjusted p-value, screening classification, and sample-size diagnostics.
\item \code{summary}: screening classification counts.
\item \code{gpcm_boundary}: bounded-\code{GPCM} scope table when relevant.
\item \code{config}: analysis configuration.
}
}
\description{
Screens whether fitted observation-level residuals for each facet level vary
systematically along a numeric person-level covariate. This is the
continuous-covariate counterpart to residual \code{\link[=analyze_dff]{analyze_dff()}} screening: it is
not a Mantel-Haenszel categorical DIF/DFF group contrast and it does not produce
ETS A/B/C classifications.
}
\details{
The helper computes the fitted observation table, forms the residual
\eqn{X - E(X)}, and fits a model-variance weighted residual slope separately
for each facet level:
\deqn{X_{obs} - E(X) = \alpha_l + \beta_l z + e.}
Weights are the inverse fitted response variance from the model. The
resulting z statistic uses the model-variance weighted covariate dispersion
as a large-sample screening approximation. Rows remain screening evidence:
use them to target follow-up, not as a standalone fairness, invariance, or
operational subgroup-decision claim.

A positive slope means people with higher covariate values scored higher
than the fitted model expected for that facet level; in DIF/DFF wording, the
level screened as relatively easier for higher covariate values. A negative
slope means relatively harder for higher covariate values.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
toy <- load_mfrmr_data("example_bias")
person_age <- data.frame(
  Person = unique(toy$Person),
  Age = seq_along(unique(toy$Person))
)
toy <- merge(toy, person_age, by = "Person", all.x = TRUE)
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", model = "RSM", maxit = 30)
mod <- analyze_dff_moderation(fit, facet = "Criterion",
                              covariate = "Age", data = toy)
mod$moderation_table
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=analyze_dff]{analyze_dff()}}, \code{\link[=analyze_dif]{analyze_dif()}}, \code{\link[=dif_interaction_table]{dif_interaction_table()}}
}
