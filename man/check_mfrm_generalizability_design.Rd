% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-generalizability.R
\name{check_mfrm_generalizability_design}
\alias{check_mfrm_generalizability_design}
\alias{summary.mfrm_generalizability_design_check}
\alias{print.summary.mfrm_generalizability_design_check}
\alias{print.mfrm_generalizability_design_check}
\alias{plot.mfrm_generalizability_design_check}
\title{Check whether a G-study design can support explicit random interactions}
\usage{
check_mfrm_generalizability_design(
  fit,
  data = NULL,
  object_facet = "Person",
  random_facets = NULL,
  random_interactions = NULL,
  min_levels = 2L,
  min_replicates_per_cell = 2L,
  sparse_cell_threshold = 0.5
)

\method{summary}{mfrm_generalizability_design_check}(object, digits = 3, ...)

\method{print}{summary.mfrm_generalizability_design_check}(x, ...)

\method{print}{mfrm_generalizability_design_check}(x, ...)

\method{plot}{mfrm_generalizability_design_check}(
  x,
  y = NULL,
  type = c("interaction_cells", "highest_order", "facet_levels", "overview"),
  metric = NULL,
  status = NULL,
  sort_by = c("status", "value", "name"),
  decreasing = NULL,
  top_n = Inf,
  draw = TRUE,
  main = NULL,
  palette = NULL,
  preset = c("standard", "publication", "compact", "monochrome"),
  ...
)
}
\arguments{
\item{fit}{An \code{mfrm_fit} from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{data}{Optional data frame. When \code{NULL}, the rating data stored on
\code{fit$prep$data} is used.}

\item{object_facet}{Facet that plays the role of the object of measurement.
Defaults to \code{"Person"}.}

\item{random_facets}{Character vector of random measurement facets. Default
uses every facet in \code{fit$config$facet_names} other than \code{object_facet}.}

\item{random_interactions}{Optional explicitly requested two-way random
interactions, such as \code{"Person:Rater"} or
\code{list(c("Rater", "Criterion"))}. The same validation rules as
\code{\link[=mfrm_generalizability]{mfrm_generalizability()}} are used.}

\item{min_levels}{Minimum observed levels required for each facet in a
requested interaction. Defaults to \code{2}.}

\item{min_replicates_per_cell}{Minimum row count used to call an observed
interaction cell replicated. Defaults to \code{2}.}

\item{sparse_cell_threshold}{Minimum observed-cell density for the crossed
interaction table. Densities below this value are flagged for review.}

\item{object}{Output from \code{check_mfrm_generalizability_design()}.}

\item{digits}{Number of digits used by the summary print method.}

\item{...}{Reserved for future extensions.}

\item{x}{Output from \code{check_mfrm_generalizability_design()} or its
\code{summary()} method.}

\item{y}{Reserved for S3 generic compatibility.}

\item{type}{Plot route. \code{"interaction_cells"} displays requested
interaction-cell density or replication, \code{"highest_order"} displays the
full object-by-facet cell review, \code{"facet_levels"} displays observed
facet-level coverage, and \code{"overview"} displays review/sensitivity
counts.}

\item{metric}{Numeric column to plot for the selected route. Defaults are
route-specific; inspect
\code{plot_data_components(plot(x, draw = FALSE))} or the source tables for
alternatives.}

\item{status}{Optional status filter, for example \code{"review"} or
\code{c("review", "sensitivity_only")}.}

\item{sort_by}{Ordering rule: \code{"status"}, \code{"value"}, or
\code{"name"}.}

\item{decreasing}{Logical ordering direction. Defaults to descending except
for \code{sort_by = "name"}.}

\item{top_n}{Optional maximum number of rows to display.}

\item{draw}{If \code{TRUE}, draw with base graphics. If \code{FALSE}, return
an \code{mfrm_plot_data} object.}

\item{main}{Optional plot title.}

\item{palette}{Optional named or unnamed colors for statuses.}

\item{preset}{Plot style preset used by the base-graphics renderer.}
}
\value{
An object of class \code{mfrm_generalizability_design_check}, a list with:
\describe{
\item{\code{overview}}{One-row summary of rows, requested interactions,
review counts, sensitivity-only counts, and highest-order cell status.}
\item{\code{facet_overview}}{Observed levels and rows per level for the
object facet and each random measurement facet.}
\item{\code{interaction_overview}}{One row per requested two-way interaction,
with possible cells, observed cells, density, cell replication, status, main
concern, and recommended action.}
\item{\code{highest_order_review}}{Observed full-cell crossing across the
object facet and all random measurement facets, used to review whether pure
error and highest-order interaction are difficult to separate.}
\item{\code{settings}}{Thresholds and facet choices used by the check.}
\item{\code{notes}}{Interpretation boundaries for the design check.}
}
\code{plot.mfrm_generalizability_design_check(draw = FALSE)} returns an
\code{mfrm_plot_data} object with reusable \code{plot_table},
\code{facet_overview}, \code{interaction_overview},
\code{highest_order_review}, \code{overview}, \code{guidance}, and
\code{figure_recipes} components.
}
\description{
\code{check_mfrm_generalizability_design()} inspects the observed rating
design before the G-study variance-component refit. It does not call
\code{lme4} and it does not estimate variance components. Instead, it reports
whether requested two-way random interactions have enough observed levels,
crossed cells, and exact-cell replication to be interpreted beyond a
sensitivity check.
}
\section{Interpretation}{

The check is descriptive. It cannot guarantee a non-singular mixed model fit,
and it cannot prove that an interaction variance component is substantively
meaningful. Rows marked \code{"sensitivity_only"} or \code{"review"} mean that
the corresponding expanded G-study should be compared with the main-effects
baseline and reported with caution.

The highest-order review is included because common MFRM rating tables have
one observation per person-by-rater-by-task or analogous full cell. In that
case the pure residual term and the highest-order interaction remain difficult
to separate, even when selected two-way interactions are estimable as a
descriptive sensitivity model.
}
\examples{
\dontrun{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 30)
design_check <- check_mfrm_generalizability_design(
  fit,
  random_interactions = c("Person:Rater", "Rater:Criterion")
)
summary(design_check)
design_check$interaction_overview
design_check$highest_order_review
plot(design_check, draw = FALSE)
plot(design_check, type = "highest_order", draw = FALSE)
plot(design_check, type = "facet_levels", metric = "Levels", draw = FALSE)
plot_data_components(plot(design_check, draw = FALSE))
}
}
\seealso{
\code{\link[=mfrm_generalizability]{mfrm_generalizability()}},
\code{\link[=compare_mfrm_generalizability]{compare_mfrm_generalizability()}},
\code{\link[=mfrm_d_study]{mfrm_d_study()}},
\code{\link[=mfrm_analysis_audit]{mfrm_analysis_audit()}},
\code{\link[=plot_data]{plot_data()}},
\code{\link[=as_ggplot]{as_ggplot()}}
}
