% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{facet_statistics_report}
\alias{facet_statistics_report}
\title{Build a facet statistics report (preferred alias)}
\usage{
facet_statistics_report(
  fit,
  diagnostics = NULL,
  metrics = c("Estimate", "Infit", "Outfit", "SE"),
  ruler_width = 41
)
}
\arguments{
\item{fit}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{diagnostics}{Optional output from \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}.}

\item{metrics}{Numeric columns in \code{diagnostics$measures} to summarize.}

\item{ruler_width}{Width of the fixed-width ruler used for \code{M/S/Q/X} marks.}
}
\value{
A named list with facet-statistics components. Class:
\code{mfrm_facet_statistics}.
}
\description{
Build a facet statistics report (preferred alias)
}
\details{
\code{summary(out)} is supported through \code{summary()}.
\code{plot(out)} is dispatched through \code{plot()} for class
\code{mfrm_facet_statistics} (\code{type = "means"}, \code{"sds"}, \code{"ranges"}).
}
\section{Interpreting output}{

\itemize{
\item facet-level means/SD/ranges of selected metrics (\code{Estimate}, fit indices, \code{SE}).
\item fixed-width ruler rows (\code{M/S/Q/X}) for FACETS-like profile scanning.
}
}

\section{Typical workflow}{

\enumerate{
\item Run \code{facet_statistics_report(fit)}.
\item Inspect summary/ranges for anomalous facets.
\item Cross-check flagged facets with fit and chi-square diagnostics.
}
}

\examples{
toy <- expand.grid(
  Person = paste0("P", 1:4),
  Rater = paste0("R", 1:2),
  Criterion = c("Content", "Organization", "Language"),
  stringsAsFactors = FALSE
)
toy$Score <- (
  as.integer(factor(toy$Person)) +
  2 * as.integer(factor(toy$Rater)) +
  as.integer(factor(toy$Criterion))
) \%\% 3
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score", method = "JML", maxit = 25)
out <- facet_statistics_report(fit)
summary(out)
p_fs <- plot(out, draw = FALSE)
class(p_fs)
}
\seealso{
\code{\link[=diagnose_mfrm]{diagnose_mfrm()}}, \code{\link[=summary.mfrm_fit]{summary.mfrm_fit()}}, \code{\link[=plot_facets_chisq]{plot_facets_chisq()}}
}
