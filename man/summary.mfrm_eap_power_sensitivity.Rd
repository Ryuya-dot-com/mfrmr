% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-prediction.R
\name{summary.mfrm_eap_power_sensitivity}
\alias{summary.mfrm_eap_power_sensitivity}
\title{Summarize EAP power-scaling sensitivity output}
\usage{
\method{summary}{mfrm_eap_power_sensitivity}(object, digits = 3, ...)
}
\arguments{
\item{object}{Output from \code{\link[=analyze_eap_power_sensitivity]{analyze_eap_power_sensitivity()}}.}

\item{digits}{Number of digits used in numeric summaries.}

\item{...}{Reserved for generic compatibility.}
}
\value{
An object of class \code{summary.mfrm_eap_power_sensitivity} with:
\itemize{
\item \code{summary}: power-condition summaries of deltas from the reference posterior
\item \code{sensitivity}: person-level sensitivity table
\item \code{baseline}: unscaled reference EAP summaries
\item \code{facet_overview}: exposure-weighted stability summary by facet
\item \code{facet_level_overview}: exposure-weighted stability summary by facet level
\item \code{row_review}: row-preparation review
\item \code{population_review}: optional person-level omission review
\item \code{settings}: sensitivity settings
\item \code{notes}: interpretation notes
}
}
\description{
Summarize EAP power-scaling sensitivity output
}
\examples{
\dontrun{
toy <- load_mfrmr_data("example_core")
keep_people <- unique(toy$Person)[1:18]
toy_fit <- suppressWarnings(
  fit_mfrm(
    toy[toy$Person \%in\% keep_people, , drop = FALSE],
    "Person", c("Rater", "Criterion"), "Score",
    method = "MML",
    quad_points = 5,
    maxit = 30
  )
)
new_units <- data.frame(
  Person = c("NEW01", "NEW01"),
  Rater = unique(toy$Rater)[1],
  Criterion = unique(toy$Criterion)[1:2],
  Score = c(2, 3)
)
sens <- analyze_eap_power_sensitivity(toy_fit, new_units, prior_power = c(0.8, 1))
summary(sens)
}
}
\seealso{
\code{\link[=analyze_eap_power_sensitivity]{analyze_eap_power_sensitivity()}}
}
