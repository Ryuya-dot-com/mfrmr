% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-advanced.R
\name{dif_interaction_table}
\alias{dif_interaction_table}
\title{Compute interaction table between a facet and a grouping variable}
\usage{
dif_interaction_table(
  fit,
  diagnostics,
  facet,
  group,
  data = NULL,
  min_obs = 10,
  p_adjust = "holm",
  abs_t_warn = 2,
  abs_bias_warn = 0.5
)
}
\arguments{
\item{fit}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{diagnostics}{Output from \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}.}

\item{facet}{Character scalar naming the facet.}

\item{group}{Character scalar naming the grouping column.}

\item{data}{Optional data frame with the group column. If \code{NULL}
(default), the data stored in \code{fit$prep$data} is used, but it
must contain the \code{group} column.}

\item{min_obs}{Minimum observations per cell. Cells with fewer than
this many observations are flagged as sparse and their test
statistics set to \code{NA}. Default \code{10}.}

\item{p_adjust}{P-value adjustment method, passed to
\code{\link[stats:p.adjust]{stats::p.adjust()}}. Default \code{"holm"}.}

\item{abs_t_warn}{Threshold for flagging cells by absolute t-value.
Default \code{2}.}

\item{abs_bias_warn}{Threshold for flagging cells by absolute
Obs-Exp average (in logits). Default \code{0.5}.}
}
\value{
Object of class \code{mfrm_dif_interaction} with:
\itemize{
\item \code{table}: tibble with per-cell statistics and flags.
\item \code{summary}: tibble summarizing flagged and sparse cell counts.
\item \code{config}: list of analysis parameters.
}
}
\description{
Produces a FACETS Table 30/31-style interaction table showing
Obs-Exp differences, standardized residuals, and significance
for each facet-level x group-value cell.
}
\details{
This function uses the fitted model's observation-level residuals
(from the internal \code{compute_obs_table()} function) rather than
re-estimating the model. For each facet-level x group-value cell,
it computes:
\itemize{
\item N: number of observations in the cell
\item ObsScore: sum of observed scores
\item ExpScore: sum of expected scores
\item ObsExpAvg: mean observed-minus-expected difference
\item Var_sum: sum of model variances
\item StdResidual: (ObsScore - ExpScore) / sqrt(Var_sum)
\item t: approximate t-statistic (equal to StdResidual)
\item df: N - 1
\item p_value: two-tailed p-value from the t-distribution
}
}
\section{Interpreting output}{

\itemize{
\item \verb{$table}: the full interaction table with one row per cell.
\item \verb{$summary}: overview counts of flagged and sparse cells.
\item \verb{$config}: analysis configuration parameters.
\item Cells with \verb{|t| > abs_t_warn} or \verb{|ObsExpAvg| > abs_bias_warn}
are flagged in the \code{flag_t} and \code{flag_bias} columns.
\item Sparse cells (N < min_obs) have \code{sparse = TRUE} and NA statistics.
}
}

\section{Typical workflow}{

\enumerate{
\item Fit a model with \code{\link[=fit_mfrm]{fit_mfrm()}}.
\item Run \code{dif_interaction_table(fit, diag, facet = "Rater", group = "Gender", data = df)}.
\item Inspect \verb{$table} for flagged cells.
\item Visualize with \code{\link[=plot_dif_heatmap]{plot_dif_heatmap()}}.
}
}

\examples{
set.seed(42)
toy <- expand.grid(
  Person = paste0("P", 1:8),
  Rater = paste0("R", 1:3),
  Criterion = c("Content", "Organization"),
  stringsAsFactors = FALSE
)
toy$Score <- sample(0:2, nrow(toy), replace = TRUE)
toy$Group <- ifelse(as.integer(factor(toy$Person)) <= 4, "A", "B")

fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                 method = "JML", model = "RSM", maxit = 25)
diag <- diagnose_mfrm(fit, residual_pca = "none")
int <- dif_interaction_table(fit, diag, facet = "Rater",
                             group = "Group", data = toy, min_obs = 2)
int$table
}
\seealso{
\code{\link[=analyze_dif]{analyze_dif()}}, \code{\link[=plot_dif_heatmap]{plot_dif_heatmap()}}, \code{\link[=dif_report]{dif_report()}},
\code{\link[=estimate_bias]{estimate_bias()}}
}
