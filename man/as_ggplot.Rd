% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-plotting.R
\name{as_ggplot}
\alias{as_ggplot}
\alias{as_ggplot.default}
\alias{as_ggplot.mfrm_design_evaluation}
\alias{as_ggplot.mfrm_design_evaluation_plot_data}
\alias{as_ggplot.mfrm_signal_detection}
\alias{as_ggplot.mfrm_signal_detection_plot_data}
\alias{as_ggplot.mfrm_plot_data}
\title{Convert mfrmr plot data to a ggplot object}
\usage{
as_ggplot(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{default}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_design_evaluation}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_design_evaluation_plot_data}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_signal_detection}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_signal_detection_plot_data}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_plot_data}(x, type = NULL, component = NULL, ...)
}
\arguments{
\item{x}{An \code{mfrm_plot_data} object, a supported draw-free simulation plot
list, or a fitted/report/review/simulation object with a
\code{plot(..., draw = FALSE)} method.}

\item{type}{Optional plot type passed to \code{plot()} when \code{x} is not already an
\code{mfrm_plot_data} object.}

\item{component}{Optional component inside the plot payload. When \code{NULL},
a suitable tabular component is selected automatically.}

\item{...}{Additional arguments passed to \code{plot(..., draw = FALSE)} when
\code{x} is not already an \code{mfrm_plot_data} object. For CCC payloads, the
converter also accepts \code{slope_aes} (\code{"none"}, \code{"linewidth"}, \code{"alpha"},
or \code{"colour"}/\code{"color"}), \code{facet_by} (\code{"auto"}, \code{"none"}, or
\code{"curve_group"}), \code{show_overlay}, and \code{show_dominance}.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{as_ggplot()} converts an existing \code{mfrm_plot_data} object, supported
draw-free simulation plot lists, or any \code{mfrmr} object whose \code{plot()} method
supports \code{draw = FALSE}, into a \code{ggplot2} object. It is an optional rendering
layer: base-R plotting remains the default package renderer, while
\code{as_ggplot()} gives users a modifiable object for themes, scales, facets,
Quarto reports, and downstream export.

The converter is intentionally conservative. It supports common
\code{mfrm_plot_data} shapes used by Wright maps, pathway maps, fit-pathway
displays, EAP power-sensitivity diagnostics, DIF/DFF summaries and heatmaps,
G-study/D-study planning plots, G-study design checks, simulation
design/recovery/signal handoff plots, category and information curves,
threshold ladders, network plot payloads, and
estimate/profile rows. When a payload cannot be inferred safely, use
\code{\link[=plot_data_components]{plot_data_components()}} and \code{\link[=plot_data]{plot_data()}} to build a custom figure directly.
}
\details{
Category characteristic curve payloads receive a dedicated converter.
Categories are colour-coded by default; for \code{GPCM}-style payloads with a
nonconstant \code{Slope} column, \code{slope_aes} can map the slope/discrimination
parameter to line width, alpha, or a continuous colour scale. For RSM and
PCM payloads the slope is constant, so the same arguments remain valid but
intentionally add little or no visual differentiation.
}
\examples{
\dontrun{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score", maxit = 30)

as_ggplot(fit, type = "wright")
as_ggplot(fit, type = "pathway")
as_ggplot(fit, type = "fit_pathway", fit_stat = "Outfit")
as_ggplot(plot(fit, type = "ccc", draw = FALSE))
as_ggplot(fit, type = "ccc", slope_aes = "linewidth")
as_ggplot(plot(fit, type = "ccc_overlay", draw = FALSE), show_overlay = TRUE)

dff <- analyze_dff(
  fit,
  facet = "Rater",
  group = "Group",
  data = toy,
  method = "residual"
)
as_ggplot(plot_dif_summary(dff, draw = FALSE))
as_ggplot(plot_dif_heatmap(dff, metric = "obs_exp", draw = FALSE))
if (requireNamespace("igraph", quietly = TRUE)) {
  review <- build_mfrm_network_review(fit, facets = "Rater")
  as_ggplot(plot(review, type = "network", draw = FALSE))
}

rec <- evaluate_mfrm_recovery(reps = 1, maxit = 30)
as_ggplot(plot(rec, type = "summary", metric = "rmse", draw = FALSE))

if (requireNamespace("lme4", quietly = TRUE)) {
  gt <- mfrm_generalizability(fit)
  as_ggplot(plot(gt, draw = FALSE))
  design_check <- check_mfrm_generalizability_design(
    fit,
    random_interactions = c("Person:Rater", "Rater:Criterion")
  )
  as_ggplot(plot(design_check, draw = FALSE))
  ds <- mfrm_d_study(gt, data.frame(Rater = c(2, 3, 4), Criterion = 4))
  as_ggplot(plot(ds, draw = FALSE))
}
}
}
