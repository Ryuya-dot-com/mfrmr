% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-as-ggplot.R, R/api-gpcm-reporting.R
\name{as_ggplot}
\alias{as_ggplot}
\alias{as_ggplot.default}
\alias{as_ggplot.mfrm_design_evaluation}
\alias{as_ggplot.mfrm_design_evaluation_plot_data}
\alias{as_ggplot.mfrm_signal_detection}
\alias{as_ggplot.mfrm_signal_detection_plot_data}
\alias{as_ggplot.mfrm_plot_data}
\alias{as_ggplot.mfrm_slope_intervals}
\alias{as_ggplot.mfrm_curve_intervals}
\alias{as_ggplot.mfrm_gpcm_bootstrap}
\alias{as_ggplot.mfrm_results}
\title{Convert draw-free mfrmr plot data to ggplot2}
\usage{
as_ggplot(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{default}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_design_evaluation}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_design_evaluation_plot_data}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_signal_detection}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_signal_detection_plot_data}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_plot_data}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_slope_intervals}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_curve_intervals}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_gpcm_bootstrap}(x, type = NULL, component = NULL, ...)

\method{as_ggplot}{mfrm_results}(x, type = NULL, component = NULL, ...)
}
\arguments{
\item{x}{An \code{mfrm_plot_data} object, or an mfrmr object with a draw-free
plot method.}

\item{type}{Optional plot type passed to \code{plot()} for a non-plot-data input.}

\item{component}{Optional tabular payload component to convert.}

\item{...}{Arguments passed to the draw-free plot method. CCC conversion
additionally accepts \code{slope_aes}, \code{facet_by}, and \code{show_overlay}.}
}
\value{
A \code{ggplot2} plot object, with a \code{mfrmr_notes} attribute when the
source plot payload contains a \code{notes} table.
Paired Wright/CCC payloads from \code{\link[=plot_compare_mfrm]{plot_compare_mfrm()}} retain their selected
comparison or difference view, group selection and monochrome panel policy.
}
\description{
\code{as_ggplot()} is an optional renderer for an \code{mfrm_plot_data} object or an
object whose \code{plot()} method supports \code{draw = FALSE}. Base graphics remain
the default; the returned \code{ggplot} can be restyled or composed downstream.
}
\details{
Dedicated conversions are provided for Wright maps, theta-to-expected-score
pathways, fit-statistic-to-measure pathways, category characteristic curves,
bubble charts, DIF/DFF summaries and heatmaps, portable-calibration
score review plots, testlet calibration, shared-rater severity and bootstrap
intervals, conditional Person scores, and
multivariate D-study comparisons. Matched ordinary/extended facet comparisons
preserve centered effects, paired/difference views, excluded rows, display
controls and alternative text; see \code{\link[=plot.mfrm_extended_comparison]{plot.mfrm_extended_comparison()}}.
Threshold-sensitivity tile and curve payloads also preserve their selected
view, display controls and text alternatives; see
\code{\link[=plot.mfrm_screening_sensitivity]{plot.mfrm_screening_sensitivity()}}.
Posterior predictive residual displays preserve their descriptive meaning
and lack of reference cutoffs; see \code{\link[=plot.mfrm_response_diagnostics]{plot.mfrm_response_diagnostics()}}.
Testlet conversions retain unavailable
rows, prior-only symbols and the conditional-interval note; they do not
estimate diagnostics or add calibration uncertainty. Extended-model interval,
precision and empirical-distribution views preserve display settings, data
exclusions and text alternatives; see \code{\link[=plot.mfrm_testlet_scores]{plot.mfrm_testlet_scores()}}. Bootstrap
conversion retains infinite endpoints as arrows and ordinary intervals as
a dashed comparison. D-study conversions
preserve G/Phi or SEM panels, fixed-count groups, score units, and unavailable
estimates. They do not refit the model or add confidence intervals.
For multivariate D-study plots, an explicit \code{component = "series"} keeps
this dedicated conversion; use \code{\link[=plot_data]{plot_data()}} for other tables.
Difference-interval plots from \code{\link[=mfrm_multivariate_d_compare]{mfrm_multivariate_d_compare()}} use their
base \code{plot()} method or \code{\link[=plot_data]{plot_data()}}; automatic conversion is not supported.
Automatic conversion of exploratory clustering plots is not supported.
Use their \code{plot()} methods for silhouettes, feature profiles, dendrograms
and imputation co-membership heatmaps, or \code{\link[=plot_data]{plot_data()}} for custom graphics.
For main-effects \code{mfrm_d_study} results, use the base \code{plot()} method or
\code{\link[=plot_data]{plot_data()}} for custom graphics; automatic conversion is refused because
generic column selection does not preserve those design comparisons.
Selecting \code{component} does not enable unsupported conversions: it cannot
preserve PCA axes, clustering membership, MI intervals or D-study differences
through generic column selection. Use \code{\link[=plot_data]{plot_data()}} to extract the table
and specify the axes, intervals and grouping explicitly in custom graphics.
Other draw-free payloads use a conservative tabular
fallback; inspect \code{\link[=plot_data_components]{plot_data_components()}} when automatic inference is not
appropriate.
Titles, subtitles, and captions are wrapped at 72 text columns for ordinary
figure widths; missing text is omitted. For narrower exports or custom line
breaks, override these labels with \code{ggplot2::labs()} on the returned plot.
Fit plots created with \code{show_title = FALSE} or \code{show_notes = FALSE} retain
those settings on conversion. To set them when converting a fit directly,
pass the flags through \code{...}. Interpretation and display notes remain
available in \code{attr(plot, "mfrmr_notes")}, when present in the source payload.
Wright, pathway, and CCC conversions share the fit-family series palette,
honour monochrome presets and supplied palette overrides, and preserve
line-type or point-shape distinctions. CCC categories retain their source
order. When \code{slope_aes = "colour"}, slopes instead use a continuous viridis
scale (a grey gradient in monochrome); categories still have line types.

Fixed-facet RSM/PCM intervals retain the selected covariance method, level,
status symbols and optional ordinary comparison. Titles, subtitles, captions,
reference lines and legends can be changed through the source plot arguments.
Methods use both line types and vertical offsets, including in monochrome.
}
\examples{
\donttest{
toy <- load_mfrmr_data("example_core")
# A balanced slice retains every Rater and Criterion while running quickly.
toy <- toy[toy$Person \%in\% unique(toy$Person)[1:12], , drop = FALSE]
fit <- fit_mfrm(toy, "Person",
                c("Rater", "Criterion"), "Score", maxit = 30)
as_ggplot(fit, type = "wright")
as_ggplot(fit, type = "fit_pathway", include_person = TRUE)
as_ggplot(plot(fit, type = "ccc", draw = FALSE))
}
}
