% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-gpcm-reporting.R
\name{plot.mfrm_slope_intervals}
\alias{plot.mfrm_slope_intervals}
\alias{plot.mfrm_gpcm_bootstrap}
\title{Display saved GPCM intervals and bootstrap results}
\usage{
\method{plot}{mfrm_slope_intervals}(
  x,
  title = "GPCM slope uncertainty",
  subtitle = paste(attr(x, "target"), attr(x, "method"), paste0(100 * attr(x, "level"),
    "\%"), if (attr(x, "simultaneous") == "none") "pointwise" else "Bonferroni", sep =
    " | "),
  reference = NULL,
  draw = TRUE,
  ...
)

\method{plot}{mfrm_gpcm_bootstrap}(
  x,
  title = "GPCM bootstrap inference",
  subtitle = "Fitted-model parametric bootstrap; unresolved refits retained",
  draw = TRUE,
  ...
)
}
\arguments{
\item{x}{Saved \code{\link[=confint.mfrm_fit]{confint.mfrm_fit()}} or \code{\link[=bootstrap_mfrm_gpcm]{bootstrap_mfrm_gpcm()}} output.}

\item{title, subtitle}{Optional plot text; NULL removes it. The interval
subtitle defaults to the saved target, method, confidence level and any
saved numerical or bootstrap cautions. Custom text overrides this default, while the
diagnostics remain in the plot data and interval tables.}

\item{reference}{Optional finite numeric vertical reference. Default NULL;
no quality threshold or scoring-policy cutoff is selected.}

\item{draw}{TRUE draws the ggplot; FALSE returns it only.}

\item{...}{For bootstrap slope results, passed to \code{confint()} to select its
target and level. Otherwise unused.}
}
\value{
Invisibly a ggplot, retaining an \code{mfrm_plot_data} attribute for
\code{\link[=plot_data]{plot_data()}}. No fitting or resampling is performed.
}
\description{
Plot the selected inference result without fitting or recomputing covariance.
These plots describe discrimination or specified contrasts, not rater severity,
agreement, quality or recommended scoring weights.
}
\details{
Crosses retain unavailable intervals. Open circles retain intervals
with infinite endpoints; the plot does not replace them with finite bounds.
The accompanying table contains all bounds and failure reasons.
For a bootstrap LRT, a histogram shows resolved null statistics and a line
the observed statistic. The annotation counts every unresolved replicate.

Use \code{\link[=apa_table]{apa_table()}} for target-aware tables, \code{\link[=plot_data]{plot_data()}} for plotted values,
and \code{\link[=as_ggplot]{as_ggplot()}} for customization. Attach one result or a named list with
\code{mfrm_results(fit, intervals = list(slopes = ci, curves = curves), compute = "never")}. Named plot routes become \code{gpcm_slopes}, \code{gpcm_curves},
etc. Exact source matching is required; default fit diagnostics are preserved.
\code{\link[=export_mfrm_results]{export_mfrm_results()}} saves these tables and plots; replay reloads the
saved RDS, preserving the selected methods without rerunning estimation.
Markdown report summaries include the finite, unbounded and unavailable
interval counts and the saved inference cautions/reasons.

For positive slope or ratio intervals spanning many orders of magnitude,
use \code{as_ggplot(ci) + ggplot2::scale_x_log10()}. Finite bounds and estimates
must be strictly positive. Keep a linear axis for signed differences.
Changing the axis does not narrow the interval or make it more reliable.
}
\examples{
# ci <- confint(fit, scale = "standardized", method = "sandwich")
# plot(ci, title = NULL)
# apa_table(ci)
# plot_data(ci, "table")
}
