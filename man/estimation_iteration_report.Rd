% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{estimation_iteration_report}
\alias{estimation_iteration_report}
\title{Build an estimation-iteration report (preferred alias)}
\usage{
estimation_iteration_report(
  fit,
  max_iter = 20,
  reltol = NULL,
  include_prox = TRUE,
  include_fixed = FALSE
)
}
\arguments{
\item{fit}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{max_iter}{Maximum replay iterations (excluding optional initial row).}

\item{reltol}{Stopping tolerance for replayed max-logit change.}

\item{include_prox}{If \code{TRUE}, include an initial pseudo-row labeled \code{PROX}.}

\item{include_fixed}{If \code{TRUE}, include a FACETS-style fixed-width text block.}
}
\value{
A named list with iteration-report components. Class:
\code{mfrm_iteration_report}.
}
\description{
Build an estimation-iteration report (preferred alias)
}
\details{
\code{summary(out)} is supported through \code{summary()}.
\code{plot(out)} is dispatched through \code{plot()} for class
\code{mfrm_iteration_report} (\code{type = "residual"}, \code{"logit_change"},
\code{"objective"}).
}
\section{Interpreting output}{

\itemize{
\item \code{iterations}: trajectory of convergence indicators by iteration.
\item \code{summary}: final status and stopping diagnostics.
\item optional \code{PROX} row: pseudo-initial reference point when enabled.
}
}

\section{Typical workflow}{

\enumerate{
\item Run \code{estimation_iteration_report(fit)}.
\item Inspect plateau/stability patterns in summary/plot.
\item Adjust optimization settings if convergence looks weak.
}
}

\examples{
toy <- expand.grid(
  Person = paste0("P", 1:4),
  Rater = paste0("R", 1:2),
  Criterion = c("Content", "Organization", "Language"),
  stringsAsFactors = FALSE
)
toy$Score <- (
  as.integer(factor(toy$Person)) +
  2 * as.integer(factor(toy$Rater)) +
  as.integer(factor(toy$Criterion))
) \%\% 3
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score", method = "JML", maxit = 25)
out <- estimation_iteration_report(fit, max_iter = 5)
summary(out)
p_iter <- plot(out, draw = FALSE)
class(p_iter)
}
\seealso{
\code{\link[=fit_mfrm]{fit_mfrm()}}, \code{\link[=specifications_report]{specifications_report()}}, \code{\link[=data_quality_report]{data_quality_report()}}
}
