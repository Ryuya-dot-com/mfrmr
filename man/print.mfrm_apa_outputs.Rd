% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-methods-print-0-1-6.R
\name{print.mfrm_apa_outputs}
\alias{print.mfrm_apa_outputs}
\title{Print an APA reporting bundle}
\usage{
\method{print}{mfrm_apa_outputs}(x, include_notes = FALSE, include_captions = FALSE, qa = FALSE, ...)
}
\arguments{
\item{x}{Output from \code{\link[=build_apa_outputs]{build_apa_outputs()}}.}

\item{include_notes}{Logical. If \code{TRUE}, append \code{table_figure_notes}
after the manuscript narrative.}

\item{include_captions}{Logical. If \code{TRUE}, append
\code{table_figure_captions} after the manuscript narrative.}

\item{qa}{Logical. If \code{TRUE}, print the compact QA summary returned by
\code{summary(x, ...)} instead of the manuscript narrative. This is the
compatibility route for workflows that previously used bare \code{apa} as a
completeness check.}

\item{...}{Optional arguments passed to \code{summary(x, ...)} when \code{qa = TRUE}.
Legacy \code{top_n} and \code{preview_chars} arguments also trigger \code{qa = TRUE}
with a warning, so old QA-preview calls do not silently print narrative
text.}
}
\description{
Print an APA reporting bundle
}
\details{
Typing an \code{mfrm_apa_outputs} object at the console prints the concise
Method / Results draft stored in \code{x$report_text}. Use \code{summary(x)} or
\code{print(x, qa = TRUE)} for the structured QA surface with content checks,
component counts, and section availability.
}
\examples{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 15)
diag <- diagnose_mfrm(fit, residual_pca = "none",
                      diagnostic_mode = "legacy")
apa <- build_apa_outputs(fit, diag)
apa
summary(apa)
print(apa, qa = TRUE, top_n = 2)
}
