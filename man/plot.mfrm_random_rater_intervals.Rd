% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-plotting-random-rater-intervals.R
\name{plot.mfrm_random_rater_intervals}
\alias{plot.mfrm_random_rater_intervals}
\title{Compare bootstrap and ordinary random-rater intervals}
\usage{
\method{plot}{mfrm_random_rater_intervals}(
  x,
  level = x$settings$level,
  method = c("studentized", "error"),
  comparison = TRUE,
  draw = TRUE,
  sort = c("input", "estimate", "uncertainty"),
  palette = c("accessible", "mono"),
  title = NULL,
  caption = NULL,
  show_title = TRUE,
  show_notes = TRUE,
  show_labels = TRUE,
  reference = 0,
  text_scale = 1,
  point_size = 2.5,
  ...
)
}
\arguments{
\item{x}{A result from \code{\link[=mfrm_random_rater_intervals]{mfrm_random_rater_intervals()}}.}

\item{level}{Nominal pointwise coverage; defaults to the saved level.}

\item{method}{\code{"studentized"} or \code{"error"}; see \code{\link[=mfrm_random_rater_intervals]{mfrm_random_rater_intervals()}}.}

\item{comparison}{Show ordinary normal intervals from the same source fit.}

\item{draw}{\code{FALSE} returns plotted data without opening a device.}

\item{sort}{Row order for interval plots: input order, increasing estimate,
or increasing interval width. Missing values come last; ties retain input
order. Sorting is descriptive, not a test of differences.}

\item{palette}{\code{"accessible"} uses blue and dark orange with filled/open
symbols; \code{"mono"} uses dark grey with the same symbol distinctions.}

\item{title, caption}{\code{NULL} uses the default text; a string replaces it;
\code{""} removes it. Interpretation notes remain in the saved plot data.}

\item{show_title, show_notes}{Show the title and caption, respectively.
\code{FALSE} hides text without deleting its metadata.}

\item{show_labels}{Show IDs on interval and precision plots. Distribution
plots summarize estimates and do not label individual IDs. For crowded
precision plots use \code{FALSE} and consult the retained table.}

\item{reference}{Vertical reference line in logits; \code{NULL} omits it.
This is an orientation aid, not a quality threshold.}

\item{text_scale}{Positive multiplier for text size.}

\item{point_size}{Positive point size in ggplot millimetres; base graphics
use the corresponding relative size (2.5 is the default).}

\item{...}{Unused.}
}
\value{
Invisibly, an \code{mfrm_plot_data} object with exact bounds, availability,
settings and the source estimates. Use \code{\link[=plot_data]{plot_data()}} for custom graphics.
}
\description{
Compare bootstrap and ordinary random-rater intervals
}
\details{
Arrows identify unbounded bootstrap endpoints. Their displayed ends
are plot limits, not finite interval limits. Ordinary intervals never
replace an unbounded bootstrap interval. These are pointwise prediction
intervals for realized rater effects, not a rater-quality classification.
}
\seealso{
\code{\link[=plot.mfrm_testlet_scores]{plot.mfrm_testlet_scores()}} for display controls and accessible
output. Bootstrap comparisons retain the interval view.
}
