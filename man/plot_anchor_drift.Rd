% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-plotting.R
\name{plot_anchor_drift}
\alias{plot_anchor_drift}
\title{Plot anchor drift or equating chain}
\usage{
plot_anchor_drift(
  x,
  type = c("drift", "chain", "heatmap"),
  facet = NULL,
  draw = TRUE,
  ...
)
}
\arguments{
\item{x}{An \code{mfrm_anchor_drift} or \code{mfrm_equating_chain} object.}

\item{type}{Plot type: \code{"drift"} (dot plot of element drift),
\code{"chain"} (cumulative offset line plot), or \code{"heatmap"}
(wave-by-element drift heatmap).}

\item{facet}{Optional character vector to filter drift plots to specific
facets.}

\item{draw}{If \code{FALSE}, return the plot data invisibly without drawing.}

\item{...}{Additional graphical parameters passed to base plotting
functions.}
}
\value{
Invisible \code{mfrm_plot_data} object containing the plot data.
}
\description{
Creates base-R plots for inspecting anchor drift across calibration waves
or visualising the cumulative offset in an equating chain.
}
\details{
Three plot types are supported:
\itemize{
\item \strong{\code{"drift"}} (for \code{mfrm_anchor_drift} objects): A dot plot of each
element's drift value, grouped by facet.  Horizontal reference lines
mark the drift threshold.  Red points indicate flagged elements.
\item \strong{\code{"heatmap"}} (for \code{mfrm_anchor_drift} objects): A wave-by-element
heat matrix showing drift magnitude.  Darker cells represent larger
absolute drift.  Useful for spotting systematic patterns (e.g., all
criteria shifting in the same direction).
\item \strong{\code{"chain"}} (for \code{mfrm_equating_chain} objects): A line plot of
cumulative offsets across the equating chain.  A flat line indicates
a stable scale; steep segments suggest large between-wave shifts.
}
}
\section{Interpreting plots}{

\itemize{
\item In drift and heatmap plots, red or dark-shaded elements are flagged
for exceeding the drift and/or SE ratio thresholds.  These elements
may warrant investigation (e.g., rater re-training, item revision).
\item In chain plots, uneven spacing between waves suggests differential
difficulty shifts.  The \eqn{y}-axis shows logit-scale offsets.
}
}

\examples{
d1 <- load_mfrmr_data("study1")
d2 <- load_mfrmr_data("study2")
fit1 <- fit_mfrm(d1, "Person", c("Rater", "Criterion"), "Score",
                 method = "JML", maxit = 25)
fit2 <- fit_mfrm(d2, "Person", c("Rater", "Criterion"), "Score",
                 method = "JML", maxit = 25)
drift <- detect_anchor_drift(list(W1 = fit1, W2 = fit2))
plot_anchor_drift(drift, type = "drift", draw = FALSE)
chain <- build_equating_chain(list(F1 = fit1, F2 = fit2))
plot_anchor_drift(chain, type = "chain", draw = FALSE)
}
\seealso{
\code{\link[=detect_anchor_drift]{detect_anchor_drift()}}, \code{\link[=build_equating_chain]{build_equating_chain()}},
\code{\link[=plot_dif_heatmap]{plot_dif_heatmap()}}, \code{\link[=plot_bubble]{plot_bubble()}}
}
