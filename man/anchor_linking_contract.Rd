% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-estimation.R
\name{anchor_linking_contract}
\alias{anchor_linking_contract}
\title{Build the anchor and linking contract for a fitted model}
\usage{
anchor_linking_contract(
  fit,
  diagnostics = NULL,
  include_person_anchors = FALSE,
  digits = 6
)
}
\arguments{
\item{fit}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{diagnostics}{Optional output from \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}. If supplied, the
returned bundle includes a displacement-review summary.}

\item{include_person_anchors}{If \code{TRUE}, candidate anchors include person
estimates. The default keeps person rows out of anchor handoff files.}

\item{digits}{Rounding digits used for candidate anchor values.}
}
\value{
A bundle of class \code{mfrm_anchor_contract} with:
\itemize{
\item \code{summary}: one-row overview of anchor rows and support status
\item \code{contract}: status table for implemented, partial, and unsupported
anchor/linking surfaces
\item \code{candidate_anchors}: output from \code{\link[=make_anchor_table]{make_anchor_table()}}
\item \code{input_anchors}: direct anchors used by the current fit, when any
\item \code{input_group_anchors}: group anchors used by the current fit, when any
\item \code{constraint_summary}: facet-level anchor/group/dummy summary
\item \code{displacement_summary}: optional \code{\link[=displacement_table]{displacement_table()}} summary
\item \code{settings}: build settings
}
}
\description{
Build the anchor and linking contract for a fitted model
}
\details{
\code{anchor_linking_contract()} is a boundary table for FACETS-facing anchor
work. It makes four surfaces explicit:
\itemize{
\item candidate anchor rows exported by \code{\link[=make_anchor_table]{make_anchor_table()}};
\item direct anchors and group anchors actually used by the current fit;
\item package-native review surfaces such as \code{\link[=review_mfrm_anchors]{review_mfrm_anchors()}} and
\code{\link[=displacement_table]{displacement_table()}};
\item FACETS command-file surfaces that are not implemented, especially full
\verb{Anchorfile=} specification rewrites with final measures, scale blocks,
current settings, data, and \verb{,A} flags.
}

The returned \code{candidate_anchors} table is usable by \code{fit_mfrm(anchors = ...)}
in later package-native fits. It is not a FACETS specification file and is
not a proof that the chosen anchors are appropriate for a receiving dataset.
Always review candidate anchors against the receiving data with
\code{\link[=review_mfrm_anchors]{review_mfrm_anchors()}} before final estimation.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 30)
contract <- anchor_linking_contract(fit)
contract$contract[, c("Surface", "Status", "Boundary")]
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=make_anchor_table]{make_anchor_table()}}, \code{\link[=review_mfrm_anchors]{review_mfrm_anchors()}},
\code{\link[=displacement_table]{displacement_table()}}, \code{\link[=export_mfrm_bundle]{export_mfrm_bundle()}}, \code{\link[=build_mfrm_manifest]{build_mfrm_manifest()}}
}
