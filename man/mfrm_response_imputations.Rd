% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-response-imputation.R
\name{mfrm_response_imputations}
\alias{mfrm_response_imputations}
\alias{print.mfrm_response_imputations}
\alias{summary.mfrm_response_imputations}
\title{Review multiple imputations of scores on assigned ratings}
\usage{
mfrm_response_imputations(
  data,
  completed,
  person,
  facets,
  score,
  event_id,
  impute,
  categories,
  assigned = NULL,
  imputation_model = NULL,
  missing = c("error", "omit")
)

\method{print}{mfrm_response_imputations}(x, ...)

\method{summary}{mfrm_response_imputations}(object, ...)
}
\arguments{
\item{data}{Original long-format rating roster, including missing scores.}

\item{completed}{A list of at least two completed data frames, or a \code{mids}
object from \code{\link[mice:mice]{mice::mice()}} fitted to the long-format roster. Each completion
must contain all original columns and rows. Row order may differ; rows
are matched by \code{event_id}.}

\item{person, score, event_id}{Column names. \code{event_id} uniquely identifies a
rating event, including repeated ratings of the same person and facets.}

\item{facets}{Nonempty character vector of facet column names.}

\item{impute}{Character vector of event IDs explicitly selecting missing
scores on assigned ratings. Observed scores cannot be selected.}

\item{categories}{The full intended contiguous integer category vector,
for example \code{0:4}. It is preserved across all completed analyses.}

\item{assigned}{Optional name of a complete logical column: \code{TRUE} denotes
an assigned rating and \code{FALSE} an unassigned combination. Without this
column every supplied row is declared assigned. Unassigned rows must have
missing scores in the original and every completion.}

\item{imputation_model}{For a list of completions, the saved model or a
nonempty list containing its specification, settings and diagnostics.
Required so that the provenance is retained. For a \code{mids} input, that
object is retained automatically; omit this argument.}

\item{missing}{How to handle assigned missing scores not selected by
\code{impute}: \code{"error"} (default) or explicit \code{"omit"}. Omitted events remain
in the roster and every completion, with missing scores, and are excluded
from each analysis. This choice is not a correction for nonresponse.}

\item{x, object}{An object returned by \code{mfrm_response_imputations()}.}

\item{...}{Unused for print and summary methods.}
}
\value{
An \code{mfrm_response_imputations} object containing the original
\code{data}, aligned \code{completed} data sets, \code{imputation_model}, an \code{events}
table with assignment/observation/imputation/omission status, a \code{support}
table counting original observed and imputed scores by person/facet level,
and \code{settings}. No imputations or failed analyses are silently discarded.
}
\description{
Preserve a rating roster, its observed scores and its explicitly selected
missing scores while importing completed data from an imputation model.
No person-by-facet grid is constructed. This function validates supplied
imputations; it does not choose or fit an imputation model.
}
\details{
The score must contain numeric integer category labels (numeric
vectors, or character/factor labels such as \code{"0"}, \code{"1"}). Recoded sentinel
missing values must already be \code{NA}. Identifiers, assignment indicators
and observed values must not change. Only selected scores may be filled;
missing auxiliary predictors may also be completed. For a \code{mids} input,
its original data and score \code{where} selection must agree with this review.

A sparse assignment and a missing assigned score are different events.
Neither absent roster rows nor explicit unassigned rows are imputed.
An entirely imputed person or facet level is visible in \code{support}; its
analysis depends on the imputation model, not on observed ratings for that
level. Imputed links do not establish empirical connectedness.

Proper multiple imputation must include uncertainty about missing values
and imputation parameters, reflect the ordinal score support and the
person/facet dependence, and be compatible with the intended analysis.
Include relevant observed predictors of nonresponse. An MAR analysis
requires an adequate conditional model; informative missingness beyond
observed predictors requires sensitivity analysis. Passing these software
checks does not establish MAR, model adequacy or interval coverage.

For a wide-format or multilevel imputation model, reshape each completion
back to the original long roster using event identities and supply the
resulting list with the saved model. Do not average completed scores.
See \code{vignette("mfrmr-response-imputation", package = "mfrmr")} for a
joint RSM example with supplied posterior predictive completions, shared
Person draws, sampling diagnostics, direct observed-score inference and a
separate lower-score sensitivity analysis. The accompanying R/Stan script
regenerates that example; it is not a general imputation engine.
Under the same score model and ignorable missingness, observed-score MML
can directly estimate the fixed-facet target without completing scores.
MI does not create additional observed information.
}
\seealso{
\code{\link[=fit_mfrm_imputed]{fit_mfrm_imputed()}}, \code{\link[=pool_mfrm_imputed]{pool_mfrm_imputed()}}, \code{\link[=mfrm_cluster_imputed]{mfrm_cluster_imputed()}}
}
