% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-import.R
\name{import_mirt_fit}
\alias{import_mirt_fit}
\alias{summary.mfrm_imported_fit}
\alias{print.summary.mfrm_imported_fit}
\title{Import an \code{mirt} fit to an mfrmr-compatible bundle}
\usage{
import_mirt_fit(
  fit,
  model = c("RSM", "PCM", "GPCM"),
  item_facet = "Item",
  compute_fit = FALSE
)

\method{summary}{mfrm_imported_fit}(object, digits = 3L, ...)

\method{print}{summary.mfrm_imported_fit}(x, ...)
}
\arguments{
\item{fit}{An object returned by \code{\link[mirt:mirt]{mirt::mirt()}} (a
\code{SingleGroupClass}).}

\item{model}{One of \code{"RSM"}, \code{"PCM"}, \code{"GPCM"}. The importer
does not reconstruct all source constraints; pass the model that was
estimated. Non-unit slopes require \code{"GPCM"}. A polytomous \code{"RSM"}
import requires source item type \code{"rsm"}.}

\item{item_facet}{Name to assign to the item facet in the
imported bundle (default \code{"Item"}).}

\item{compute_fit}{Logical. When \code{TRUE}, run \code{\link[mirt:itemfit]{mirt::itemfit()}}
and \code{\link[mirt:personfit]{mirt::personfit()}} to populate Infit / Outfit / OutfitZSTD
columns on the returned facet tables, plus build a
measurement-side diagnostics bundle. Person fit uses source EAP scores.
Default \code{FALSE} extracts parameters without calculating fit statistics.}

\item{object, x}{An imported measurement bundle.}

\item{digits}{Number of digits for displayed estimates.}

\item{...}{Additional arguments (unused by imported summaries).}
}
\value{
An \code{mfrm_imported_fit} object. Slots:
\describe{
\item{\code{summary}}{Model / method / N / LogLik / AIC / BIC.}
\item{\code{facets$person}}{Person ID, Estimate, SE, Extreme, plus
Infit / Outfit / OutfitZSTD / Zh when \code{compute_fit = TRUE}.}
\item{\code{facets$others}}{Item-level estimates and slopes; with
\code{compute_fit = TRUE}, also available Infit / Outfit statistics.}
\item{\code{steps}}{Absolute adjacent-category thresholds on the source ability
scale, labelled in \code{Parameterization}; these are not centered step
deviations. Rating-scale offsets are included.}
\item{\code{config}}{List with the declared \code{model} and facet names
used for the import; downstream plot and table helpers consult
this to dispatch correctly on the imported bundle.}
\item{\code{diagnostics}}{\code{mfrm_diagnostics}-shape bundle when
\code{compute_fit = TRUE}; \code{NULL} otherwise.}
\item{\code{source}}{Imported-from metadata.}
}
}
\description{
Extracts item, step, and person parameters from a \code{\link[mirt:mirt]{mirt::mirt()}}
fit and returns an \code{mfrm_imported_fit} object. The returned
object has the public slots \code{summary}, \code{facets$person},
\code{facets$others}, \code{steps}, \code{config}, and \code{source} that the mfrmr
plot and table helpers expect. Only unidimensional Rasch and partial-credit
response models with positive slopes and ordinary category scores are
supported. Graded-response, guessing and multidimensional models are refused.
With \code{compute_fit = TRUE}, source Infit / Outfit statistics are attached.
}
\section{Source scale}{

Item difficulty is the mean of its absolute adjacent-category thresholds.
Source identification and slopes are retained without rescaling. For mirt
\code{gpcmIRT} and \code{rsm}, the category offset is included as \code{b - c / a}.
Person estimates are EAP; the \code{SE} column contains conditional posterior
SDs, not sampling SEs. Person labels use retained source row names or
P-prefixed row positions. Original identifiers discarded by mirt cannot be
recovered. Imported summaries describe these conventions without assuming
a native mfrmr population distribution or slope normalization.
}

\section{Imported uncertainty}{

Imported SEs retain the source package's interpretation. The measurement-side
diagnostics do not reconstruct the joint parameter covariance, so joint facet
chi-square statistics, degrees of freedom and p-values are unavailable.
Posterior SDs do not supply sampling SEs for separation reliability.
Other separation summaries require valid SEs for every finite estimate and
remain descriptive. Imported Wright maps show points only: source uncertainty
conventions do not establish one common confidence-interval calculation.
Re-import older saved bundles from the existing source-package fit to update
difficulties, thresholds and uncertainty labels. The mirt and TAM importers
accept \code{compute_fit = TRUE} when source fit statistics are needed; no model
re-estimation is required.
}

\section{Scope}{

Use \code{summary()} for source-scale tables and \code{plot()} for a point-only Wright
map. Available source fit statistics remain in the facet and diagnostic
tables. Native model curves, comprehensive \code{\link[=mfrm_results]{mfrm_results()}} reports,
response-level diagnostics, \code{\link[=run_qc_pipeline]{run_qc_pipeline()}}, bias/DIF analysis, anchoring
and portable calibration are unavailable for imported bundles. This is a
one-way fitted-object import of the documented fields.
}

\examples{
\donttest{
if (requireNamespace("mirt", quietly = TRUE)) {
  response_matrix <- matrix(sample(0:1, 120, replace = TRUE), nrow = 40)
  colnames(response_matrix) <- paste0("Item", seq_len(ncol(response_matrix)))
  fit <- mirt::mirt(response_matrix, 1, itemtype = "Rasch", verbose = FALSE)
  imported <- import_mirt_fit(fit, model = "RSM")
  imported$summary
}
}
}
\seealso{
\code{\link[=import_tam_fit]{import_tam_fit()}}, \code{\link[=import_erm_fit]{import_erm_fit()}}
}
