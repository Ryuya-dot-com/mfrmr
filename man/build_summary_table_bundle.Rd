% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-reports.R
\name{build_summary_table_bundle}
\alias{build_summary_table_bundle}
\title{Build a manuscript-oriented table bundle from \code{summary()} outputs}
\usage{
build_summary_table_bundle(
  x,
  which = NULL,
  appendix_preset = NULL,
  include_empty = FALSE,
  digits = 3,
  top_n = 10,
  preview_chars = 160
)
}
\arguments{
\item{x}{An \code{mfrm_fit}, \code{mfrm_diagnostics}, \code{mfrm_precision_review},
\code{mfrm_fit_measures}, \code{mfrm_facets_fit_review},
\code{mfrm_person_fit_indices}, \code{mfrm_data_description},
\code{mfrm_reporting_checklist}, \code{mfrm_apa_outputs},
\code{mfrm_design_evaluation}, \code{mfrm_signal_detection},
\code{mfrm_recovery_simulation}, \code{mfrm_recovery_assessment},
\code{mfrm_population_prediction}, \code{mfrm_future_branch_active_branch},
\code{mfrm_facets_run}, \code{mfrm_results}, \code{mfrm_report}, \code{mfrm_bias},
\code{mfrm_anchor_review},
\code{mfrm_linking_review}, \code{mfrm_misfit_casebook}, \code{mfrm_weighting_review},
\code{mfrm_model_choice_review}, \code{mfrm_unit_prediction},
\code{mfrm_plausible_values}, \code{mfrm_generalizability},
\code{mfrm_generalizability_design_check},
\code{mfrm_generalizability_bootstrap},
\code{mfrm_generalizability_comparison}, or \code{mfrm_analysis_audit} object, one
of their \code{summary()} outputs, or a
\code{summary.mfrmr_recovery_validation} object from the packaged validation
protocol.}

\item{which}{Optional character vector selecting a subset of named tables.}

\item{appendix_preset}{Optional appendix-oriented table preset:
\code{"all"}, \code{"recommended"}, \code{"compact"}, \code{"methods"}, \code{"results"},
\code{"diagnostics"}, or \code{"reporting"}. Cannot be combined with \code{which}.
Section-aware presets keep returned tables whose bundle catalog maps to
the requested appendix section.}

\item{include_empty}{If \code{TRUE}, retain empty tables in the returned bundle.}

\item{digits}{Digits forwarded when \code{summary()} must be computed from a raw
object.}

\item{top_n}{Row cap forwarded to compact \code{summary()} methods when \code{x} is a
raw object.}

\item{preview_chars}{Character cap forwarded to
\code{summary.mfrm_apa_outputs()} when \code{x} is a raw APA-output object.}
}
\value{
An object of class \code{mfrm_summary_table_bundle} with:
\itemize{
\item \code{overview}
\item \code{table_index}
\item \code{plot_index}
\item \code{tables}
\item \code{appendix_preset}
\item \code{notes}
\item \code{source_class}
\item \code{summary_class}
}
}
\description{
Build a manuscript-oriented table bundle from \code{summary()} outputs
}
\details{
This helper turns the package's compact summary objects into a reproducible
table bundle for manuscript drafting, appendix handoff, or downstream
formatting. It does not replace \code{\link[=apa_table]{apa_table()}}; instead, it provides a
consistent bridge from \code{summary()} to named \code{data.frame} components that can
later be rendered with \code{\link[=apa_table]{apa_table()}} or exported directly.

The public entry point validates \code{x} and the summary-object contract up
front, so malformed summaries fail with a package-level message instead of
falling through to opaque downstream errors.

The function first normalizes \code{x} through the corresponding \code{summary()}
method when needed, then records a \code{table_index} describing every available
table and returns the selected tables in \code{tables}. Optional appendix presets
can be applied at bundle-construction time when you want a conservative
manuscript-facing subset before plotting or export.
}
\section{Supported inputs}{

\itemize{
\item \code{\link[=fit_mfrm]{fit_mfrm()}} or \code{summary(fit)}
\item \code{\link[=diagnose_mfrm]{diagnose_mfrm()}} or \code{summary(diag)}
\item \code{\link[=precision_review_report]{precision_review_report()}} or \code{summary(precision_review)}
\item \code{\link[=fit_measures_table]{fit_measures_table()}} or \code{summary(fit_measures)}
\item \code{\link[=facets_fit_review]{facets_fit_review()}} or \code{summary(facets_fit_review)}
\item \code{\link[=compute_person_fit_indices]{compute_person_fit_indices()}} or \code{summary(person_fit)}
\item \code{\link[=describe_mfrm_data]{describe_mfrm_data()}} or \code{summary(ds)}
\item \code{\link[=reporting_checklist]{reporting_checklist()}} or \code{summary(chk)}
\item \code{\link[=build_apa_outputs]{build_apa_outputs()}} or \code{summary(apa)}
\item \code{\link[=evaluate_mfrm_design]{evaluate_mfrm_design()}} or \code{summary(sim_eval)}
\item \code{\link[=evaluate_mfrm_signal_detection]{evaluate_mfrm_signal_detection()}} or \code{summary(sig_eval)}
\item \code{\link[=evaluate_mfrm_recovery]{evaluate_mfrm_recovery()}} or \code{summary(rec)}
\item \code{\link[=assess_mfrm_recovery]{assess_mfrm_recovery()}} or \code{summary(rec_assessment)}
\item \code{summary(validation)} from \code{recovery-validation.R}
\item \code{\link[=predict_mfrm_population]{predict_mfrm_population()}} or \code{summary(pred)}
\item \code{planning_schema$future_branch_active_branch} or \code{summary(...)}
\item \code{\link[=run_mfrm_facets]{run_mfrm_facets()}} or \code{summary(out)}
\item \code{\link[=mfrm_results]{mfrm_results()}} or \code{summary(res)}
\item \code{\link[=mfrm_report]{mfrm_report()}} or \code{summary(report)}
\item \code{\link[=estimate_bias]{estimate_bias()}} or \code{summary(bias)}
\item \code{\link[=review_mfrm_anchors]{review_mfrm_anchors()}} or \code{summary(review)}
\item \code{\link[=build_linking_review]{build_linking_review()}} or \code{summary(review)}
\item \code{\link[=build_misfit_casebook]{build_misfit_casebook()}} or \code{summary(casebook)}
\item \code{\link[=build_weighting_review]{build_weighting_review()}} or \code{summary(review)}
\item \code{\link[=build_model_choice_review]{build_model_choice_review()}} or \code{summary(review)}
\item \code{\link[=predict_mfrm_units]{predict_mfrm_units()}} or \code{summary(pred_units)}
\item \code{\link[=sample_mfrm_plausible_values]{sample_mfrm_plausible_values()}} or \code{summary(pv)}
\item \code{\link[=mfrm_generalizability]{mfrm_generalizability()}}, \code{\link[=check_mfrm_generalizability_design]{check_mfrm_generalizability_design()}},
\code{\link[=bootstrap_mfrm_generalizability]{bootstrap_mfrm_generalizability()}},
\code{\link[=compare_mfrm_generalizability]{compare_mfrm_generalizability()}}, \code{\link[=mfrm_analysis_audit]{mfrm_analysis_audit()}}, or their
\code{summary()} outputs
}
}

\section{Interpreting output}{

\itemize{
\item \code{overview}: one-row metadata about the source summary and table counts.
\item \code{table_index}: table names, dimensions, roles, and manuscript-oriented
descriptions.
\item \code{plot_index}: which returned tables contain numeric content and which
bundle-level plot types can use them directly.
\item \code{tables}: named \code{data.frame} objects ready for formatting or export.
\item \code{appendix_preset}: active appendix subset mode (\code{"none"} when not used).
\item \code{notes}: short guidance about omitted empty tables or source-level caveats.
\item fit-level caveats use the \code{analysis_caveats} role; pre-fit data
score-support caveats use the \code{score_category_caveats} role. Both roles are
classified as diagnostics and stay in \code{recommended} appendix subsets.
\item simulation summaries from \code{\link[=evaluate_mfrm_design]{evaluate_mfrm_design()}},
\code{\link[=evaluate_mfrm_recovery]{evaluate_mfrm_recovery()}}, \code{\link[=evaluate_mfrm_signal_detection]{evaluate_mfrm_signal_detection()}}, and
\code{\link[=predict_mfrm_population]{predict_mfrm_population()}} retain their \code{runtime}, \code{reading_order},
\code{next_actions}, and \code{reporting_notes} tables so the appendix handoff
preserves the same reader-first route shown by \code{summary()}.
\item recovery-assessment and recovery-validation summaries expose
\code{diagnostic_reporting_notes} before \code{diagnostic_review} or
\code{diagnostic_oc_summary} so fit/separation caveats can be reported without
treating them as recovery or release gates.
\item recovery-validation summaries expose \code{condition_reporting_notes} before
\code{condition_summary} so GPCM generator stress and sparse score support are
not mistaken for recovery-metric failures.
\item precision-review summaries expose \code{fit_separation_basis} so fit,
ZSTD, separation/reliability/strata, and QC thresholds remain separate
reporting surfaces rather than implicit validation gates.
\item fit-measure and FACETS fit-review summaries expose df/ZSTD sensitivity
tables under precision-review roles, keeping MnSq status, ZSTD
standardization, and external FACETS matching distinct in appendix
handoffs.
\item latent-regression fit summaries expose \code{population_coding} in the methods
appendix role so categorical levels, contrasts, and encoded columns can be
documented with the coefficient table.
\item fit summaries expose \code{design_spec} in a methods appendix role so the
RSM/PCM/GPCM threshold structure, FACETS analogue, and external
design-matrix vocabulary are carried into table bundles.
\item model-choice review summaries expose \code{comparison_guidance} and
\code{comparison_reporting_templates} so model-comparison claims can be checked
before text is copied into a manuscript or reviewer response.
}
}

\section{Typical workflow}{

\enumerate{
\item Build a compact object with \code{summary(...)}.
\item Convert it with \code{build_summary_table_bundle(...)}.
\item For model comparisons, inspect \code{comparison_guidance} before
\code{comparison_table}; the former states the claim boundary for the latter.
\item Use \code{bundle$tables[[...]]} directly, or hand a selected table to
\code{\link[=apa_table]{apa_table()}} for formatted manuscript output.
\item If you want a manuscript appendix subset up front, use a preset such as
\code{appendix_preset = "recommended"}, \code{"compact"}, or \code{"diagnostics"}.
\item For recovery-assessment or recovery-validation summaries, inspect
\code{bundle$tables$reading_order} first when it is available.
\item For design-evaluation, recovery-simulation, signal-detection, population-
forecast, and diagnostic-screening summaries, inspect the exported
\code{reading_order}, \code{next_actions}, and \code{reporting_notes} tables before using
dense metric tables in an appendix.
\item For recovery-assessment or recovery-validation summaries with retained
diagnostics, read \code{diagnostic_reporting_notes} before the raw
\code{diagnostic_review} or \code{diagnostic_oc_summary}. Read
\code{condition_reporting_notes} before \code{condition_review} or
\code{condition_summary} when bounded \code{GPCM} generator stress is part of the
plan.
}
}

\examples{
\dontrun{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 30)
bundle <- build_summary_table_bundle(fit)
bundle$table_index
summary(bundle)$role_summary
}

# Recovery-validation output can be converted to appendix-ready tables.
\dontrun{
source(system.file("validation", "recovery-validation.R", package = "mfrmr"))
validation <- mfrmr_run_recovery_validation(
  case_ids = c("gpcm_slope_profile", "gpcm_high_dispersion_sparse"),
  quick = TRUE,
  seed = 20260525
)
validation_bundle <- build_summary_table_bundle(summary(validation))
validation_bundle$tables$reading_order
validation_bundle$tables$topline_release_decision
validation_bundle$tables$condition_reporting_notes
validation_bundle$tables$condition_summary
validation_bundle$tables$diagnostic_reporting_notes
}
}
\seealso{
\code{\link[=summary]{summary()}}, \code{\link[=apa_table]{apa_table()}}, \code{\link[=reporting_checklist]{reporting_checklist()}},
\code{\link[=build_apa_outputs]{build_apa_outputs()}}
}
