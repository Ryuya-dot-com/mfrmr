% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-classic-plots.R
\name{plot_expected_score_curve}
\alias{plot_expected_score_curve}
\title{Plot expected score curves}
\usage{
plot_expected_score_curve(
  fit,
  curve_group = NULL,
  theta_range = c(-6, 6),
  theta_points = 241L,
  draw = TRUE
)
}
\arguments{
\item{fit}{An \code{mfrm_fit} object.}

\item{curve_group}{Optional curve group label to retain.}

\item{theta_range}{Numeric length-2 theta range.}

\item{theta_points}{Number of theta grid points.}

\item{draw}{If \code{TRUE}, draw with base graphics.}
}
\value{
An \code{mfrm_plot_data} object with an \code{expected} data frame.
}
\description{
Draws model-implied expected score curves across theta for the active
rating-scale / partial-credit curve groups. This is the user-facing classic
expected-score front door; the underlying coordinates are the same scale
curves returned by \code{\link[=category_curves_report]{category_curves_report()}}.
}
\details{
For each curve group and theta grid point, the expected score is
\deqn{E[X \mid \theta] = \sum_k x_k P(X = x_k \mid \theta).}
This is the model-implied expected category score, not an empirical fit
smoother.
}
\examples{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 25)
p <- plot_expected_score_curve(fit, draw = FALSE)
head(p$data$expected)
}
\seealso{
\code{\link[=category_curves_report]{category_curves_report()}}, \code{\link[=plot_test_characteristic_curve]{plot_test_characteristic_curve()}},
\code{\link[=plot_cumulative_category_curve]{plot_cumulative_category_curve()}}
}
