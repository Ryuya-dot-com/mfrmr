% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-classic-plots.R
\name{plot_test_characteristic_curve}
\alias{plot_test_characteristic_curve}
\title{Plot a design-weighted test characteristic curve}
\usage{
plot_test_characteristic_curve(
  fit,
  theta_range = c(-6, 6),
  theta_points = 201L,
  draw = TRUE
)
}
\arguments{
\item{fit}{An \code{mfrm_fit} object.}

\item{theta_range}{Numeric length-2 theta range.}

\item{theta_points}{Number of theta grid points.}

\item{draw}{If \code{TRUE}, draw with base graphics.}
}
\value{
An \code{mfrm_plot_data} object with a \code{tcc} data frame.
}
\description{
Computes the expected total and mean score over the observed many-facet
design while varying the person measure theta. Unlike
\code{\link[=plot_expected_score_curve]{plot_expected_score_curve()}}, this summarizes the realized design as a
whole rather than one threshold/criterion curve group.
}
\details{
For each observed design cell \code{d}, the helper computes
\eqn{E_d[X \mid \theta]} from the fitted rating-scale, partial-credit, or
bounded GPCM structure, then aggregates with the observed exposure weight
\eqn{w_d}:
\deqn{TCC(\theta) = \sum_d w_d E_d[X \mid \theta].}
\code{ExpectedMeanScore} divides this total by \eqn{\sum_d w_d}.
}
\examples{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 25)
tcc <- plot_test_characteristic_curve(fit, draw = FALSE)
head(tcc$data$tcc)
}
