% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-classic-plots.R
\name{plot_cumulative_category_curve}
\alias{plot_cumulative_category_curve}
\title{Plot cumulative category curves}
\usage{
plot_cumulative_category_curve(
  fit,
  curve_group = NULL,
  theta_range = c(-6, 6),
  theta_points = 241L,
  draw = TRUE
)
}
\arguments{
\item{fit}{An \code{mfrm_fit} object.}

\item{curve_group}{Optional curve group label to retain.}

\item{theta_range}{Numeric length-2 theta range.}

\item{theta_points}{Number of theta grid points.}

\item{draw}{If \code{TRUE}, draw with base graphics.}
}
\value{
An \code{mfrm_plot_data} object with a \code{cumulative} data frame.
}
\description{
Draws \eqn{P(X \ge k)} curves across theta for ordered response categories.
These are cumulative ordered-category curves, not mirt's empirical plot and
not an S-X2 item-fit test.
}
\details{
For each non-minimum category threshold \eqn{k}, the displayed curve is
\deqn{P(X \ge k \mid \theta) = \sum_{x_j \ge k} P(X = x_j \mid \theta).}
}
\examples{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 25)
cc <- plot_cumulative_category_curve(fit, draw = FALSE)
head(cc$data$cumulative)
}
