% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-calibration-methods.R
\name{mfrm_calibration_score_methods}
\alias{mfrm_calibration_score_methods}
\alias{summary.mfrm_calibration_score}
\alias{print.mfrm_calibration_score}
\alias{print.summary.mfrm_calibration_score}
\alias{plot.mfrm_calibration_score}
\title{Review and plot portable fixed-calibration scores}
\usage{
\method{summary}{mfrm_calibration_score}(object, digits = 3L, ...)

\method{print}{mfrm_calibration_score}(x, ...)

\method{print}{summary.mfrm_calibration_score}(x, ...)

\method{plot}{mfrm_calibration_score}(
  x,
  type = c("interval", "precision", "edge_mass"),
  top_n = 40L,
  sort_by = c("estimate", "sd", "person"),
  label_review = TRUE,
  main = NULL,
  draw = TRUE,
  preset = c("standard", "publication", "compact", "monochrome"),
  ...
)
}
\arguments{
\item{object, x}{An \code{mfrm_calibration_score} returned by
\code{\link[=score_mfrm_calibration]{score_mfrm_calibration()}}.}

\item{digits}{Number of decimal places in the summary.}

\item{...}{Reserved for generic compatibility.}

\item{type}{One of \code{"interval"}, \code{"precision"}, or \code{"edge_mass"}.}

\item{top_n}{Maximum number of scored Persons shown. Review rows are
selected first when truncation is necessary. Use \code{Inf} to show all.}

\item{sort_by}{Selection priority after review rows: absolute estimate,
posterior SD, or Person identifier.}

\item{label_review}{Whether review points are labelled in the precision and
edge-mass views. Interval plots always label the displayed Persons.}

\item{main}{Optional plot title.}

\item{draw}{If \code{TRUE}, draw with base R graphics. The returned
\code{mfrm_plot_data} is available invisibly in either case.}

\item{preset}{Visual preset: \code{"standard"}, \code{"publication"}, \code{"compact"},
or \code{"monochrome"}.}
}
\value{
\code{print()} returns its input invisibly. \code{summary()} returns a
\code{summary.mfrm_calibration_score} containing
\code{overview}, \code{estimates}, \code{review}, \code{row_review}, \code{settings}, and \code{notes}.
\code{plot()} returns an \code{mfrm_plot_data} with the selected plotting table,
selection accounting, unplotted Person dispositions, interpretation
guidance, and plotting settings.
}
\description{
These methods provide the first review surface for the result returned by
\code{\link[=score_mfrm_calibration]{score_mfrm_calibration()}}. \code{print()} gives a compact batch disposition,
\code{summary()} exposes readable score and review tables, and \code{plot()} shows
conditional score uncertainty or numerical review quantities without
refitting a model.
}
\details{
The default interval plot shows posterior EAP estimates and central
intervals. \code{type = "precision"} plots the number of valid response rows
against posterior SD. \code{type = "edge_mass"} compares posterior mass on the
two outer quadrature nodes with the recorded review threshold. Review
dispositions are highlighted in every view.

These are score-batch review displays, not calibration-fit diagnostics.
Posterior SDs and intervals are conditional on the frozen point calibration
and its recorded prior. They exclude calibration-parameter uncertainty.
Persons with no valid responses have no score coordinate and are retained
in \code{summary(x)$review} and in the plot payload's
\code{unplotted_dispositions} component.
}
\examples{
\donttest{
dat <- load_mfrmr_data("example_core")
ids <- unique(dat$Person)
training <- dat[dat$Person \%in\% ids[1:18], , drop = FALSE]
fit <- fit_mfrm(
  training, "Person", c("Rater", "Criterion"), "Score",
  model = "RSM", method = "MML", quad_points = 5, maxit = 20
)
calibration <- freeze_mfrm_calibration(
  validate_mfrm_calibration(extract_mfrm_calibration(fit))
)
new_rows <- dat[dat$Person \%in\% ids[19:20], , drop = FALSE]
scores <- score_mfrm_calibration(calibration, new_rows)
summary(scores)
plot(scores, type = "interval")
plot(scores, type = "edge_mass", draw = FALSE)
}
}
\seealso{
\link{mfrm_calibration_workflow}, \link{mfrm_calibration_capabilities},
\code{\link[=plot_data]{plot_data()}}, \code{\link[=plot_data_components]{plot_data_components()}}, \code{\link[=as_ggplot]{as_ggplot()}}
}
