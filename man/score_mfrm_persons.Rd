% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-person-scoring.R
\name{score_mfrm_persons}
\alias{score_mfrm_persons}
\alias{summary.mfrm_person_scores}
\alias{print.mfrm_person_scores}
\alias{plot.mfrm_person_scores}
\title{Score source Persons under ordinary or extended RSM calibration}
\usage{
score_mfrm_persons(fit, persons = NULL, level = 0.95, quad_points = NULL)

\method{summary}{mfrm_person_scores}(object, ...)

\method{print}{mfrm_person_scores}(x, ...)

\method{plot}{mfrm_person_scores}(x, ...)
}
\arguments{
\item{fit}{A numerically ready ordinary RSM MML, testlet or shared-rater fit.}

\item{persons}{Distinct source Person IDs; \code{NULL} returns all source Persons,
including Persons whose assigned scores are all missing.}

\item{level}{Conditional equal-tail interval probability, between zero and one.}

\item{quad_points}{Extension quadrature order; \code{NULL} uses its fit's order.
Ordinary ability integration is continuous and does not use quadrature;
leave this argument \code{NULL} for ordinary fits.}

\item{object, x}{An ordinary \code{mfrm_person_scores} result.}

\item{...}{Display controls passed to \code{\link[=plot.mfrm_testlet_scores]{plot.mfrm_testlet_scores()}}; unused
by print and summary.}
}
\value{
Ordinary fits return \code{mfrm_person_scores} with \code{table},
\code{scoring_data}, data usage, settings and source metadata. Extensions retain
their existing scoring classes. Supply saved scores to \code{\link[=compare_mfrm]{compare_mfrm()}}
as \code{person_scores}; extension scores also support model-aware maps through
\code{\link[=mfrm_results]{mfrm_results()}}. Saved plots/reports do not recompute scores.
}
\description{
Compute conditional EAPs, posterior SDs and continuous equal-tail intervals
from a fitted model's complete source roster. Selection changes outputs,
not the data conditioning shared effects. No calibration is re-estimated.
}
\details{
Ordinary fits use the bounded model/population specification of
\code{\link[=mfrm_response_diagnostics]{mfrm_response_diagnostics()}}: additive unanchored severity facets and
known N(0,1) or an estimated intercept-only normal population. Both the
fitted population mean and variance are retained. EAP, posterior SD and
interval endpoints integrate the continuous conditional density; endpoints
are not quadrature-grid quantiles. Ordinary plug-in scoring is unchanged.

For extensions this calls \code{\link[=predict.mfrm_testlet]{predict.mfrm_testlet()}} or
\code{\link[=score_mfrm_random_rater]{score_mfrm_random_rater()}} on the complete source roster. Their numerical
checks, approximation limits and uncertainty definitions apply unchanged.
Use those functions directly for a different complete scoring roster.

Intervals condition on fitted calibration and the assumed normal
population. They exclude calibration-estimation uncertainty, do not test
Person differences and do not guarantee frequentist coverage for each
fixed ability. Missing-only Persons remain \code{prior_only}; zero ability
variance and failed integration return \code{unavailable}, not zero-width
intervals. No missing scores are imputed.
}
\examples{
\donttest{
ratings <- load_mfrmr_data("example_core")
fit <- fit_mfrm(ratings, "Person", c("Rater", "Criterion"), "Score")
scores <- score_mfrm_persons(fit, persons = unique(ratings$Person)[1:4])
scores$table
plot(scores)
}
}
\seealso{
\code{\link[=plot.mfrm_testlet_scores]{plot.mfrm_testlet_scores()}}, \code{\link[=mfrm_response_diagnostics]{mfrm_response_diagnostics()}}
}
