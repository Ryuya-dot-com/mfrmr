% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-generalizability.R
\name{compare_mfrm_generalizability}
\alias{compare_mfrm_generalizability}
\alias{plot.mfrm_generalizability_comparison}
\title{Compare main-effects and interaction-expanded G-study decompositions}
\usage{
compare_mfrm_generalizability(
  fit,
  data = NULL,
  object_facet = "Person",
  random_facets = NULL,
  random_interactions = NULL,
  design_grid = NULL,
  residual_scaling = c("sensitivity", "highest_order", "single_condition", "none"),
  reml = TRUE,
  progress = interactive()
)

\method{plot}{mfrm_generalizability_comparison}(
  x,
  y = NULL,
  type = c("coefficients", "coefficient_delta", "variance_delta", "d_study_overlay",
    "design_check"),
  metric = NULL,
  model = NULL,
  x_var = NULL,
  group_var = NULL,
  panel_by = NULL,
  design_check_type = c("interaction_cells", "highest_order", "facet_levels", "overview"),
  design_check_status = NULL,
  design_check_sort_by = c("status", "value", "name"),
  design_check_decreasing = NULL,
  component_type = NULL,
  sort_by = c("value", "source", "none"),
  decreasing = TRUE,
  top_n = NULL,
  draw = TRUE,
  main = NULL,
  palette = NULL,
  preset = c("standard", "publication", "compact", "monochrome"),
  ...
)
}
\arguments{
\item{fit}{An \code{mfrm_fit} from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{data}{Optional data frame passed to \code{\link[=mfrm_generalizability]{mfrm_generalizability()}}.}

\item{object_facet}{Facet that plays the role of the object of measurement.}

\item{random_facets}{Character vector of random measurement facets. Default
uses every facet other than \code{object_facet}.}

\item{random_interactions}{Explicit two-way random interactions for the
expanded model, such as \code{"Person:Token"} or
\code{c("Person:Rater", "Rater:Token")}.}

\item{design_grid}{Optional D-study grid passed to \code{\link[=mfrm_d_study]{mfrm_d_study()}} for both
baseline and expanded decompositions. When \code{NULL}, observed facet counts
are used.}

\item{residual_scaling}{Residual-scaling assumption passed to
\code{\link[=mfrm_d_study]{mfrm_d_study()}}. The default \code{"sensitivity"} returns the three built-in
assumptions for both decompositions.}

\item{reml}{Logical, passed to \code{\link[=mfrm_generalizability]{mfrm_generalizability()}}.}

\item{progress}{Logical, passed to \code{\link[=mfrm_generalizability]{mfrm_generalizability()}} for the two
G-study refits.}

\item{x}{Output from \code{\link[=compare_mfrm_generalizability]{compare_mfrm_generalizability()}} for the plot method.}

\item{y}{Reserved for S3 generic compatibility.}

\item{type}{Comparison plot route. \code{"coefficients"} compares baseline and
expanded \code{G}/\code{Phi}; \code{"coefficient_delta"} shows expanded-minus-baseline
movement; \code{"variance_delta"} shows source-level variance movement;
\code{"d_study_overlay"} overlays projected D-study rows by model; and
\code{"design_check"} compares the baseline and expanded design-check signals.}

\item{metric}{Metric(s) to display. Defaults to \code{G}/\code{Phi} for coefficient
routes, \code{DeltaVariance} for variance movement, and \code{Phi} for D-study
overlays.}

\item{model}{Optional model filter using values from the comparison tables,
such as \code{"baseline_main_effects"} or \code{"interaction_expanded"}.}

\item{x_var}{Planned-count column for \code{type = "d_study_overlay"}.}

\item{group_var}{Optional grouping column for \code{type = "d_study_overlay"}.}

\item{panel_by}{Optional single small-multiple column for
\code{type = "d_study_overlay"}.}

\item{design_check_type}{Route passed to each model's
design-check plot table when \code{type = "design_check"}.}

\item{design_check_status}{Optional design-check status filter.}

\item{design_check_sort_by}{Design-check ordering rule.}

\item{design_check_decreasing}{Optional design-check ordering direction.}

\item{component_type}{Optional component-type filter for
\code{type = "variance_delta"}.}

\item{sort_by}{Ordering rule for \code{type = "variance_delta"}:
\code{"value"} (default), \code{"source"}, or \code{"none"}.}

\item{decreasing}{Logical. Whether \code{sort_by} uses decreasing order.}

\item{top_n}{Optional positive integer limiting displayed rows after
filtering and sorting.}

\item{draw}{If \code{TRUE}, draw with base graphics. If \code{FALSE}, return an
\code{mfrm_plot_data} object for custom graphics.}

\item{main}{Optional plot title.}

\item{palette}{Optional vector of colors for base graphics.}

\item{preset}{Plot style preset used by the base-graphics renderer.}

\item{...}{Reserved for future extensions.}
}
\value{
An object of class \code{mfrm_generalizability_comparison}, a list with
\code{summary}, \code{comparison_review}, \code{coefficients}, \code{variance_components},
\code{variance_delta}, \code{d_study}, \code{design_checks}, \code{baseline}, \code{expanded}, and
\code{warnings}.
}
\description{
\code{compare_mfrm_generalizability()} fits the historical main-effects
G-study baseline and an explicitly requested interaction-expanded G-study,
then reports how the G / Phi coefficients, variance components, and optional
D-study projections change. This is a sensitivity-review layer: it helps
users see how much observed-score variance moves out of \code{Residual} when
targeted token, rater, item, or criterion interactions are estimated
separately. It does not compare MFRM separation reliability with G-theory
reliability-like coefficients.
}
\details{
This comparison should not be read as automatic evidence that the expanded
model is superior. If the expanded model is singular, or if a named
interaction variance is estimated at zero, the result usually means the data
do not support separating that component from the remaining residual/error
structure. The intended workflow is to use this helper as a reporting
sensitivity check, then keep the main-effects baseline or a smaller
interaction set when the expanded model is unstable.
}
\examples{
\dontrun{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 30)
if (requireNamespace("lme4", quietly = TRUE)) {
  cmp <- compare_mfrm_generalizability(
    fit,
    random_interactions = c("Person:Rater", "Rater:Criterion"),
    design_grid = expand.grid(Rater = 2:4, Criterion = 4),
    residual_scaling = "sensitivity"
  )
  cmp$summary
  cmp$comparison_review
  cmp$design_checks$overview
  cmp$variance_delta
  cmp$warnings
  plot(cmp, type = "design_check", draw = FALSE)
}
}
}
\seealso{
\code{\link[=mfrm_generalizability]{mfrm_generalizability()}}, \code{\link[=mfrm_d_study]{mfrm_d_study()}}
}
