% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-testlet-scoring.R
\name{plot.mfrm_testlet}
\alias{plot.mfrm_testlet}
\title{Plot testlet-model fixed-facet estimates}
\usage{
\method{plot}{mfrm_testlet}(
  x,
  facet = x$input$columns$facets[1],
  draw = TRUE,
  style = c("interval", "precision", "distribution"),
  sort = c("input", "estimate", "uncertainty"),
  palette = c("accessible", "mono"),
  title = NULL,
  caption = NULL,
  show_title = TRUE,
  show_notes = TRUE,
  show_labels = TRUE,
  reference = 0,
  text_scale = 1,
  point_size = 2.5,
  intervals = c("none", "normal"),
  level = 0.95,
  ...
)
}
\arguments{
\item{x}{An object from \code{\link[=fit_mfrm_testlet]{fit_mfrm_testlet()}}.}

\item{facet}{A fixed facet to display; defaults to the first fixed facet.}

\item{draw}{\code{FALSE} returns plot data without opening a device.}

\item{style}{\code{"interval"} keeps every labeled row; \code{"precision"} plots
estimates against interval width (upper minus lower bound); \code{"distribution"}
shows the empirical cumulative distribution of finite point estimates,
excluding prior-only and unavailable scores. No density is reconstructed.}

\item{sort}{Row order for interval plots: input order, increasing estimate,
or increasing interval width. Missing values come last; ties retain input
order. Sorting is descriptive, not a test of differences.}

\item{palette}{\code{"accessible"} uses blue and dark orange with filled/open
symbols; \code{"mono"} uses dark grey with the same symbol distinctions.}

\item{title, caption}{\code{NULL} uses the default text; a string replaces it;
\code{""} removes it. Interpretation notes remain in the saved plot data.}

\item{show_title, show_notes}{Show the title and caption, respectively.
\code{FALSE} hides text without deleting its metadata.}

\item{show_labels}{Show IDs on interval and precision plots. Distribution
plots summarize estimates and do not label individual IDs. For crowded
precision plots use \code{FALSE} and consult the retained table.}

\item{reference}{Vertical reference line in logits; \code{NULL} omits it.
This is an orientation aid, not a quality threshold.}

\item{text_scale}{Positive multiplier for text size.}

\item{point_size}{Positive point size in ggplot millimetres; base graphics
use the corresponding relative size (2.5 is the default).}

\item{intervals}{\code{"none"} (default) shows points only; \code{"normal"}
explicitly requests observed-information normal-approximation bounds.
Interval-width views or sorting require this explicit selection.}

\item{level}{Nominal level of explicitly requested calibration intervals;
default 0.95. This does not establish finite-sample coverage.}

\item{...}{Unused.}
}
\value{
Invisibly, an \code{mfrm_plot_data} object; use \code{\link[=plot_data]{plot_data()}}.
}
\description{
Plot testlet-model fixed-facet estimates
}
\details{
Requested whiskers are observed-information normal approximations
for calibration parameters; finite-sample coverage is not established. Missing
intervals remain missing; points do not classify rater quality.
\code{\link[=plot_data]{plot_data()}} and \code{\link[=plot_data_components]{plot_data_components()}} provide the same table without
drawing. \code{\link[=as_ggplot]{as_ggplot()}} preserves the interval meaning and missing whiskers.
}
\seealso{
\link{mfrmr_workflow_methods}, \code{\link[=mfrmr_output_guide]{mfrmr_output_guide()}},
\code{\link[=mfrmr_interval_guide]{mfrmr_interval_guide()}}, \code{\link[=plot.mfrm_testlet_scores]{plot.mfrm_testlet_scores()}}
}
