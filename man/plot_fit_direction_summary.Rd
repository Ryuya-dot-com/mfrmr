% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-fit-direction-summary.R
\name{plot_fit_direction_summary}
\alias{plot_fit_direction_summary}
\title{Plot underfit and overfit direction rates}
\usage{
plot_fit_direction_summary(
  x,
  diagnostics = NULL,
  facet = NULL,
  directions = c("underfit", "overfit", "mixed", "in_band"),
  value = c("rate", "count"),
  draw = TRUE,
  ...
)
}
\arguments{
\item{x}{Output from \code{\link[=fit_direction_summary]{fit_direction_summary()}}, \code{\link[=fit_p_table]{fit_p_table()}}, or
\code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{diagnostics}{Optional diagnostics passed through when \code{x} is a fit.}

\item{facet}{Optional facet filter.}

\item{directions}{Direction labels to include.}

\item{value}{Plot rates or counts.}

\item{draw}{If \code{TRUE}, draw with base graphics; otherwise return plotting
data invisibly.}

\item{...}{Passed to \code{\link[=fit_direction_summary]{fit_direction_summary()}} when \code{x} is a fit or
fit-p table.}
}
\value{
An \code{mfrm_plot_data} object with \code{summary} and long-form \code{data}
slots.
}
\description{
Visualize the direction table returned by \code{\link[=fit_direction_summary]{fit_direction_summary()}}. The
default plot is a stacked base-R bar chart; with \code{draw = FALSE}, the function
returns a stable \code{mfrm_plot_data} payload for custom graphics.
}
\examples{
\donttest{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 25)
diag <- diagnose_mfrm(fit, residual_pca = "none")
p <- plot_fit_direction_summary(fit, diagnostics = diag, draw = FALSE)
p$data$data
}
}
\seealso{
\code{\link[=fit_direction_summary]{fit_direction_summary()}}, \code{\link[=fit_p_table]{fit_p_table()}},
\code{\link[=plot_simulation_misfit_rates]{plot_simulation_misfit_rates()}}
}
