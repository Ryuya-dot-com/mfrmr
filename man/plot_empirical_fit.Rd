% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-empirical-fit.R
\name{plot_empirical_fit}
\alias{plot_empirical_fit}
\title{mirt-style empirical fit plot for an MFRM facet level}
\usage{
plot_empirical_fit(
  fit,
  diagnostics = NULL,
  facet = NULL,
  level = NULL,
  category = NULL,
  bins = 10L,
  min_bin_n = 5L,
  draw = TRUE,
  preset = c("standard", "publication", "compact"),
  main = NULL
)
}
\arguments{
\item{fit}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}}.}

\item{diagnostics}{Optional output from \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}. When omitted,
diagnostics are computed with \code{residual_pca = "none"}.}

\item{facet}{Facet to inspect. \code{NULL} uses the first non-person facet.}

\item{level}{Level within \code{facet}. \code{NULL} chooses the most extreme
element-level MnSq row within the selected facet, falling back to the
first observed level.}

\item{category}{Optional score category. When \code{NULL}, the plot compares
observed and expected mean score. When supplied, the plot compares the
observed category proportion with the fitted category probability.}

\item{bins}{Number of approximately equal-count person-measure bins.}

\item{min_bin_n}{Minimum weighted bin size flagged in the returned table.}

\item{draw}{If \code{TRUE}, draw with base graphics.}

\item{preset}{Visual preset.}

\item{main}{Optional plot title.}
}
\value{
An \code{mfrm_plot_data} object. The \code{bin_table} / \code{data} slot contains
\code{Bin}, \code{N}, \code{MeanPersonMeasure}, \code{Observed}, \code{Expected}, \code{Residual}, \code{SE},
\code{StdResidual}, and \code{LowN}; \code{raw_table} contains the filtered row-level
metrics used to build the bins.
}
\description{
Plots observed response behavior against model-expected behavior across
person-measure bins for one facet level. This gives a mirt-inspired
empirical fit view for many-facet models: points show empirical bin means
(or empirical category proportions), and the line shows the corresponding
fitted-model expectation in the same bins.
}
\details{
This is a descriptive empirical overlay, not mirt's \code{S_X2} test and not a
replacement for strict marginal diagnostics. It is designed for the common
research workflow: identify a level with \code{\link[=fit_p_table]{fit_p_table()}},
\code{\link[=plot_bubble]{plot_bubble()}}, or \code{summary(diagnose_mfrm(...))}, then inspect where the
observed response curve departs from the fitted model across the person
measure scale.
}
\examples{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 25)
diag <- diagnose_mfrm(fit, residual_pca = "none")
p <- plot_empirical_fit(fit, diagnostics = diag,
                        facet = "Rater", bins = 5, draw = FALSE)
p$data$target
p$data$bin_table
p_cat <- plot_empirical_fit(fit, diagnostics = diag,
                            facet = "Rater", category = 4,
                            bins = 5, draw = FALSE)
p_cat$data$metric
}
\seealso{
\code{\link[=fit_p_table]{fit_p_table()}}, \code{\link[=plot_bubble]{plot_bubble()}}, \code{\link[=plot_person_fit]{plot_person_fit()}},
\code{\link[=plot_qc_dashboard]{plot_qc_dashboard()}}
}
