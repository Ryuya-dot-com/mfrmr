% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-fit-direction-summary.R
\name{fit_direction_summary}
\alias{fit_direction_summary}
\title{Summarize underfit and overfit directions from fit statistics}
\usage{
fit_direction_summary(
  fit,
  diagnostics = NULL,
  scope = c("element", "person", "category"),
  p_adjust = "holm",
  alpha = 0.05,
  lower = NULL,
  upper = NULL,
  reference = c("mfrmr", "facets"),
  zstd_cap = c("auto", "none", "facets")
)
}
\arguments{
\item{fit}{Output from \code{\link[=fit_mfrm]{fit_mfrm()}} or a data frame returned by
\code{\link[=fit_p_table]{fit_p_table()}}.}

\item{diagnostics}{Optional output from \code{\link[=diagnose_mfrm]{diagnose_mfrm()}}. Ignored when
\code{fit} is already a fit-p table.}

\item{scope}{Fit-statistic scope passed to \code{\link[=fit_p_table]{fit_p_table()}}.}

\item{p_adjust}{Multiplicity adjustment passed to \code{\link[=fit_p_table]{fit_p_table()}}.}

\item{alpha}{Screening alpha used for adjusted p-value counts.}

\item{lower, upper}{Optional MnSq screening band. Defaults to
\code{\link[=mfrm_misfit_thresholds]{mfrm_misfit_thresholds()}}.}

\item{reference}{Fit-statistic reference passed to \code{\link[=fit_p_table]{fit_p_table()}}.}

\item{zstd_cap}{ZSTD cap policy passed to \code{\link[=fit_p_table]{fit_p_table()}}.}
}
\value{
A data frame of class \code{mfrm_fit_direction_summary} with one row per
\code{Scope} x \code{Facet} x reference combination. Count columns report the number
of levels in each direction; rate columns use the number of classified
levels as denominator. The original fit-p table is stored in the \code{detail}
attribute.
}
\description{
Build a compact direction table from \code{\link[=fit_p_table]{fit_p_table()}} output. The table
separates \code{underfit}, \code{overfit}, \code{mixed}, and \code{in_band} labels so researchers
can report whether misfit is driven by noisy/unpredictable responses
(MnSq above the upper band) or overly predictable responses (MnSq below the
lower band).
}
\details{
Direction labels are MnSq-band labels. ZSTD and adjusted p values are counted
separately through \code{PFlagN} and \code{PFlagRate}; they do not define the
underfit/overfit direction. This keeps the substantive interpretation
stable when users compare \code{reference = "mfrmr"} with \code{reference = "facets"}.
}
\examples{
\donttest{
toy <- load_mfrmr_data("example_core")
fit <- fit_mfrm(toy, "Person", c("Rater", "Criterion"), "Score",
                method = "JML", maxit = 25)
diag <- diagnose_mfrm(fit, residual_pca = "none")
dir_tab <- fit_direction_summary(fit, diagnostics = diag)
dir_tab[, c("Facet", "UnderfitRate", "OverfitRate", "AnyMisfitRate")]
}
}
\seealso{
\code{\link[=fit_p_table]{fit_p_table()}}, \code{\link[=plot_fit_direction_summary]{plot_fit_direction_summary()}},
\code{\link[=summarize_simulation_misfit]{summarize_simulation_misfit()}}
}
