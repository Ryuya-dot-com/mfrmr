# mfrmr 0.2.3 GPCM score-rule v4 freeze record

Status: bounded rule and calibration interpretation frozen for a future
structurally disjoint confirmation design, 2026-08-11. Confirmation execution,
general `NUM-SCORE-TOL`, a boundary theorem, inference, and release promotion
remain unauthorized.

## Frozen lineage

The no-fit seal binds the complete package `DESCRIPTION`/`NAMESPACE`/`R/`/`src/`
payload, six executable validation sources, the immutable rejected v3
confirmation, and the one-time v4 boundary-completion artifact. The freeze
source SHA-256 is
`3baab8bfabf5b05600a2a12057cfcb6b79c7c3c665824675afb6cafa9c56744b`;
the resulting source/artifact/rule seal is
`0c11704614ada80a3d60add7b665e8955b098fc8c7211ef392cab4704871537c`.

The executable source registry contains:

- v4 rule: `c746bd02b435e851af0ff89fff6320c34e66a1aa9fe8c52d0b6447689dd5a126`;
- retrospective evaluator: `831fcd4683e46785291c832242867f1962b56ce3a142c09da78fcbc311d08025`;
- completion design: `bf6ae572a3c0c2253c6fbd35fc5138eaaae92728d7d9588f1d50cebfafcae838`;
- completion runner: `78e0bcfd14c5c4343e0ff4beeb9c250b324539f1dffe8468bed4ccaf13f8090e`;
- authorization: `41f51a6d3e56b09ec92d67aee2f3ff92b0438a49c4e0d370701071072a95d3a3`;
- independent validator: `ff2b8e55316251411076480751f1ed39459d5a2be8675c0bce8a99482631b9e7`.

The rejected v3 artifact remains unchanged at
`7836d859cca48e9a3641d94edda000218bb9c9f2903d801d7b9c9f03da017f2e`.
The v4 completion artifact is fixed at
`5998c6c5f01a9436af0af152d30315655291275d72c169649e048f0d5647400e`.

## Mathematical review

V4 changes only the representation-aware classification of prespecified
constructed inclusive-boundary points. The independent analytic-score,
five-point finite-difference, expanded-log Jacobian, and positive-slope
Jacobian rules retain the v3 absolute floors, relative rates, and finite-
difference spread/roundoff multipliers exactly. The log-slope envelope remains
three.

For a constructed point, the permitted binary64 forward error is the sum of
input-rounding, sequential-summation, and final comparison bounds. For a
retained fitted solution the allowance remains exactly zero. Retrospective
application changes exactly one six-level construction from the extreme
handoff to the finite region, leaves every retained-solution classification
unchanged, and honestly records that its finite difference was missing. The
new calibration-only fixture supplies exactly that missing derivative without
reopening or retrying any v3 cell.

The independent completion validator reproduces the issued and consumed
authorization hashes, complete 4/24/1/30 denominator, aggregate maxima, source
chain, and artifact identity. All four numerical components pass. The source
fit remains `review`, its Person-marginal inference is not promoted, and the
completion fixture is permanently confirmation-ineligible.

## Target-path review

The authorization stores the target in repository-relative form. It does not
claim that an absolute path was recorded. The independent validator resolves
that exact relative target from the repository root to the fixed artifact and
records both `RepositoryRelativeTargetDisclosed = TRUE` and
`AbsoluteTargetRecorded = FALSE` in the freeze decision. The prospective v4
schema required an exact output-path field, same-process consumption, and
embedded provenance, but did not require an absolute path. Adding an absolute-
path rejection after observing the result would be a post-hoc rule change;
instead the actual path form is part of this seal and must remain visible.

## Frozen boundary

The decision is
`v4_bounded_rule_frozen_for_disjoint_confirmation`.
`DisjointConfirmationDesignMayProceed = TRUE`, while
`ConfirmationExecutionAuthorized = FALSE`,
`GeneralNUMSCORETOLFrozen = FALSE`, `BoundaryProven = FALSE`, and
`InferenceAuthorized = FALSE`. Twenty-six focused expectations validate the
rule, six-source/two-artifact chain, retrospective invariance, completion,
authorization, target disclosure, and non-promotion boundary without fitting.

The next step is to seal a new structurally disjoint v4 confirmation family
without fitting. It must not reuse v3 or completion persons, raters, criteria,
response constructions, or results. Its design, denominators, source identity,
and execution authorization must be fixed before any confirmation result is
opened. No large simulation is required for this bounded implementation-
mathematics claim.
