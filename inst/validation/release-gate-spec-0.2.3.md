# mfrmr 0.2.3 release-gate specification

## Document state

| Field | Value |
| --- | --- |
| Target release | 0.2.3 |
| Specification ID | `0.2.3-draft.83d2b2b1g24` |
| Date | 2026-08-11 |
| Weak-information adjunct | Draft.83d2b2b1g24 implements the response-free fresh-site one-shard issuer. Six recomputed gates bind exact b1g23 entry/R0201 identities to nested runtime/site receipts. A focused GO issues one hash-valid production record and unexecuted active manifest; a forced occupied-target NO-GO remains auditable and cannot issue. Fifty-one assertions pass without generating a response, fit, checkpoint, root, or lock. |
| Current integration state | b1g17--b1g23 evidence remains frozen; b1g24 closes the one-shard issuance implementation and demonstrates an exact R0201 authorization instance. `Replicate201MayBeOpened=TRUE` applies only to a fresh validated record and active manifest; no shard has executed. `LargeSimulationMayStart=FALSE`, all later shards remain unauthorized, and full R0201 execution/review is next. |
| Status | M1 is recorded and M2 remains in progress. Drafts.45--79 retain the additive MML/JML, GPCM-owner, quadrature, Person-boundary, TAM/immer, adversity, checkpoint, connectivity, and topology evidence recorded below. Draft.80 freezes the typed observed-score G-theory/D-study roadmap; Draft.81 implements its parser/algebra oracle; Draft.82 adds balanced ANOVA/MoM and lme4 fixtures; Draft.83a adds incidence auditing; Draft.83b adds allocation algebra; Draft.83c1 adds covariance/information and lme4 diagnostics; Draft.83c2 adds exact-overlap Gaussian ML/REML glmmTMB/lme4 parity; Draft.83d1 freezes a pre-simulation ADEMP registry and denominators; Draft.83d2a deterministically generates 22 executable scenario identities while preserving two anchor blocks; Draft.83d2b0 binds all generated data and 89 fit units to exact scalable structural pre-fit adjudication; Draft.83d2b1 records all 89 atomic balanced-MoM/lme4/glmmTMB outcomes while retaining a failed near-boundary zero-false-ready gate; Draft.83d2b2b1b records exact-design fitted-null bootstrap mechanics without promoting an inferential result; Draft.83d2b2b1c authorizes a checkpointed descriptive feasibility run under a new untouched-data manifest; Draft.83d2b2b1d executes all 3,000 frozen pairs with exact resume; Draft.83d2b2b1e executes the three-profile viewed-data numerical sensitivity while failing its finite-only replay gate and retaining backend-specific numerical blockers; Draft.83d2b2b1f separately adjudicates the seven matching typed non-finite states without refitting or changing b1e; Draft.83d2b2b1g freezes the glmmTMB stabilization DAG and 9,000-pair manifest; Draft.83d2b2b1g1 implements its atomic runner and completes only a 120-pair viewed smoke with strict BFGS joint-state failures; Draft.83d2b2b1g2 prospectively repairs fixed-coordinate transport on the same denominator; Draft.83d2b2b1g3 no-refit separates objective, reported-likelihood, optimizer, gradient, curvature, and envelope evidence; Draft.83d2b2b1g4 retains raw derivatives and separates scale-aware, spectral-PD, Cholesky, and direct-step observables while withholding full execution; Draft.83d2b2b1g5 freezes a sealed, source-audited stationarity-calibration design with analytic affine controls and separate numerical/boundary/statistical states; Draft.83d2b2b1g6 freezes only the high-accuracy reference mechanics after analytic and nonreserved adaptive-derivative, multialgorithm, curvature, boundary-profile, and integrity checks; Draft.83d2b2b1g7 completes the fail-closed backend-accounting, candidate-state, profile-aggregation, and method-coverage preauthorization audit without opening reserved data; Draft.83d2b2b1g8 completes repeatable nonreserved glmmTMB ML high-accuracy reference coverage; Draft.83d2b2b1g9 completes the analytic lme4 objective preflight; Draft.83d2b2b1g10 completes repeatable nonreserved lme4 ML/REML reference coverage; Draft.83d2b2b1g11 freezes the truth-blind acceptance/Monte-Carlo policy; Draft.83d2b2b1g12 implements the coordinate-correct production boundary probe; Draft.83d2b2b1g13 implements exact-resume complete-denominator mechanics; Draft.83d2b2b1g14 freezes real production adapters plus a runtime-, dependency-, output-, unit-, and 100-shard-identified reserved manifest while retaining authorization, calibration, and stationarity blockers; Draft.83d2b2b1g15 completes the response-free filesystem, capacity, shard-accounting, and one-way authorization-readiness preflight while issuing no execution authorization; Draft.83d2b2b1g15a retains the 30-by-100 design only for numerical-rule calibration after separating independent datasets from dependent computational rows and withholding achieved-precision or broad-performance claims; and Draft.83d2b2b1g16 supersedes the narrower activation-eligibility state after a same-seed ambient-RNG counterexample and eight runtime/process/single-writer blockers, while leaving replicate 201 sealed. A separately labelled future Draft.85a0 preflight implements supplied-matrix multivariate covariance/allocation/composite algebra without changing the ordered univariate prerequisites or public claim. Criterion-owned and rater-owned aligned GPCM remain separate, while decoupled, multiplicative, multidimensional, response-style, severity-interaction, and repeated-rating families remain explicit non-claim or generator-only strata. Expanded replicated recovery and coverage, compact-interval and tail proofs, complete nonlinear JML/MML slope proofs, fit/DFF operating characteristics, prospective quadrature calibration, PCA computability, estimator-specific weak-information rules, metric-specific external eligibility, G-theory object-nested and replicate operators, executable univariate recovery rows, multivariate covariance estimation/recovery, uncertainty/external overlap, candidate identity, confirmation, and release engineering remain pending. G-theory parser, balanced estimation, covariance/information, matched-backend diagnostics, pre-simulation registry/generator/prefit/atomic-fit/bootstrap-mechanics/feasibility-planning/execution/numerical-sensitivity/typed-replay/stabilization-design/runner-smoke/alignment-replay/numerical-adjudication/stationarity-instrumentation/calibration-design/reference-calibration/preauthorization-audit/ML-reference-coverage/lme4-objective-preflight/lme4-reference-coverage/acceptance-policy/production-boundary-probe/exact-resume-runner-mechanics/production-adapter-preflight/one-way-authorization-preflight/Monte-Carlo-value-audit/pre-activation-hardening evidence, supplied-matrix multivariate algebra, and current-surface compatibility rows remain review-only; no checklist row or production stationarity criterion is newly passed/frozen. |
| Prior b1g3 delta | Draft.83d2b2b1g3 adds no-refit adjudication contract `934f96be6e23cd728576cfedbb44d48b68137fbbc74e84d19d7200b8ad52ccc0`, pure ledger adjudicator, result record, and guarded test tier. Result `c7c35d5b961c578b6234a1f29f628f13dac357bc1b046e012832d28ca7f3d4de` binds all 120 pair rows and 20 route envelopes after recomputing b1g2 scientific hashes. The installed glmmTMB `logLik` mask is source-hashed; raw objective, reported likelihood, optimizer code, two gradient surfaces, and two curvature diagnostics remain independent. The inherited 1e-6 value partitions signs descriptively and is not a pass rule. No full execution authorization, stationarity criterion, sufficient numerical-eligibility rule, size, power, production `B`, threshold, recovery, interval, D-study stability, coefficient, estimator preference, public support, candidate, confirmation, or checklist pass is added. Historical b2b0/b1a/b1b/b1c/b1d/b1e/b1f/b1g/b1g1/b1g2 identities, Draft.84, future Draft.85, and `FiniteOriginalJMLMaximum = FALSE` remain intact. |
| Confirmation authorized | No |
| Current b1g4 delta | Draft.83d2b2b1g4 adds scale-aware instrumentation contract `97dcdd0103a3eb9a714ac56008f801af57853bc816f42e5bc9ab33dd63f3ae32`, content-addressed raw derivative sidecars, exact fit/derivative replay, no-fit resume, and a threshold-free 240-fit/40-stratum adjudication. Execution `a825ab427da7e4a8160e428a7a6b00038f364b1c15049df6d5e4bf03bbbbbade` and result `40949ff311e6dbe1289cf6488aa2db3642a65ca64d6b794b47aeb5001d53acf1` reproduce. `ScaleAwareMeasurementSchemaReady=TRUE` is narrow; no stationarity rule, sufficient numerical-eligibility rule, full execution, calibration, size/power, recovery, interval, D-study stability, coefficient, estimator preference, public support, candidate, confirmation, or checklist pass is added. Historical b2b0/b1a/b1b/b1c/b1d/b1e/b1f/b1g/b1g1/b1g2/b1g3 identities, Draft.84, future Draft.85, and `FiniteOriginalJMLMaximum = FALSE` remain intact. |
| Current b1g5 delta | Draft.83d2b2b1g5 adds design contract `278353d1668501d04dd3af4adc96dfcd39b232796057242418f89601b22b99ac` and sealed workload manifest `0dbe9e92bed7baa27b6c5f29bed0759a789bcc02c285bd77d749a9cc9666e4d0`. It revalidates b1g4, separates finite stationarity, curvature, boundary-limit, and statistical-resolution states, freezes five candidate metric families plus an eight-stage reference architecture, and verifies affine Hessian-inertia/Newton-decrement identities. The 3,000-dataset reservation maps to 144,000 candidate fits and 24,000 reference problems, all unauthorized. `DesignSchemaReady=TRUE` is narrow; reference tolerances, stationarity, sufficient numerical eligibility, calibration execution, full execution, bootstrap, inference, coefficient, public support, candidate, confirmation, and every checklist pass remain false. |
| Current b1g6 delta | Draft.83d2b2b1g6 adds reference contract `60e04706736c0e7273dfa321d0d41a3a9ed4bb8362a0b7d428f8507653ecce9a`, nonreserved manifest `87b42667d3dbeb2ecd045b23b32cf23a5f9919b0d26ac75c5771baf691770d3a`, and execution `28f155c91065cb56ebe695234eab7867392e25fe413ab362717e760f5e775e72`. Six analytic objectives and four full/reduced glmmTMB REML objectives from nonreserved replicates 901--902 pass AD-independent adaptive finite-difference, numerical-Hessian symmetry, three-algorithm consensus, Newton, nuisance-stationary boundary-profile, and sidecar-integrity gates. `ReferenceToleranceFrozen=TRUE` is narrow; the production stationarity criterion, reserved calibration 201--300, full stabilization, bootstrap operating characteristics, inference, coefficient, public support, candidate, confirmation, and every checklist pass remain false or unauthorized. |
| Current b1g7 delta | Draft.83d2b2b1g7 adds audit `b293987e768ec0e998d3224a6df0689f0ab8b6f2268704ef422e333865d82765` and corrected prospective manifest `7cce9d42faccfbbdf928c9ec4978fef25c50aa562750141fbab45a53b75885f8`. Six glmmTMB versus three lme4 profiles yield a 108,000 candidate-fit upper bound, correcting b1g5 by 36,000 without rewriting its historical identity; 24,000 reference problems remain. Candidate state precedence is fail-closed and profile aggregation is per dataset-method-model-role by minimum finite objective plus frozen exact-tie priority, independent of metric and truth. Only glmmTMB REML reference mechanics are ready. `PreauthorizationAuditReady=TRUE` is narrow; authorization readiness, method coverage, acceptance policy, boundary probe, runner, reserved calibration, stationarity, inference, coefficient, public support, candidate, confirmation, and every checklist pass remain false or unauthorized. |
| Current b1g8 delta | Draft.83d2b2b1g8 adds ML coverage contract `1216ae3591fc026a61b4fb6581ebe79e33d34e4e2b6bf04a969a4c93c3e06689`, nonreserved manifest `2974db4aefd07636d286b8227edb6dd50b481764e9dd7060296bd379a2688434`, and execution `46ea4be751a3c54904bac28da31f15e5e05f347b9e8f10a1194887f55557807d`. The same generator hashes as b1g6 feed a separately identified ML surface; every polished objective differs from REML by more than `1e-3`. Four objectives pass consensus, AD-independent derivative, curvature, and sidecar checks; both six-point profiles pass nuisance stationarity and support finite interiors; a second complete replay reproduces rows and hashes. `GlmmTMBMethodCoverageReady=TRUE` is narrow; lme4 ML/REML, complete method coverage, calibration authorization, stationarity, inference, coefficient, public support, candidate, confirmation, and every checklist pass remain false or unauthorized. |
| Current b1g9 delta | Draft.83d2b2b1g9 adds analytic audit `83faaaf570bd814c000924aa21396ade00958fb8134cec553a0eaa985382ca67` and preflight contract `20d6fb656ac2f2996e5881a07729a3e4fb2f417859f90efde7ee72784ba62092`. Independent dense ML/REML objectives agree with lme4 theta-only closures at about `1e-12`, analytic gradients agree with Richardson differences within `5e-9`, fitted accessors/log-likelihoods agree, and exact-zero full-to-reduced identities hold in both modes. Source-hashed negative controls exclude misleading convenience accessors. `Lme4ObjectivePreflightReady=TRUE` is narrow; the lme4 box solver, boundary profile, nonreserved replay, two lme4 method lanes, complete coverage, calibration authorization, stationarity, inference, coefficient, public support, candidate, confirmation, and every checklist pass remain false or unauthorized. |
| Current b1g10 delta | Draft.83d2b2b1g10 adds analytic audit `ec331fe2856fb42014c3bf1939079c90f195f2cfb9dcbcb195af55cc96599c0a`, policy `9e0031e200739c66bf2510c56068bbf4f3e1f9075669167becced7ec7f158a82`, contract `419fbf43fd1b86ab494aa96224916c0bfa9c1e1ef2668f8877d9d39659bcc7e0`, nonreserved manifest `f26d4a2fe5670d9b9395f97669f0ef368f9c5067580394ff5b20acccf5e8580b`, and execution `b84c1d53f8653bb5329a0a165e2249b36e5d12e10c26099ab15cbdfac4281e8a`. Eight lme4 ML/REML full/reduced objectives from replicates 901--902 pass three-algorithm consensus, an independent sparse Gaussian objective/gradient oracle, analytic Newton polishing, raw and curvature-scaled KKT, and positive free-curvature checks. Four seven-point nuisance-reoptimized profiles retain finite interiors and agree with their reduced zero endpoints within `2.27e-11`; a complete repeat reproduces every atomic row, sidecar hash, and execution hash. `ReferenceMethodCoverageComplete=TRUE` is narrow; `BoundaryFirstOrderSufficiencyClaim=FALSE`, and acceptance policy, production boundary probe and runner, calibration authorization/execution, stationarity, inference, coefficient, public support, candidate, confirmation, and every checklist pass remain false or unauthorized. |
| Current b1g11 delta | Draft.83d2b2b1g11 adds four-lane receipt `777e7550a188f89515854738e3b7e42ef418037de4c9f7166a67d61e6dfa2e9e`, policy `7962e47df285812d8c785f206d51925b44a13d02037b7b40a619cb80ce833a62`, selection audit `cfdaa73ddc0beb6cc6ca3fbdc2cd7c73bd899bf9e6bebaab675ccc7bd88b16f7`, and contract `1dcc877da78d3975271b33629b3d67bd9f0f48d675fb1ed62e5704baa46b8b1a`. Twenty-four candidates retain primary-cell denominators and distinct safety, boundary, abstention, failure, and reference-unresolved states. Zero observed events retain one-sided exact-binomial upper bounds (`0/100 -> 0.0295130496070`), and 59 zero-event trials are required before that upper bound is at most 0.05. Any observed safety false ready or false boundary handoff rejects a candidate; 20 required decisive reference classes prevent an always-indeterminate rule from winning. Nine focused tests with 102 expectations pass, including retained receipt validation and negative-result controls. `AcceptancePolicyFrozen=TRUE` and `MonteCarloDecisionPolicyFrozen=TRUE` are narrow; production boundary probe, runner, calibration authorization/execution, stationarity threshold/criterion, inference, coefficient, public support, candidate, confirmation, and every checklist pass remain false or unauthorized. |
| Current b1g12 delta | Draft.83d2b2b1g12 adds policy `fb7f938a0e1e5b7be598a180f7d5c06eb3176ebea7f1bfc269491befa865cb6c`, analytic audit `272e5ee2c274c8972ecde6feb039db02e9beb937d4db910555812c35af627eab`, and contract `53a36d72388eb8b4e096ef817aaf94959aa1b3fd3257190cf5c0a8164383d9da`. lme4 exact-zero theta profiles and glmmTMB decreasing log-SD profiles reoptimize nuisance coordinates and require separately fitted reduced-objective agreement. Flat, nonmonotone, endpoint-mismatched, and failed paths remain inconclusive or non-evaluable; `FirstOrderBoundarySufficiencyClaim=FALSE`. lme4/glmmTMB x ML/REML endpoint differences range from `1.9895e-13` to `6.5340e-09`. Eight focused tests with 120 expectations pass. `ProductionBoundaryProbeReady=TRUE` is narrow; runner, calibration authorization/execution, stationarity threshold/criterion, inference, coefficient, public support, candidate, confirmation, and every checklist pass remain false or unauthorized. |
| Current b1g13 delta | Draft.83d2b2b1g13 adds runner policy `c346aebac1a13770e755402b6a0b7f0e1338d8f9f8b238103b7291e01e250f38` and contract `8fb599cd4abbabb454ce416fe3470d3e0f8d23f0bc8f2662630083fb1ec388da`. Atomic dataset-method checkpoints independently retain complete candidate-fit, 24-candidate-per-role decision, and high-accuracy-reference ledgers. Cold, interrupted/resumed, complete-reuse, and corrupted-checkpoint-repair mechanics fixtures reproduce execution `4cdbb0ed2ba69588f81e3fcbd3df634b92a4b7e1929bac387cd6a8562a18100f` in nine tests with 129 expectations. `RunnerImplementationReady=TRUE` is narrow; production evaluator adapters, reserved run manifest, calibration authorization/execution, stationarity threshold/criterion, inference, coefficient, public support, candidate, confirmation, and every checklist pass remain false or unauthorized. |
| Current b1g14 delta | Draft.83d2b2b1g14 adds adapter contract `baf48a948b86c1769aba8a574619c6ce57be17b4b5747ae935f0e430392518a1`, runtime `94cb18393b87ef8409f231b2e62c507f43fb3294cdefeb0f3e8c19c8235e7753`, reserved manifest `019fedf063ce90c3492f9eb37f6dbec43a42474ce9096e7cc2891491d7a158c8`, and nonreserved execution `b9ad747a62b1e14cf1da1e0e4cee8a0a341db969596df3e2de87b25ba908caae`. Real lme4/glmmTMB x ML/REML adapters complete exact 36-fit, 192-decision, and eight-reference ledgers, retain one typed `start_snapshot` failure, share candidate/reference generator and structural pre-fit identities, reproduce a four-unit no-fit reuse, and require the dry execution hash to revalidate before preflight. One hundred exact reserved shards reconstruct all sealed counts but remain non-executable. Seven focused tests with 114 assertions pass. `ProductionAdapterPreflightReady=TRUE` is narrow; calibration authorization/execution/data/results, stationarity threshold/criterion, inference, coefficient, public support, candidate, confirmation, D-study decision, and every checklist pass remain false or unauthorized. |
| Current b1g15 delta | Draft.83d2b2b1g15 adds authorization-preflight contract `44a6d4e2677af111e6eeeae8b7b3143ce8521db34da1769a86b85f32c63ca551` and prospective shard bundle `dfd5099b84dc8ff64b605261d957173e63b8b04645b1f90cc7262b8120d54e82`. One hundred independently hashed, non-executable manifests exactly partition all sealed units and outputs. An actual output-parent probe checks absent target, advisory access, RDS write, checked same-directory rename, identical readback, cleanup, and `df -Pk` capacity. Frozen planning requires 47,775,834,368 available bytes, no more than 296.823333 serial hours or 2.968233 hours per shard under conservative multipliers, and one concurrent shard. Eight focused tests with 149 assertions pass. `AuthorizationReadinessAuditReady=TRUE` and `AuthorizationActivationEligible=TRUE` are narrow; `ExecutionAuthorizationRecordIssued=FALSE` and calibration execution/data/results, stationarity threshold/criterion, inference, coefficient, public support, candidate, confirmation, D-study decision, and every checklist pass remain false or unauthorized. |
| Prior b1g15a delta | Draft.83d2b2b1g15a adds precision-purpose policy `f91a2b54fd766a35255097feaaf27e0be446a2e5f454c6b19f7eb85686a5e58d`, contract `9695149f99885bd4647c40466fef37e99cf321aa7786e232476586730f4fa1d8`, and audit `67987463d2fa587441714da5b6a8fc9046f6c2cc3ec604a416a264762d868f45`. It decomposes 3,000 independent datasets from 108,000 dependent candidate fits, 576,000 decisions, and 24,000 references. Complete-denominator n=100 yields worst-case Bernoulli MCSE 0.05, 0/100 upper 0.029513, and probability 0.952447 of seeing at least one true 3% event. The 3%/95% pair is a purpose-audit benchmark that cannot enter candidate selection. Five focused tests with 87 assertions pass. `NumericalCalibrationDesignPurposeJustified=TRUE` is narrow and retains the 30-by-100 plan; `CalibrationPrecisionEvidenceReady=FALSE`, and broad performance, D-study operating-characteristic, universal sample-size, authorization, calibration, confirmation, inference, and checklist claims remain false or unavailable. |
| Current b1g16 delta | Draft.83d2b2b1g16 adds hardening policy `dc3fb20ef72d64842b1fed7273ebcaa6b2bd794b8757f59ccea03a4d3e945c3c`, portable contract `9ea428bfec87509cacdadba6250d837aef5090c07550f2c0603b79164f2c58c0`, collision-free seed audit `34d69696fb17e6f3af9213113a7f629ab69ff16d2826f6412db01716009cd027`, ambient-RNG negative control `b95dbfdddca4515ec70dcfb23680039de565e744c2b6e4fbc6468a95c62bc7f0`, and portable blocker decision `e4e32bc3fc93c4469ce88ccf46e91866b6a22ef3e81d71a9a3dc483d767f1799`. Four gates pass and eight block; five focused tests with 92 assertions pass. The full runtime/audit hashes are site snapshots and do not enter the portable contract. `PreactivationHardeningAuditReady=TRUE` means the stop decision is complete; `AuthorizationActivationEligible=FALSE`, `LargeSimulationMayStart=FALSE`, and `Replicate201MayBeOpened=FALSE`. |
| Current b1g17 delta | Draft.83d2b2b1g17 adds RNG policy `9d54aeee4e7cf9bc3b20e8e96fa99dfa79febf070cc0d732377287ce897e20a9`, 30-scenario manifest `d3fe95bc5eae79dbc76e622fb2767eb3b0d14a378c767eaaf15872685ca351f7`, cross-ambient replay `39054c3ef7b78783065134ee38303d73d3c6601edfddc8e68cebb8990949f955`, contract `90869c6874e2884b7bf5bc96c1939bd95d1b41603ea76a1d2b1c617f32c700d2`, and audit `918e7da5e0ba484bbdef4251965c25ff31c5d4a39be237c3d218ed47fceae397`. Four focused tests with 67 assertions pass. Five generator component gates pass and `RNG-ADAPTER-01` blocks. `HardenedGeneratorReady=TRUE` and `RNG01ProspectivelyResolved=TRUE` are component-local; `AuthorizationRNG01Closed=FALSE`, `LargeSimulationMayStart=FALSE`, and `Replicate201MayBeOpened=FALSE`. |
| Current b1g18 delta | Draft.83d2b2b1g18 adds adapter policy `4a4d5ff4becc42e931806cc19d97449d8ec083b65b970d201230f4f4b5e19684`, contract `0373db563cd16c63693b02b968dbbd49221a77e1f666b87cc63b17cb6f786e64`, hardened dry manifest `090b761a835098b037b3f65021ebe22eeae9df822628df6898685b1f84e99d15`, hardened execution `14a44a4382e50ba2819d57072288bd213789935d4ed6d78fba674a8081b62aeb`, and paired audit `1e8461eca060b00215240d65c506873d6f082f66b6216fb1ff30761df7dfdb63`. Four focused tests with 65 assertions pass. `NonreservedAdapterRebaseReady=TRUE`; reserved-manifest rebase remains deferred, so authorization and large-simulation flags remain false. |
| Current b1g19 delta | Draft.83d2b2b1g19 adds evidence receipt `3e7911686df046bc5fe507ab276194e463952c45b04230facbadb42120500313`, policy `6a68e71aa5cb30c7f1460b8c81ba76e135ed42dcf2d341a8f966d2e1783ea905`, contract `5075f23a5af8edb7d77ff3cd4c4efaad4d7a624995c3ee04a62ed04a2bee49f5`, hardened reserved manifest `da3905e9b9a605f42e877f695226a0c5ee7089cc04f68ad7ee6350de25c9cbd6`, 100-shard bundle `634159d6d85ea04ecf9447330af122c01284644211aa2dd78d85ab34a92661df`, and audit `9bc4d4dafbeec602f7718c7249bf15f4aeb5a6dbd5862b46e4d8889dc2540d7a`. Four focused tests with 69 assertions pass. `ReservedManifestRebaseReady=TRUE` is response-free and non-executable; entry-point, runtime, runner, authorization, and large-simulation flags remain false. |
| Current b1g20 delta | Draft.83d2b2b1g20 adds lineage receipt `4a453c9b44fd03ae456ba1fda2f8d65208c3ab63b23c6c51d1f6026dfe3e4e92`, kernel policy `ca0b9691600d4aa1241741cb0fa8710de1ea892a8e243c323425c6b76047136c`, contract `86a6015b1eebdb4c9cf8cfa57110d24354ec5999e62f477c82506d8cc6b1edce`, isolated runtime `b32aca03814bd2ae12a7475e61caa338c464a46e4bd90209b186ccc1da9383b0`, and lock/root mechanics audit `ec573bb5e939e4bd711ae267c41894db89b14d576a19d4ac7ac45c268b3071cf`. Four focused tests with 54 assertions pass. Nine shared gates pass and only `RUNNER-01`/`AUTH-RECORD-01` block. `AuthorizationKernelReady=TRUE` is non-executable; authorization and large-simulation flags remain false. |
| Current b1g21 delta | Draft.83d2b2b1g21 adds guarded policy `ecdf55015369c04e9ec81549fe68a624d0c0a025a154f28eeedc3adbd41d86aa`, contract `f6d932f5261b3816bd16afa820cc2c36acf188d5144277b22623a9b41245f552`, fixture manifest `0a515e0977774887094321284a723e058d9b1723f3d45f505245429dc93d6db3`, runner `b179c44076107c64e0f7d030585d38dec11bffba5231dcf4a702631a11742e1c`, and worker `8a7bfbe987a4178f83351508d29defeb631acb9f3cd9c1f724ae85d68fbf2df7`. Four tests with 54 assertions execute the actual replicate-902 four-lane path, preserve all 36/192/8 rows and the one typed fit failure, establish b1g18 semantic parity, and reuse all four checkpoints in a second isolated child. `RUNNER-01` passes; `AUTH-RECORD-01` alone blocks, and all authorization, calibration, confirmation, inference, and large-simulation flags remain false. |
| Current b1g22 delta | Draft.83d2b2b1g22 adds parent receipt `2be44c3fdda1dc455a83eecdd6c0613240050db4b4aee8cb0cb47399b8d73818`, decision policy `4d89c7235e9ae8537b8b9743ba356c8eaf0ad85308cd9c1cf5a4ec87cc562c04`, source audit `d0a59e573ef9c6fba7af5a308a58bc974b04c0a87ca74dc70de1aca39c506b07`, and decision `3df37fa52c9ff688bd5110d4ae097a8fed10123eb898f9967fdcb5fd791c9ab6`. Four focused tests with 48 assertions bind exact prospective shard R0201 and its 30/120/1,080/5,760/240 denominators, verify both existing reserved-source stops, and fail closed under receipt, policy, audit, gate, and issuance mutations. Five gates pass while `RESERVED-ENTRY-01`, `ACTIVE-MANIFEST-01`, and `SITE-RECEIPT-01` block. `AuthorizationDecisionComplete=TRUE` records `no_go_refused_not_issued`; all authorization, calibration, confirmation, inference, and large-simulation flags remain false. |
| Current b1g23 delta | Draft.83d2b2b1g23 adds entry policy `61ee07c4ea86087a7ad8731ef374b38ec7f12621cc8e818501f4e4bab85abd07`, contract `0dca57ff5546d49c2ef49b27e89bd571d19fbbe93ea3698dbdb7d87b69abfd3a`, reduction record `c1c08749050445743e6938357b64aed921fc6b620fdbec31c3586ea40c2f0af0`, active reduction manifest `2221cc38bf526b1cf7d0f66d59090b1af7b1c8b9cbdc6f144caaa06c57399f3a`, entry source `eec3a92a69aa1d1378137342094e536c4c71c3674b15c7a1260869cbcb0d1394`, and worker `a2ed1788b2ad96f3feb298beee67bff2aa99e8d97f3a874f6ed2bd32c3e799cd`. Exact AST transforms replace only the b1g17 generator, b1g18 prepare, and b1g13 execute admission guards; all scientific bodies remain parent-hash bound. Four tests with 75 assertions establish exact b1g21 36/192/8 parity, one retained fit failure, isolated initial execution, exact four-unit resume, complete R0201 prospective counts, and fail-closed capability/record/manifest/job/result mutations. Production issuance, fresh site binding, active R0201 creation, response generation, replicate 201, inference, and large simulation remain false or prohibited. |
| Current b1g24 delta | Draft.83d2b2b1g24 adds issuance source `c93287f1e813bb3dd7265179f6adbe8742486b1f32ec7235297f8b8820a5700f`, policy `688b30494612169df6db370da5460b24f8606106acf695a5921aeab217eb186e`, and contract `b9af6f03b4b3f0a34d1e8e33c6feb5a8ff62ef6516b78f136772cae79eacc620`. Six gates are recomputed from exact entry, active-conversion, runtime, target-specific site, R0201 scope, and confirmation-isolation evidence. One observed GO issues one record and derives an unexecuted 30/120/1,080/5,760/240 active manifest; one forced site-failure NO-GO remains hash-valid and cannot issue. Fifty-one assertions cover both branches and mutation controls. No response, fit, checkpoint, root, or lock is created. Only an exact fresh R0201 record may open replicate 201; full R0201 execution/review remains pending and large simulation stays prohibited. |
| Evidence checklist | `release-evidence-checklist-0.2.3.csv` |
| FACETS stress plan | `facets-jml-stress-plan-0.2.3.md` |
| TAM/immer stress plan | `tam-immer-estimator-stress-plan-0.2.3.md` |
| G-theory reconstruction | `gtheory-reconstruction-roadmap-0.2.3.md`; `gtheory-design-algebra-contract-0.2.3.md`; `gtheory-design-algebra-prototype-0.2.3.R`; `gtheory-design-algebra-record-0.2.3.md`; `gtheory-balanced-estimation-contract-0.2.3.md`; `gtheory-balanced-estimation-prototype-0.2.3.R`; `gtheory-balanced-estimation-record-0.2.3.md`; `gtheory-design-incidence-contract-0.2.3.md`; `gtheory-design-incidence-audit-0.2.3.R`; `gtheory-design-incidence-record-0.2.3.md`; `gtheory-allocation-operator-contract-0.2.3.md`; `gtheory-allocation-operator-prototype-0.2.3.R`; `gtheory-allocation-operator-record-0.2.3.md`; `gtheory-covariance-information-contract-0.2.3.md`; `gtheory-covariance-information-audit-0.2.3.R`; `gtheory-covariance-information-record-0.2.3.md`; `gtheory-glmmtmb-parity-contract-0.2.3.md`; `gtheory-glmmtmb-parity-prototype-0.2.3.R`; `gtheory-glmmtmb-parity-record-0.2.3.md`; `gtheory-ademp-registry-contract-0.2.3.md`; `gtheory-ademp-registry-prototype-0.2.3.R`; `gtheory-ademp-registry-record-0.2.3.md`; `gtheory-ademp-generator-contract-0.2.3.md`; `gtheory-ademp-generator-prototype-0.2.3.R`; `gtheory-ademp-generator-record-0.2.3.md`; `gtheory-ademp-prefit-contract-0.2.3.md`; `gtheory-ademp-prefit-prototype-0.2.3.R`; `gtheory-ademp-prefit-record-0.2.3.md`; `gtheory-ademp-fit-contract-0.2.3.md`; `gtheory-ademp-fit-prototype-0.2.3.R`; `gtheory-ademp-fit-record-0.2.3.md`; `gtheory-multivariate-algebra-contract-0.2.3.md`; `gtheory-multivariate-algebra-prototype-0.2.3.R`; `gtheory-multivariate-algebra-record-0.2.3.md` |
| G-theory weak-information calibration | `gtheory-weak-information-calibration-contract-0.2.3.md`; `gtheory-weak-information-calibration-prototype-0.2.3.R`; `gtheory-weak-information-calibration-record-0.2.3.md`; `test-gtheory-weak-information-calibration-prototype.R` |
| G-theory weak-information pilot plan | `gtheory-weak-information-pilot-contract-0.2.3.md`; `gtheory-weak-information-pilot-prototype-0.2.3.R`; `gtheory-weak-information-pilot-record-0.2.3.md`; `test-gtheory-weak-information-pilot-prototype.R` |
| G-theory stationarity instrumentation | `gtheory-weak-information-glmmtmb-stationarity-instrumentation-contract-0.2.3.md`; `gtheory-weak-information-glmmtmb-stationarity-instrumentation-0.2.3.R`; `gtheory-weak-information-glmmtmb-stationarity-instrumentation-record-0.2.3.md`; `test-gtheory-weak-information-glmmtmb-stationarity-instrumentation.R` |
| G-theory stationarity calibration design | `gtheory-weak-information-glmmtmb-stationarity-calibration-design-contract-0.2.3.md`; `gtheory-weak-information-glmmtmb-stationarity-calibration-design-0.2.3.R`; `gtheory-weak-information-glmmtmb-stationarity-calibration-design-record-0.2.3.md`; `test-gtheory-weak-information-glmmtmb-stationarity-calibration-design.R` |
| G-theory stationarity reference calibration | `gtheory-weak-information-glmmtmb-stationarity-reference-calibration-contract-0.2.3.md`; `gtheory-weak-information-glmmtmb-stationarity-reference-calibration-0.2.3.R`; `gtheory-weak-information-glmmtmb-stationarity-reference-calibration-record-0.2.3.md`; `test-gtheory-weak-information-glmmtmb-stationarity-reference-calibration.R` |
| G-theory stationarity calibration preauthorization audit | `gtheory-weak-information-stationarity-calibration-authorization-audit-contract-0.2.3.md`; `gtheory-weak-information-stationarity-calibration-authorization-audit-0.2.3.R`; `gtheory-weak-information-stationarity-calibration-authorization-audit-record-0.2.3.md`; `test-gtheory-weak-information-stationarity-calibration-authorization-audit.R` |
| G-theory glmmTMB ML reference coverage | `gtheory-weak-information-glmmtmb-ml-reference-coverage-contract-0.2.3.md`; `gtheory-weak-information-glmmtmb-ml-reference-coverage-0.2.3.R`; `gtheory-weak-information-glmmtmb-ml-reference-coverage-record-0.2.3.md`; `test-gtheory-weak-information-glmmtmb-ml-reference-coverage.R` |
| G-theory lme4 objective-reference preflight | `gtheory-weak-information-lme4-objective-reference-preflight-contract-0.2.3.md`; `gtheory-weak-information-lme4-objective-reference-preflight-0.2.3.R`; `gtheory-weak-information-lme4-objective-reference-preflight-record-0.2.3.md`; `test-gtheory-weak-information-lme4-objective-reference-preflight.R` |
| G-theory lme4 ML/REML reference coverage | `gtheory-weak-information-lme4-reference-coverage-contract-0.2.3.md`; `gtheory-weak-information-lme4-reference-coverage-0.2.3.R`; `gtheory-weak-information-lme4-reference-coverage-record-0.2.3.md`; `test-gtheory-weak-information-lme4-reference-coverage.R` |
| G-theory stationarity acceptance policy | `gtheory-weak-information-stationarity-acceptance-policy-contract-0.2.3.md`; `gtheory-weak-information-stationarity-acceptance-policy-0.2.3.R`; `gtheory-weak-information-stationarity-acceptance-policy-record-0.2.3.md`; `test-gtheory-weak-information-stationarity-acceptance-policy.R` |
| G-theory production boundary probe | `gtheory-weak-information-production-boundary-probe-contract-0.2.3.md`; `gtheory-weak-information-production-boundary-probe-0.2.3.R`; `gtheory-weak-information-production-boundary-probe-record-0.2.3.md`; `test-gtheory-weak-information-production-boundary-probe.R` |
| G-theory weak-information inference audit | `gtheory-weak-information-inference-audit-0.2.3.md`; `gtheory-weak-information-diagnostic-refit-prototype-0.2.3.R`; `gtheory-weak-information-diagnostic-refit-record-0.2.3.md`; `test-gtheory-weak-information-diagnostic-refit-prototype.R` |
| G-theory weak-information bootstrap mechanics | `gtheory-weak-information-bootstrap-contract-0.2.3.md`; `gtheory-weak-information-bootstrap-prototype-0.2.3.R`; `gtheory-weak-information-bootstrap-record-0.2.3.md`; `test-gtheory-weak-information-bootstrap-prototype.R` |
| G-theory weak-information feasibility | `gtheory-weak-information-feasibility-contract-0.2.3.md`; `gtheory-weak-information-feasibility-prototype-0.2.3.R`; `gtheory-weak-information-feasibility-record-0.2.3.md`; `test-gtheory-weak-information-feasibility-prototype.R`; `gtheory-weak-information-feasibility-runner-contract-0.2.3.md`; `gtheory-weak-information-feasibility-runner-0.2.3.R`; `gtheory-weak-information-feasibility-execution-record-0.2.3.md`; `test-gtheory-weak-information-feasibility-runner.R`; `gtheory-weak-information-numerical-sensitivity-contract-0.2.3.md`; `gtheory-weak-information-numerical-sensitivity-0.2.3.R`; `gtheory-weak-information-numerical-sensitivity-record-0.2.3.md`; `test-gtheory-weak-information-numerical-sensitivity.R`; `gtheory-weak-information-typed-replay-contract-0.2.3.md`; `gtheory-weak-information-typed-replay-0.2.3.R`; `gtheory-weak-information-typed-replay-record-0.2.3.md`; `test-gtheory-weak-information-typed-replay.R`; `gtheory-weak-information-glmmtmb-stabilization-contract-0.2.3.md`; `gtheory-weak-information-glmmtmb-stabilization-prototype-0.2.3.R`; `gtheory-weak-information-glmmtmb-stabilization-record-0.2.3.md`; `test-gtheory-weak-information-glmmtmb-stabilization.R`; `gtheory-weak-information-glmmtmb-stabilization-runner-contract-0.2.3.md`; `gtheory-weak-information-glmmtmb-stabilization-runner-0.2.3.R`; `gtheory-weak-information-glmmtmb-stabilization-smoke-record-0.2.3.md`; `test-gtheory-weak-information-glmmtmb-stabilization-runner.R`; `gtheory-weak-information-glmmtmb-alignment-contract-0.2.3.md`; `gtheory-weak-information-glmmtmb-alignment-runner-0.2.3.R`; `gtheory-weak-information-glmmtmb-alignment-smoke-record-0.2.3.md`; `test-gtheory-weak-information-glmmtmb-alignment.R`; `gtheory-weak-information-glmmtmb-numerical-adjudication-contract-0.2.3.md`; `gtheory-weak-information-glmmtmb-numerical-adjudication-0.2.3.R`; `gtheory-weak-information-glmmtmb-numerical-adjudication-record-0.2.3.md`; `test-gtheory-weak-information-glmmtmb-numerical-adjudication.R` |
| G-theory stationarity exact-resume runner | `gtheory-weak-information-stationarity-exact-resume-runner-contract-0.2.3.md`; `gtheory-weak-information-stationarity-exact-resume-runner-0.2.3.R`; `gtheory-weak-information-stationarity-exact-resume-runner-record-0.2.3.md`; `test-gtheory-weak-information-stationarity-exact-resume-runner.R` |
| G-theory production-adapter preflight | `gtheory-weak-information-production-adapter-preflight-contract-0.2.3.md`; `gtheory-weak-information-production-adapter-preflight-0.2.3.R`; `gtheory-weak-information-production-adapter-preflight-record-0.2.3.md`; `test-gtheory-weak-information-production-adapter-preflight.R` |
| G-theory one-way authorization preflight | `gtheory-weak-information-one-way-authorization-preflight-contract-0.2.3.md`; `gtheory-weak-information-one-way-authorization-preflight-0.2.3.R`; `gtheory-weak-information-one-way-authorization-preflight-record-0.2.3.md`; `test-gtheory-weak-information-one-way-authorization-preflight.R` |
| G-theory Monte Carlo value audit | `gtheory-weak-information-monte-carlo-value-audit-contract-0.2.3.md`; `gtheory-weak-information-monte-carlo-value-audit-0.2.3.R`; `gtheory-weak-information-monte-carlo-value-audit-record-0.2.3.md`; `test-gtheory-weak-information-monte-carlo-value-audit.R` |
| G-theory pre-activation hardening audit | `gtheory-weak-information-preactivation-hardening-audit-contract-0.2.3.md`; `gtheory-weak-information-preactivation-hardening-audit-0.2.3.R`; `gtheory-weak-information-preactivation-hardening-audit-record-0.2.3.md`; `test-gtheory-weak-information-preactivation-hardening-audit.R` |
| G-theory RNG-hardened generator replay | `gtheory-weak-information-rng-hardened-generator-contract-0.2.3.md`; `gtheory-weak-information-rng-hardened-generator-0.2.3.R`; `gtheory-weak-information-rng-hardened-generator-record-0.2.3.md`; `test-gtheory-weak-information-rng-hardened-generator.R` |
| G-theory hardened adapter rebase | `gtheory-weak-information-hardened-adapter-rebase-contract-0.2.3.md`; `gtheory-weak-information-hardened-adapter-rebase-0.2.3.R`; `gtheory-weak-information-hardened-adapter-rebase-record-0.2.3.md`; `test-gtheory-weak-information-hardened-adapter-rebase.R` |
| M1 review record | `release-gate-m1-review-0.2.3.md` |
| Public direction | Repository-root `ROADMAP.md` |
| Internal sequence | `internal-roadmap-0.2.3.md` |

The Draft.83d2b2b1g15a adjunct preserves the completed Draft.77 evidence from
`tam-immer-jml-factor-stress-0.2.3.R`, governed by
`tam-immer-jml-factor-checkpoint-contract-0.2.3.md` and recorded in
`tam-immer-jml-factor-pilot-record-0.2.3.md`, plus Draft.78
`tam-immer-jml-connected-design-0.2.3.R`,
`tam-immer-jml-connected-design-contract-0.2.3.md`, and
`tam-immer-jml-connected-design-record-0.2.3.md`, plus Draft.79
`tam-immer-jml-topology-calibration-0.2.3.R`,
`tam-immer-jml-topology-calibration-contract-0.2.3.md`, and
`tam-immer-jml-topology-calibration-record-0.2.3.md`. The matched-topology smoke
is an additive structural calibration, not a replacement or post hoc
reinterpretation. Draft.82 remains the additive repository-only balanced
estimator prototype; Draft.83a remains its pre-fit observed-design audit.
Draft.83b remains the planned allocation algebra; Draft.83c1 remains the
covariance/information and lme4 audit. Draft.83c2 adds only the exact-overlap
glmmTMB comparison. Draft.83d1 adds only the pre-simulation registry, metric
routing, paired manifest, and denominator contract and changes none of those
result identities or public APIs. The future Draft.85a0 preflight adds only
supplied-matrix multivariate algebra and likewise changes no fitted result or
public API. Draft.83d2a adds deterministic data identities and audits only.
Draft.83d2b0 adds scalable structural pre-fit identities only. Draft.83d2b1
adds repository-only atomic point-fit evidence and retains a failed negative-
control gate. Draft.83d2b2a adds only a truth-blind diagnostic registry and
one-replicate covering smoke. Draft.83d2b2b0 adds only the replicated phase,
precision, candidate-architecture, and authorization contracts plus a schema
run; it freezes no rule and changes no public API. Draft.83d2b2b1a
prospectively supersedes only its feasibility execution permission, withdraws
the invalid common-coordinate rule families, and adds source-audited raw
full/reduced diagnostics. It does not alter any reserved dataset or public API.
Draft.83d2b2b1b adds only fitted-null simulation/refit and failure-bound
mechanics on already viewed datasets. It does not reauthorize feasibility,
select a production bootstrap method or replication count, or alter any
reserved dataset or public API.
Draft.83d2b2b1c freezes metadata for the untouched feasibility band and adds
runtime/checkpoint evidence from already viewed data. Its separate
authorization record permits only the exact 3,000 descriptive rows; it changes
no public API and does not itself generate any reserved dataset.
Draft.83d2b2b1d executes those exact rows with atomic route and dataset
identities and a no-refit full-resume check. It changes no public API,
threshold, bootstrap method, inferential state, or D-study decision.
Draft.83d2b2b1e re-fits only those already viewed rows under frozen numerical
profiles. It changes no public API or inferential state, selects no optimizer
or practical-equivalence threshold, and keeps calibration data untouched.
Draft.83d2b2b1f then reads only the immutable b1d/b1e ledgers, prospectively
types finite and non-finite replay states, and records zero mismatches. It does
not alter the b1e result, refit any model, or promote numerical sensitivity.
Draft.83d2b2b1g then freezes only the glmmTMB start/restart/derivative design
and exact viewed-route manifest. It implements no runner, authorizes no
stabilization execution, selects no numerical cutoff, and leaves calibration
data untouched.
Draft.83d2b2b1g1 implements the atomic runner and executes only the frozen
120-pair viewed smoke. It retains strict start-snapshot failures, authorizes no
full-manifest run, selects no alignment tolerance, and leaves calibration data
untouched.
Draft.83d2b2b1g2 then applies one prospectively specified, tolerance-free
fixed-coordinate alignment to every returned fit and re-executes the same
120-pair denominator under a new identity. It recovers representation and
dependency transport only; it authorizes no full-manifest run, numerical
eligibility rule, calibration, inference, or public API change.
Draft.83d2b2b1g3 reads only that immutable scalar/hash ledger and replaces no
fit. It separates raw objective from curvature-masked reported likelihood,
exposes optimizer and gradient evidence hidden by precedence, and constructs
labelled best-observed six-profile envelopes without a global-maximum claim.
It authorizes no full run, stationarity rule, calibration, or public change.
Draft.83d2b2b1g4 then replays only the same viewed denominator under a
prospective raw-derivative measurement contract. It retains actual parameter,
gradient, and Richardson vectors/matrices, keeps lme4-compatible and
Newton-type scalings distinct, and separates spectral positivity from
numerical factorability. It changes no fitted result, chooses no cutoff or
optimizer, authorizes no full run, and changes no public API.
Draft.83d2b2b1g5 then constructs only a sealed calibration-design identity.
It separates numerical, curvature, boundary-limit, and statistical-resolution
states, verifies affine coordinate algebra, and maps the untouched reservation
to a prospective workload. It does not freeze reference tolerances, generate
replicate 201, select a stationarity rule, authorize full execution, or change
any public API.
Draft.83d2b2b1g6 then calibrates only those intensive reference mechanics on
analytic objectives and nonreserved replicates 901--902. Its AD-independent
adaptive finite-difference, anchored TMB evaluation, multialgorithm,
curvature, and nuisance-profile gates pass. It does not generate replicate
201, select a production stationarity rule, authorize full execution, or
change any public API.
Draft.83d2b2b1g7 then audits the prerequisites for opening replicate 201 and
fails closed. It corrects only the prospective backend-profile accounting,
freezes truth-blind state and profile-aggregation semantics, and records that
three of four method-specific reference lanes plus the acceptance policy,
boundary probe, and runner remain incomplete. It reads no reserved data,
authorizes no execution, and changes no public API.
Draft.83d2b2b1g8 then reuses the unchanged b1g6 mechanics by exact function
hash on a separately identified glmmTMB ML surface. It executes and repeats
only nonreserved replicates 901--902, advances glmmTMB method coverage, and
leaves lme4 coverage and every calibration/inferential gate false. It changes
no public API.
Draft.83d2b2b1g9 then uses only a deterministic analytic fixture to identify
lme4's theta-only profiled ML and REML objectives and gradients independently.
It source-hashes and excludes two misleading convenience routes. It reads no
nonreserved or reserved response, implements no reference solver or boundary
profile, advances no method-coverage lane, and changes no public API.
Draft.83d2b2b1g10 then implements the likelihood-mode-preserving nonnegative-
theta reference solver and executes only nonreserved replicates 901--902. An
independent sparse Gaussian oracle reduces to the b1g9 dense oracle, and raw
KKT evidence is recovered by bounded analytic-gradient Newton polishing rather
than by redefining stationarity on a curvature-scaled coordinate. Seven-point
nuisance-reoptimized target-theta profiles keep finite interiors and terminate
at the matching reduced objectives. A complete repeat reproduces atomic rows,
sidecar hashes, and execution identity. This closes only the four reference-
mechanics lanes; it freezes no acceptance threshold, makes no first-order
boundary-sufficiency claim, reads no calibration response, authorizes no
production run, and changes no public API.
Draft.83d2b2b1g11 then freezes only how a future calibration may reject and
order its 24 prespecified candidates. It preserves primary-cell denominators,
requires zero observed safety errors and decisive coverage of every required
reference class, retains exact-binomial uncertainty without a post-selection
coverage claim, and specifies a completed negative calibration when no
candidate passes. It binds four nonreserved reference receipts but reads no
reserved response, selects no score family or cutpoint, implements no boundary
probe or runner, authorizes no calibration or confirmation, and changes no
public API.
Draft.83d2b2b1g12 then implements only the lower-cost application boundary
probe. It preserves lme4 theta and glmmTMB log-SD coordinates, reoptimizes
nuisance parameters at every point, and maps a path to the common boundary
route only after monotone material improvement and a separately fitted
reduced-objective match. Flat, nonmonotone, mismatched, and failed paths
remain typed; first-order zero is insufficient. Analytic and four backend-
likelihood fixtures pass, but no reserved response is read, no runner or
threshold is created, no calibration or confirmation is authorized, and no
public API changes.
Draft.83d2b2b1g13 then implements only exact-resume and complete-denominator
runner mechanics. It reconstructs the sealed workload counts, retains all
failed candidate and reference rows, and makes cold, interrupted/resumed,
complete-reuse, and repaired-checkpoint fixture executions scientifically
identical. Its executable manifest is restricted to nonreserved mechanics
replicates 901--902. Production evaluator adapters and a reserved execution
manifest are not frozen; no 201--300 response is generated or read, no
threshold is selected, no calibration or confirmation is authorized, and no
public API changes.
Draft.83d2b2b1g14 then freezes the real four-lane production adapters and a
runtime-, dependency-, output-, unit-, and shard-identified reserved manifest.
The nonreserved dry-run retains complete candidate, decision, reference, and
typed-failure denominators and reproduces exact reuse. All 100 reserved shards
remain non-executable; no 201--300 response is generated or read, no threshold
is selected, no calibration or confirmation is authorized, and no public API
changes. A later independent authorization audit must also verify production
destination permission, capacity, and same-filesystem behavior.
Draft.83d2b2b1g15 then completes that response-free authorization preflight.
One hundred prospective shard manifests exactly partition the sealed workload
but remain non-executable. The frozen output parent passes target-absence,
actual write, checked same-directory rename, identical readback, cleanup, and
capacity checks, while conservative storage/time planning fixes one concurrent
shard and prohibits early stopping. Readiness and activation eligibility are
true, but no authorization record is issued; no 201--300 or 501--700 response
is generated or read, no threshold is selected, no calibration or confirmation
is authorized, and no public API changes. A later immutable activation artifact
must repeat the dynamic site and runtime checks and bind only the exact shard
hashes before replicate 201 can be reconsidered.
Draft.83d2b2b1g15a then audits whether that computation has a proportionate
scientific purpose. It identifies 3,000 independent datasets but only 100
planned trials per primary cell; optimizer profiles, roles, 24 candidates, and
references do not enlarge the Monte Carlo denominator. Complete-denominator
0/100 has one-sided 95% upper 0.029513 and detects at least one true 3% event
with probability 0.952447. The plan is retained only for numerical-rule
calibration and a valid negative result. Reference-unresolved rows reduce the
actual denominator, so achieved precision remains false; broad bias/RMSE/
coverage, D-study operating-characteristic, and universal sample-size claims
remain unavailable. No response is read, no activation state changes, and no
public API changes.

Draft.83d2b2b1g16 then applies stronger pre-activation reproducibility and
single-writer requirements. The exact phase-specific seed ledger is collision-
free, but nonreserved replicate 901 demonstrates that the current generator is
not self-contained with respect to ambient RNG kind. The earlier runtime hash
does not bind the RNG, numerical libraries, locale/timezone, glmmTMB parallel
control, or thread environment. No isolated vanilla process, reserved-only
runner, exclusive lock, activation/resume marker, or per-shard capacity recheck
has yet been implemented. Four gates pass and eight block. This audit therefore
supersedes b1g15 activation eligibility without invalidating its narrower site
and workload checks. No response is read, `LargeSimulationMayStart=FALSE`, and
no public API changes.

Draft.83d2b2b1g17 repairs only the generator component. It preserves the
historical b1g2a function and counterexample, and creates a separately hashed
wrapper that fixes and records all three RNG kinds and restores caller state.
All 30 scenarios at nonreserved replicate 901 reproduce identical hardened,
historical-parent, and analysis-data hashes across two ambient RNG states. A
no-seed path and reserved-band error path also restore caller state. The
wrapper cannot access calibration 201--300 or confirmation 501--700. Five
component gates pass, but `RNG-ADAPTER-01` blocks because production candidate
and reference adapters still bind the historical identity. Therefore
`HardenedGeneratorReady=TRUE` is not an authorization claim:
`AuthorizationRNG01Closed=FALSE`, `LargeSimulationMayStart=FALSE`, and no
public API changes.

Draft.83d2b2b1g18 then rebuilds the nonreserved production preparation,
candidate evaluator, reference evaluator, manifest, checkpoint, and execution
lineage against b1g17 without editing b1g14. Paired historical/hardened
glmmTMB/lme4 x ML/REML execution preserves all numerical, typed-state,
selection, reference-state, and complete-denominator fields. Generator,
pre-fit, reference-sidecar, manifest, checkpoint, and execution hashes change
as intended. All four hardened checkpoints reuse exactly. The reserved entry
point and shard manifest remain deferred, so this is a nonreserved integration
result only: `AuthorizationRNG01Closed=FALSE`,
`LargeSimulationMayStart=FALSE`, and no public API changes.

Draft.83d2b2b1g19 then rebuilds the prospective reserved manifest and all 100
shard manifests without generating responses or fitting models. Workload IDs,
partition, and complete-denominator counts reduce exactly to b1g14. Because the
historical atomic-unit hash did not bind its adapter/generator contract, every
unit and shard receives a new active identity while the old hash is retained
only as explicit provenance. The active/historical scientific registries do
not overlap. Every prospective object remains non-executable and rejects
confirmation use. No reserved entry point, extended runtime contract, or
locked runner is supplied; `AuthorizationRNG01Closed=FALSE`,
`LargeSimulationMayStart=FALSE`, and no public API changes.

Draft.83d2b2b1g20 consolidates the reusable runtime and filesystem boundary
instead of adding more model-specific preauthorization layers. A canonicalized
isolated vanilla child reproduces exact RNG, startup, locale/timezone,
BLAS/LAPACK, package, glmmTMB, and thread identities. Atomic lock contention,
initial activation versus exact resume, unmarked-root rejection, and fresh
filesystem/capacity checks pass in response-free fixtures. Nine common gates
pass. The actual reserved runner and a separate immutable authorization record
remain the only activation blockers, so the kernel is non-executable,
`LargeSimulationMayStart=FALSE`, and no public API changes.

Draft.83d2b2b1g21 connects that boundary to the real hardened scientific
evaluator path without opening reserved data. Nonreserved replicate 902 passes
isolated initial execution, complete-denominator semantic reduction, and exact
resume. Reserved 201 and confirmation 501 are rejected before generation.
`RUNNER-01` is closed, but no immutable authorization record exists;
`AUTH-RECORD-01`, `LargeSimulationMayStart=FALSE`, and the sealed replicate-201
boundary remain.

Draft.83d2b2b1g22 audits that activation claim at exact-source resolution. The
b1g21 preparation guard rejects reserved replicates, and its inherited b1g13
execution loop explicitly permits only the exact nonreserved mechanics run.
Thus the coarse `AUTH-RECORD-01` label decomposes into missing
`RESERVED-ENTRY-01`, `ACTIVE-MANIFEST-01`, and `SITE-RECEIPT-01`
prerequisites. The response-free decision is `no_go_refused_not_issued`.
`AuthorizationRecordIssued=FALSE`, `Replicate201MayBeOpened=FALSE`, and
`LargeSimulationMayStart=FALSE`; the next task is a response-free reserved
entry point and one-shard active-manifest implementation, followed by a
separate fresh-site issuance decision.

Draft.83d2b2b1g23 implements those mechanics without opening a reserved
response. The b1g17 generator contains a third independent reserved stop, so
the exact generator, preparation, and checkpoint bodies are all retained
behind a record-bound ephemeral capability. A nonreserved replicate-902 child
exactly matches b1g21 and exactly resumes. The R0201 conversion is implemented
with complete denominators, but no production issuer, record, active R0201
object, fresh receipt binding, reserved root, or lock exists. Thus
`ReservedEntryPointImplementationReady=TRUE` and
`ActiveManifestConversionImplementationReady=TRUE` do not change
`AuthorizationRecordIssued=FALSE`, `Replicate201MayBeOpened=FALSE`, or
`LargeSimulationMayStart=FALSE`.

Draft.83d2b2b1g24 adds the separate response-free issuer. Fresh isolated-
runtime and target-specific site receipts, exact R0201 scope, entry/conversion
readiness, and confirmation isolation pass six recomputed gates. The observed
GO issues one hash-valid record and unexecuted active manifest; an occupied-
target control yields a hash-valid NO-GO that cannot issue. No response, fit,
checkpoint, root, or lock is created. `Replicate201MayBeOpened=TRUE` is bound
to an exact fresh issuance instance, not a completed run. R0201 execution and
complete-ledger review remain next, and `LargeSimulationMayStart=FALSE`.

Draft.68 retains the primary owner-pilot controls at the package defaults,
`maxit = 400` and 31-point Gaussian--Hermite quadrature. The faster q=5
setting remains smoke-only. A later MML integration-sensitivity lane requires
its own registered execution identity and cannot be pooled with the q=31
primary recovery summaries; therefore q=31 is a prespecified starting basis,
not evidence that integration error is negligible.

The declared score support is also part of evidence identity. Owner-pilot
fits use `keep_original = TRUE`; an observed 1/3/4 pattern declared on a 1--4
scale remains an internal-zero four-category challenge and must not be
renumbered into a contiguous three-category problem.

The historical `ReplayBlockerResolved=true` flag still applies only to the
narrow Draft.62 additive JML recession replay defect. Draft.68 does not extend
that flag to nonlinear GPCM boundary, recovery, integration, or confirmation
work.

This document turns the 0.2.3 roadmap into a testable gate contract. It is a
planning artifact, not evidence that any gate has passed. In particular,
numeric rows marked `pilot_required` must be calibrated on pilot data, changed
to `frozen_numeric`, and reviewed before any confirmatory result is viewed.
Until then, M2 remains incomplete and M3-M5 evidence cannot be generated.

The specification and checklist are repository evidence. `.Rbuildignore`
excludes `inst/validation` from the CRAN source package, so these protocols do
not add CRAN check time or create a public package API.

## Scope and non-scope

0.2.3 strengthens evidence for the existing single-observed-scale,
unidimensional `RSM`, `PCM`, and bounded-`GPCM` contract. It may add small
public fields or guards when needed to prevent an unsupported interpretation.
It does not add:

- native multidimensional latent-trait estimation or dimension scores;
- multiple observed `ScaleId` values or scale-specific PCM;
- threshold/step anchors or frozen-calibration operational scoring;
- unrestricted GPCM, posterior-predictive checks, MCMC, or multivariate
  G-theory;
- native CML/CCML, a new JML bias-correction option, or a hierarchical rater or
  other latent local-dependence model family; or
- a package/runtime dependency on ConQuest, FACETS, TAM, or immer. Locally
  executed synthetic external validation remains release evidence outside
  CRAN.

The public estimator labels remain `MML` and `JML`. `JMLE` is an input alias
for `JML`, not a separate estimator. Person estimates from MML and JML are not
assumed equal and are not compared without a named common estimand.

### GPCM model and evidence identity

The implemented bounded route is an **aligned single-owner relative-slope
GPCM**. Exactly one selected facet owns both the positive slope block and its
step ladder, so `slope_facet == step_facet`. The fitted adjacent-category
kernel uses geometric-mean-one relative slopes and one latent dimension. This
does not implement simultaneous criterion and rater slopes, different slope
and step owners, a rater-by-item slope, or a multidimensional latent trait.

`gpcm-model-identity-contract-0.2.3.csv` is the hashed Draft.63/Draft.66 family
map retained for historical execution identity. P1r/P1s provide the
prospective current-default overlay; the older map must not be silently
rewritten and then presented as the contract used by the sealed pilot.
Every generated GPCM result, replay record, external normalizer, and retained
aggregate must add the following fields to the common result schema:

`SlopeOwner`, `StepOwner`, `SlopeComposition`, `LatentDimensionCount`,
`AbilityScaleContract`, and `RuntimeIdentity`.

The first four fields are part of the estimand identity. `RuntimeIdentity`
must resolve to the loaded source or installed package content actually used;
an ad hoc test that silently dispatches to another installed `mfrmr` cannot
become release evidence. The JML and MML rows remain separate because jointly
estimated Person coordinates and the current estimated intercept-only normal
population location/scale do not give the relative-slope constraint the same
inferential meaning. The historical fixed-standard-normal MML branch remains
an explicit legacy stratum and cannot be pooled with the current default.

`NUM-GPCM-BOUND` is retained only so earlier draft records remain interpretable.
From Draft.63 onward it is a calibration umbrella, not a claim-promotion row.
A criterion-owned result cannot satisfy `NUM-GPCM-ALIGN-RATER`, and a pooled
result lacking the four identity fields satisfies neither owner-specific gate.

## Gate states and evidence roles

### Release decisions

| Checklist value | Meaning |
| --- | --- |
| `blocker_if_failed` | A failure or unresolved result is a 0.2.3 No-Go. |
| `caveat_if_incomplete` | It may ship only when the limitation is unavoidable in the affected first-screen output and documentation. |
| `roadmap_if_missing` | It must remain guarded and outside the advertised 0.2.3 scope. |

### Evidence status

Every result row uses one of:

- `not_run`: no candidate-linked result exists;
- `ok`: the frozen acceptance rule passed;
- `concern`: a blocking requirement failed or could not be established;
- `review`: a prespecified caveat requires human classification;
- `not_applicable`: the frozen scenario registry proves the row is outside the
  candidate's claimed scope.

`review` is not a synonym for pass. A blocker row cannot finish as `review`.

### Evidence roles

| Role | Permitted use |
| --- | --- |
| `unit` | Exact formula, schema, reduction, and fail-closed regression checks. |
| `pilot` | Criterion calibration and feasibility only; never release confirmation. |
| `confirmation` | Locked release decision on untouched simulation seeds, Persons, or external data. |
| `sensitivity` | Robustness context that cannot hide a failed confirmation row. |
| `external` | Matched result produced by a named external engine and normalized under a recorded contract. |
| `engineering` | Candidate-linked build, check, timing, CI, and artifact evidence. |

Pilot and confirmation rows must have disjoint seed registries and, for an
empirical dimensionality challenge, disjoint Person partitions or a clearly
labelled same-sample sensitivity status.

### Criterion states

| State | Meaning |
| --- | --- |
| `frozen_structural` | Exact schema, identity, formula, prohibition, or fail-closed rule that does not require pilot calibration. |
| `pilot_required` | The metric or design is named, but its numeric value or grid must be calibrated before confirmation. |
| `frozen_numeric` | Pilot calibration is complete and the numeric rule is frozen for confirmation. |
| `candidate_required` | The rule is defined but can be evaluated only after a candidate or external result exists. |
| `roadmap_guard` | The route remains unavailable or deferred in 0.2.3. |

## Freeze and invalidation rules

M2 may freeze the gate only when all of the following are true:

1. every blocker row has a complete scenario definition, metric, direction,
   threshold, aggregation rule, and missing/failed-replicate policy;
2. no blocker row remains `pilot_required`, contains `TBD`, or relies on an
   unstored software default;
3. candidate Q matrices, model constraints, partitions, seed registries,
   integration ladders, and consequence criteria have stable identifiers;
4. the specification and checklist hashes are recorded and reviewed; and
5. no confirmatory result has been inspected under the proposed thresholds.

Pilot findings may revise the current draft, but each revision increments the
specification ID and records the reason. The first confirmation-eligible
version is named `0.2.3-frozen.1`. Any later change to a blocker scenario,
metric, tolerance, Q matrix, partition, seed, integration policy, or
aggregation rule invalidates all M3-M5 evidence and requires a new frozen
version.

## Candidate and evidence identity

### Candidate manifest

The M3 manifest must record at least:

| Field | Requirement |
| --- | --- |
| Package identity | Version, source commit, tree hash, tarball SHA-256, and selected check-log SHA-256. |
| Gate identity | Specification ID/hash and checklist hash. |
| Runtime identity | R version, platform, dependency versions, compiler, and relevant environment flags. |
| Data identity | Scenario ID, input hash, category map, missingness map, weight policy, and Person partition hash. |
| Model identity | Family, estimator, facets, interactions, anchors/constraints, free-parameter map, and starting-value hash. |
| Integration identity | Engine, nodes, QMC setting, sequence/hash, seed where operative, and evaluation policy. |
| External identity | Program, version/edition, run date, command/input/output hashes, and normalization version. |
| Random identity | Generator seed registry, bootstrap seed registry, and failed-replicate policy. |

The release-readiness reader expects
`release-candidate-manifest-0.2.3.csv` as a two-column `Field`,`Value` table
with exactly one non-empty row for each of `CandidateId`, `PackageVersion`,
`SourceCommit`, `SourceTreeHash`, `TarballSHA256`, `SpecificationId`,
`CheckLogSHA256`, `SpecificationSHA256`, `ChecklistSHA256`, `RVersion`, `Platform`,
`DependencyIdentity`, `Compiler`, `EnvironmentFlags`,
`DataRegistryIdentity`, `ModelRegistryIdentity`, `IntegrationRegistryIdentity`,
`ExternalRegistryIdentity`, and `SeedRegistryIdentity`. SHA-256 and Git object
fields must use lowercase or uppercase hexadecimal without a prefix. A
registry may state `not_applicable` only when the frozen scenario registry
proves that it is outside the candidate scope.

Beginning with 0.2.3, the current README, current NEWS section, and
`cran-comments.md` must not preserve numeric test/check pass counts as
free-standing prose. Engineering status is reconstructed from candidate-linked
logs and hashes; if a human-readable count is needed, it is regenerated from
that exact evidence for the release rather than copied from a previous run.
Historical NEWS sections are immutable and are not reclassified by this rule.

No local absolute path, user name, identifier-bearing case data, license key,
or proprietary binary is retained in repository evidence.

### Result-row schema

Every generated gate result must contain:

`Gate`, `Item`, `ScenarioId`, `CandidateCommit`, `SpecId`, `EvidenceRole`,
`Metric`, `Estimate`, `Threshold`, `Direction`, `MonteCarloSE`, `NumericalSE`,
`ReplicatesPlanned`, `ReplicatesRetained`, `FailedReplicates`, `Status`,
`EvidencePath`, and `EvidenceHash`.

For an exact structural check, `Estimate`, `MonteCarloSE`, and `NumericalSE`
may be empty, but `Status`, the exact rule, and evidence identity may not be.
Aggregates must link to retained per-replicate evidence outside the package.

The release-readiness reader expects these rows in
`release-gate-results-0.2.3.csv`. `CandidateCommit` and `SpecId` must match the
candidate manifest. Each `EvidencePath` is a repository-relative path without
parent traversal and its `EvidenceHash` must match the retained file's
SHA-256. Every checklist `Gate`/`Item` and every named semicolon-separated
`ScenarioId` must resolve to at least one result row; `ALL` is the only
wildcard. Blocker and roadmap-guard items finish only when every matched row
is `ok`. A caveat item may finish as `review` only when no matched row is
`concern`, `not_run`, or missing; that review state propagates to the overall
readiness decision.

## Scenario registry

The final frozen registry may split a row into more cells, but may not omit the
following scenario classes.

| Scenario ID | Evidence role | Required design |
| --- | --- | --- |
| `NUM-BIN-REDUCE` | unit/confirmation | Two-category RSM and PCM reduction to the intended binary model. |
| `NUM-RSM-CORE` | pilot/confirmation | Connected unidimensional RSM across planned Person counts and facet sizes. |
| `NUM-PCM-CORE` | pilot/confirmation | Connected PCM with unequal supported step ladders in the current rectangular contract. |
| `NUM-GPCM-BOUND` | pilot | Legacy bounded-GPCM calibration umbrella. It preserves earlier draft traceability but cannot promote an owner-specific claim after Draft.63. |
| `NUM-GPCM-ALIGN-CRITERION` | unit/pilot/confirmation | Aligned criterion-owned relative slopes and criterion-owned steps across unit, near-flat, moderate, and boundary-adjacent regimes, with the full model-identity fields retained. |
| `NUM-GPCM-ALIGN-RATER` | unit/pilot/confirmation | Aligned rater-owned relative slopes and rater-owned steps, including separate severity, slope, and step recovery under workload, ability-range, link, and category-support variation. |
| `NUM-ENGINE-PARITY` | pilot/confirmation | Direct, hybrid, and EM-plus-polish evaluated at the same retained parameter vector and quadrature. |
| `REC-SMALL-CORE` | pilot/confirmation | Small supported designs used to calibrate finite-sample recovery limits. |
| `REC-STANDARD-CORE` | pilot/confirmation | Typical connected designs for every claimed parameter class. |
| `DES-SPARSE-LINKED` | pilot/confirmation | Sparse but connected rater assignment with explicit common-Person links. |
| `DES-WEAK-BRIDGE` | pilot/confirmation | Weak links, bridge levels, and articulation cases. |
| `DES-TWO-RATER` | pilot/confirmation | Two-rater complete and sparse panels, stratified by shared-Person support and local information. |
| `DES-RATER-NO-COMMON-PERSON` | unit/confirmation | Two-rater negative control with no common Persons; unsupported comparisons must fail closed. |
| `DES-DISCONNECTED` | unit/confirmation | Deliberately disconnected negative control that must fail closed. |
| `DES-EMPTY-CATEGORY` | unit/confirmation | Empty or near-empty category support with a prespecified support classification. |
| `DES-CATEGORY-IMBALANCE` | pilot/confirmation | Middle/single-category dominance, skewed targeting, concentration, entropy, and local support by model. |
| `DES-GPCM-RATER-CATEGORY` | pilot/confirmation | Rater-owned slope/step attribution crossed with category use, workload, ability targeting/range, common-Person links, weak bridges, and empty or rare within-rater cells. |
| `DES-EXTREME-SCORE` | pilot/confirmation | Extreme Person/facet score patterns separated from optimizer status. |
| `INT-JML-EXT-PROFILE` | unit/pilot | Independently free extreme-Person profile-limit objective, finite original-JML cap path, raw/profile structural separation and paired structural recovery, plus anchored/constraint-coupled negative controls. Person recovery and profile uncertainty are ineligible; it is not a bias-corrected method mode. |
| `IC-FORMULA` | unit | Exact AIC, Person-BIC, and Sclove-SABIC formulas. |
| `IC-FREE-DIMENSION` | unit | Constraint-aware parameter count under centering, anchors, steps, slopes, interactions, and latent regression. |
| `IC-WEIGHT-POLICY` | unit | Unit weights, constant-within-Person frequency weights, and row-varying weights. |
| `IC-INTEGRATION` | pilot/confirmation | Common-evaluation quadrature/QMC ladder for delta-criterion stability. |
| `DIM-SYN-TRUE-1D` | pilot/confirmation | True 1D false-selection control across targeting and sparse-design cells. |
| `DIM-SYN-TRUE-2D` | pilot/confirmation | Prespecified 2D alternatives spanning moderate and high dimension correlations. |
| `DIM-SYN-INTERACTION` | pilot/confirmation | Rater-by-criterion interaction without latent multidimensionality. |
| `DIM-SYN-CONFOUNDED` | pilot/confirmation | Deliberately inseparable interaction/dimension negative control. |
| `DIM-GPCM-SLOPE-ABSORB` | sensitivity | Generator-only challenge separating true 1D discrimination from multidimensionality, location interaction, and local dependence that a free slope may absorb. It produces no native dimension score. |
| `DIAG-BIAS-NULL` | pilot/confirmation | Additive null grid for conditional bias-screen false-positive and multiplicity behavior. |
| `DIAG-BIAS-NONNULL` | pilot/confirmation | Planted zero-marginal facet interactions across effect size and cell information. |
| `DIAG-PCAR-NULL` | pilot/confirmation | Prespecified residual-PCA null grid across topology and missingness. |
| `DIAG-PCAR-LOCAL` | pilot/confirmation | Planted local response dependence with exploratory PCAR sensitivity and attribution controls. |
| `DIAG-GPCM-FIT-NULL` | pilot/confirmation | Design-stratified null operating characteristics for GPCM residual and infit/outfit-like summaries; no universal cutoff is presumed. |
| `DIAG-GPCM-FIT-NONNULL` | pilot/confirmation | Planted slope, step, interaction, multidimensional, and local-dependence alternatives with attribution rather than detection-only reporting. |
| `DFF-GPCM-UNIFORM` | pilot/confirmation | Location-like group/rater severity shifts under exact refit identity, null Type-I error, multiplicity, imbalance, sparse links, and practical-consequence reporting. |
| `DFF-GPCM-NONUNIFORM-NEG` | unit/pilot/confirmation | Group-specific slope or threshold-weight negative control. A current location-only DFF screen must not receive credit for fitting an unimplemented nonuniform estimand. |
| `DIM-EMP-DISCOVERY` | pilot | Residual PCAR/Q3-style hypothesis generation on discovery Persons only. |
| `DIM-EMP-CONFIRM` | confirmation | Frozen TAM alternatives on untouched Persons or an external sample. |
| `EXT-CQ-BINARY` | external | Matched unidimensional ConQuest binary MML core. |
| `EXT-CQ-RSM` | external | Matched unidimensional ConQuest RSM MML core. |
| `EXT-CQ-PCM` | external | Matched unidimensional ConQuest PCM MML core. |
| `EXT-TAM-MML-1D` | external | Matched TAM unidimensional MML reference, separate from its dimensionality and JML lanes. |
| `EXT-FACETS-QUALIFY` | external | Deterministic binary, RSM, and PCM microcases binding executable metadata/hash to report-header version and exact artifacts. |
| `EXT-FACETS-RSM-CORE` | external | Paired JML RSM truth recovery and transformed parameter agreement over the mandatory connected core. |
| `EXT-FACETS-PCM-CORE` | external | Paired JML PCM truth recovery and transformed parameter agreement over the mandatory connected core. |
| `EXT-FACETS-ANCHOR` | external | Element/group anchor and scale-origin cases within current mfrmr support; threshold anchors remain excluded. |
| `EXT-FACETS-SPARSE` | external | Sparse, weak-link, bridge, and disconnected topology classification and false-ready checks. |
| `EXT-FACETS-EDGE` | external | Extreme score, category support, missingness, and failure-classification cases. |
| `EXT-FACETS-DFF` | external | Optional definition-matched fit/DFF null and non-null rows; never part of core merely because FACETS emits them. |
| `EXT-TAM-JML-RAW` | external | TAM `adj = 0, bias = FALSE`, eligible as original unadjusted JML only without extreme Person scores; failed/nonfinite extreme rows remain in the denominator. |
| `EXT-TAM-JML-ADJ` | external | TAM documented `adj = .3, bias = FALSE`; adjusted extreme Person values enter structural estimation. |
| `EXT-TAM-JML-BC` | external | TAM `adj = 0, bias = TRUE`; the raw item basis is postscaled by `(I - 1) / I` and remains eligible only without extreme Persons. |
| `EXT-TAM-JML-BC-ADJ` | external | TAM `adj = .3` structural estimation followed by `(I - 1) / I` postscaling, never pooled with either factor alone. |
| `EXT-IMMER-JML-RAW` | external | immer `est_method = "jml", eps = .3`; original unadjusted JML only without extremes, because extreme Person scores receive epsilon handling. |
| `EXT-IMMER-JML-EPS` | external | immer `est_method = "eps_adj", eps = .3`; a distinct fuzzy Person-and-item sufficient-statistic equation, not merely an extreme display. |
| `ALT-RATER-STYLE` | sensitivity | Centrality/extremity generator with severity, threshold-spacing/weight, and GPCM-slope attribution kept distinct. |
| `ALT-RATER-BUNDLE-LD` | sensitivity | Repeated-rating or latent-rating dependence generator used to challenge fit, Q3/PCAR, slope, and DFF attribution without engine-equivalence claims. |
| `FUT-GPCM-DECOUPLED-OWNER` | unit/roadmap | Public-scope guard proving that current outputs do not claim a slope owner different from the step owner. |
| `FUT-GPCM-MULTIPLICATIVE` | unit/roadmap | Public-scope guard proving that current outputs do not claim simultaneous multiplicative criterion/task and rater slope blocks. |
| `FUT-GPCM-SEVERITY-INTERACTION` | unit/roadmap | Public-scope guard separating rater-by-item location interaction and item-specific steps from a cell-specific slope. |
| `FUT-GPCM-MULTIDIMENSIONAL` | unit/roadmap | Public-scope guard proving that the current unidimensional route does not produce native multidimensional scores or generalized multidimensional MFRM claims. |
| `EXT-IMMER-JML-BC` | external | immer `est_method = "jml_bc", eps = .3`; `jml` extreme handling followed by item-basis postscaling by `(Ibar - 1) / Ibar`. |
| `EXT-IMMER-CML` | external | immer CML structural-parameter reference on eligible Rasch-family rows; no Person or bounded-GPCM claim. |
| `EXT-IMMER-CCML` | external | immer CCML structural-parameter reference on eligible Rasch-family rows; no Person or bounded-GPCM claim. |
| `ALT-IMMER-HRM-LD` | sensitivity | HRM-generated latent-rating/local-dependence challenge to additive mfrmr diagnostics, never an engine-equivalence row. |
| `ENG-CRAN-SMOKE` | engineering | Deterministic CRAN-side package workload. |
| `ENG-FULL-CORE` | engineering | Complete non-CRAN blocker suite and cross-platform matrix. |

The frozen registry must give every simulation cell an ADEMP record: aim,
data-generating mechanism, estimand, fitted method, and performance measures.
Sample sizes, level counts, assignment densities, category probabilities,
effect sizes, and replication counts remain `pilot_required` in this draft.

## G0: candidate identity

Candidate identity is exact, not tolerance based.

- Every internal and external evidence row must resolve to one manifest.
- Source commit, installed-package source, tarball, and check-log package
  version must agree.
- The gate specification, checklist, Q matrices, Person partitions, scenario
  registry, seed registry, and integration registry must match their hashes.
- An external result without executable hash/file metadata, output-reported
  version, command/control/input/output hashes, date, locale, parser/generator
  and normalizer fingerprints is unresolved for release comparison. An
  executable/report version difference is retained as a separate evidence
  stratum and does not stop unrelated pilot execution.
- A stale result from another candidate is a blocker even when its numerical
  values would pass.

## G1: MML stationarity and numerical agreement

### Canonical score

The gate operates on the identified free parameter vector after equality
constraints. The current bounded-scope GPCM implementation is not optimized
under box bounds: `n-1` free log slopes are expanded to `n` sum-zero log
slopes, then exponentiated to positive slopes with geometric mean one. Its G1
rule therefore uses the transformed free-coordinate score and stores both the
log-slope and positive-slope Jacobians; it does not call this a projected
gradient. If a future implementation introduces a genuine inequality or box
constraint, that route requires a separately declared projected-gradient/KKT
rule before entering this gate. The analytical score is checked against an
independently implemented central-difference or automatic-differentiation
reference on small binary, RSM, PCM, and bounded-GPCM fixtures.

The draft.12 fixed pilot uses q=31, two fixed hashed fixtures, a relative-step
ladder of `1e-4`, `3e-5`, and `1e-5`, and both retained-solution and
deterministic nonzero-score points. All ten run/point references were
computable; the maximum absolute/scaled score difference was `6.91e-9`, and
the largest numeric step-ladder range was `6.91e-8`. The GPCM log/slope
Jacobian check reached a maximum difference of `3.00e-10`. Exact binary
RSM=PCM and unit-slope GPCM=PCM log-probability, probability, objective, and
common-score reductions also held. These values are recorded in
`numerical-stationarity-pilot-record-0.2.3.md` as pilot calibration and
structural-regression evidence only.

The current package review tolerance
`max(1e-4, 10 * reltol)` is a pilot baseline, not the frozen 0.2.3 release
criterion. Pilot work must determine whether a scaled score is also required
when parameter classes have materially different curvature.

### Retained solution

- Direct, hybrid, and EM-plus-polish routes are compared at a common retained
  parameter vector, likelihood implementation, quadrature, and constraints.
- Optimizer code zero and EM relative change are secondary evidence. Neither
  can override a failed canonical score check.
- `maxit` is a prespecified ceiling. Iteration-limited fits remain review-only
  and cannot pass a blocker row through repeated ad hoc reruns.
- Objective and transformed parameter differences use pilot-calibrated
  absolute and relative tolerances by parameter class.
- Binary reduction, unit-slope reduction, and step/slope transformation checks
  are exact regression blockers.

The draft.13 fixed pilot applies this contract to binary and four-category
additive RSM/PCM fits at q=31. It records four paths per run: public direct,
public hybrid, converged raw EM as a diagnostic, and common direct polish
started from the exact hashed raw-EM vector. The mandatory parity set is
direct, hybrid, and EM-plus-common-direct-polish; raw EM readiness or native
relative-likelihood convergence cannot satisfy the row by itself.

Each retained path vector is evaluated through all direct, EM, and hybrid fit
contexts after their quadrature, identification, coordinate order, objective,
and score structures are shown to be identical. Across all 16 fixed run/path
summaries, both the objective evaluator range and the coordinate-wise score
evaluator range were exactly zero. Across the 12 mandatory path pairs, the
largest objective difference was `1.47e-10`, the largest free or expanded
parameter difference was `5.73e-6`, and the largest mandatory-path common
score was `5.06e-5`. All mandatory paths happened to be inference-ready and no
path emitted a warning. These are pilot observations only:
`NUM-OBJECTIVE-TOL` and `NUM-PARAMETER-TOL` remain `pilot_required` until the
grid is expanded and a reviewed margin rule is frozen.

Engine parity currently applies only to additive equal-slope RSM/PCM. GPCM has
one supported direct engine; requesting EM or hybrid falls back to direct and
cannot count as independent parity evidence. Model-estimated interactions and
latent regression likewise fall back to direct for unsupported engine
requests. The fixed scope registry and fail-closed tests preserve this
boundary. Full details are recorded in
`mml-engine-parity-pilot-record-0.2.3.md`.

## G2: recovery and sparse-design behavior

Core recovery is evaluated separately for every claimed parameter class and
scenario cell. The minimum record is bias, RMSE, interval coverage where the
interval is supported, standard-error availability, run success,
inference-ready rate, Monte Carlo standard error, terminal score, objective,
condition indicator, and elapsed time.

Readiness has three scopes: fit, parameter, and comparison. The stored fit
record retains input, estimability, category, boundary, and numerical component
states; parameter output retains one status per displayed/fixed coordinate;
external normalization retains eligibility per metric and parameter class.
The legacy scalar `InferenceReady` is only a conservative compatibility
summary and cannot be the sole input to a 0.2.3 gate.

Draft.47 continues to bind this gate to readiness contract
`mfrmr-readiness-0.2.3-v3` in
`readiness-contract-0.2.3.md`, its repository-only schema validator, and the
36-row adversarial fixture registry. `InferenceReady` is `TRUE` only for fit
state `ready`; every other fit state maps to `FALSE`. A
`ready_with_exclusions` fit retains separately estimable parameter rows, but
old Boolean-only consumers cannot call the whole fit ready. Saved objects
without the contract are `legacy_unknown`, regardless of their former scalar.
This structural freeze completes WP0 only. It does not make current runtime
surfaces compliant and does not satisfy the propagation or confirmation gate.

Draft.43 also binds Person-indexed MML posterior alignment and validation
capability to this gate. Any MML Person, expected-score, residual, fit, bias,
or residual-PCA evidence produced before commit `655f6bf` is rejected unless
the retained Person order is independently proven aligned. Row permutation
must leave EAP and posterior SD values invariant by Person ID. Dependency-
limited JML boundary runs remain valid fail-closed capability observations but
cannot substitute for an evaluated boundary audit. The two-replicate
feasibility run freezes no operating-characteristic or diagnostic threshold.

Draft.44 binds long-run reuse to checkpoint schema
`mfrmr-gpcm-repilot-checkpoint-v1`. Only a complete four-route data cell can
be reused. Selected/declared manifests, execution controls, loaded-package
bytes, validation-runner bytes, capability versions/content or absence, R/platform,
and RNG contract enter execution identity; absolute paths do not. Resume is
explicit and rejects unexpected RDS files or any schema, execution, cell,
payload, scenario, route, and manifest mismatch. A same-directory verified
temporary payload is published by rename, and a final completion marker is
valid only while every listed aggregate and default checkpoint hash agrees.
Historical draft.43 outputs predate this schema and remain non-resumable. This
closes no statistical recovery, coverage, diagnostic, or comparison gate.

Draft.45 adds the repository-only MML metamorphic contract before any new
external numeric comparison is eligible. Ten transformations are crossed with
RSM, PCM, and bounded GPCM under one retained-data design. A comparison passes
only when semantic key sets agree, all declared metric differences are within
pilot screening tolerances, model-result states agree, and both fits have
`NumericalState = ready`. Missing and zero-weight encodings may retain distinct
input-provenance/readiness audit fields, but they may not change the effective
rows or downstream fit quantities. The 30/30 v3 pass is software-property
pilot evidence only. It does not freeze the tolerances or replace sparse
target-size, recovery, coverage, diagnostic, or external-comparison evidence.

Draft.46 changes release-scope governance without changing an evidence result.
The then-current 87-row Draft.46 checklist established that the checklist is an
inventory and cannot be used as a count of equally mandatory serial tasks.
Each `(Gate, Item)` must be assigned to one of
three portfolios before criterion freeze: `release_spine` (must pass for the
candidate), `claim_conditional` (must pass only to promote its named claim;
otherwise the claim is disabled, unavoidably caveated, or deferred), or
`deferred` (outside bounded 0.2.3 scope). Every conditional row must name its
fail-closed fallback and affected public surfaces. No row is deleted, passed,
or converted to confirmation evidence by portfolio assignment.

WP5 deterministic eligibility work may begin for stable RSM/PCM metric slices
without borrowing incomplete GPCM or diagnostic readiness. WP6 target-size
construction/runtime evidence may proceed without waiting for external
normalization. WP7 may prespecify replication, Monte Carlo precision, seeds,
and manifests, and may run calibration pilots only on dependency-stable
slices. A later affected code or contract change invalidates those rows.
Confirmation remains globally prohibited until the portfolio profile,
release-spine dependencies, numeric criteria, and candidate identity are
frozen. The governing rationale and rechecked official version sources are in
`roadmap-reassessment-record-0.2.3.md`.

Draft.47 begins the noninferential target-size slice authorized by that
governance decision. The six selected rows are exactly the executable
`target_sparse` rows from the already-hashed 70-cell pilot manifest; no cell
was added or removed after viewing a result. Each has 400 generated Persons
and one executed seed, while the later pilot remains explicitly declared as
five replicates per cell. All six executed with zero unexpected runner failure
and zero false-ready state. Two disconnected exact-rank controls failed before
optimization, two returned blocked/review states, one PCM JML fit retained
extreme-Person exclusions, and one imbalanced/missing PCM MML fit was ready.
The ready fit is not a recovery or diagnostic pass. The mixed-adversity GPCM
MML fit reached the fixed iteration limit and remained blocked; its optimizer
slope error is ineligible as primary recovery evidence.

Four returned fits reached exploratory residual PCA. In the most complex GPCM
cell, `psych` reported that the smoothed-correlation determinant and parts of
the objective were undefined even though an object returned. Diagnostic
promotion therefore requires a new fail-closed PCA computability state that
captures messages, matrix rank/support, and smoothing/repair provenance. The
run's R `gc()` high-water values are memory proxies, not OS peak resident
memory or a supported-size ceiling. The authoritative independently validated
bundle and all hashes are recorded in
`target-scale-sparse-stress-pilot-record-0.2.3.md`. Balanced target baselines,
weak-bridge gradients, OS memory, the five-replicate pilot, recovery/coverage,
and every numeric criterion remain open.

Draft.48 executes those target baselines and weak-bridge gradients as a new
one-replicate calibration layer. Complete balanced and clean matched-sparse
RSM, PCM, and GPCM data are each fitted by JML and MML without changing the
generated data. The two-Rater PCM bridge cells use 0, 1, 2, 5, 10, 20, and 40
common Persons under one truth hash. The authoritative v2 completes all 26
routes, preserves all 13 paired data hashes, has zero unexpected failures and
zero false-ready rows, and records one expected exact zero-overlap JML failure.
The v1 bridge rows are superseded because different overlap levels used
different truth seeds.

All clean 12-Rater/12-Criterion JML cells reached the iteration limit after
roughly 204--480 seconds. Their MML partners completed in roughly 2.5--17
seconds but remained review-only because of terminal-gradient and, for GPCM,
marginal-boundary contract gaps. This separates a JML scale/design-dimension
bottleneck from MML numerical-readiness work; it does not justify loosening a
tolerance. At positive bridge overlap, MML was ready while JML alternated
between extreme-Person exclusions and iteration-limited blocking. The recovery
traces were nonmonotone. Neither binary connectivity nor numerical readiness
therefore freezes minimum overlap. Replicated common-truth recovery, local
information, failure denominators, MCSE, and estimator-specific criteria are
required. Process-lifetime OS peak memory is recorded but is not per-cell
allocation or a capacity limit. PCA remains unrun pending its computability
state. Results and hashes are in
`target-scale-baseline-bridge-pilot-record-0.2.3.md`.

Draft.49 decomposes the draft.48 JML computation hypothesis with 14 PCM data
cells and 34 routes. Person/row growth, fixed-row Rater panel/topology,
fixed-row Criterion/step growth, fixed-parameter row exposure, and a matched
forced-extreme control are fitted on identical JML/MML inputs. Selected JML
cells add explicit optimizer controls. All routes executed with zero unexpected
failure and zero false-ready result under the fixed 60-iteration calibration
ceiling.

The P200 and P400 complete nonextreme auto routes switched to L-BFGS-B and
blocked, whereas explicit BFGS on the same data was ready in similar time;
P200 explicit L-BFGS-B reproduced auto. This places a replicated optimizer-
dispatch grid around the current 200-parameter threshold on the corrective
path. It does not authorize a threshold change: BFGS remained blocked for the
R12 panel, C12 step panel, and forced-extreme controls. Fixed-row Rater results
also changed zero-common-Person pair topology, so they are not a pure Rater-
dimension experiment.

At fixed 249-parameter dimension, the 1,200-row sparse cell was slower and less
well conditioned than 2,400 rows, while the 7,200-row cell was the slowest but
ready. Forcing 20 low and 20 high Persons under the P200 truth approximately
doubled JML time and blocked both optimizers. The largest single-axis fit was
about 51 seconds, leaving the hundreds-of-seconds draft.48 result as a compound
interaction rather than a single row/dimension effect. Phase-specific timing
now rejects the optimizer as the primary elapsed-time explanation in these
cells: structural and joint recession certification dominate, while the
optimizer accounts for only a small JML share. Draft.51 implements the
mathematically equivalent structural global-cone prescreen before target
enumeration without bypassing certificate or fail-closed states. The fixed v4
rerun preserves all 19 semantic/readiness states and all 12 same-fit structural
target classifications. In the selected negative-cone PCM routes, 908 legacy
target LP calls become 12 cone LP calls and structural phase time falls from
139.63 to 13.80 seconds. Joint recession attribution now precedes any further
performance implementation. The full records are
`jml-bottleneck-decomposition-pilot-record-0.2.3.md`,
`jml-phase-profile-pilot-record-0.2.3.md`, and
`jml-structural-cone-prescreen-pilot-record-0.2.3.md`.

Draft.23 implements the RSM/PCM linear-coordinate portion of the estimability
gate. Sparse QR acts on the optimizer's constrained Person/facet/interaction/
step basis; exact deficiency stops before optimization with a structured
condition. MML is audited without free Person columns and is compared with a
counterfactual free-Person JML design so assumption-only panel linkage remains
explicit. Dense SVD is restricted to bounded explanation fixtures. This does
not complete G1/G2: nonlinear GPCM slope and latent-variance coordinates,
fitted information, weak-information calibration, target-size performance,
and the broader property grid remain unresolved.

Draft.25 adds only the first fitted-information instrument. For stationary
retained fits with nonlinear free coordinates and total free dimension at most
80, the runtime stores a symmetric-eigenvalue tolerance ladder from a dense
numerical Hessian of the same direct negative log-likelihood and analytical
gradient used in fitting, with an explicit free-coordinate step, objective
reevaluation/difference, gradient maximum, Hessian asymmetry, and a nonlinear-
block diagonal summary. It records
explicit nonstationary, malformed-vector, dimension-limit, and unavailable
states. The dimension cap is computational, not inferential. This result cannot
create `weak_information`, change readiness, certify nonlinear structural
identification, or satisfy G1/G2 before its pilot-calibrated rule is frozen.

Draft.26 adds a distinct nonlinear parameterization audit at the retained
vector. Analytic GPCM free-log to sum-zero-log to positive-slope Jacobians and
latent free-log-variance to positive-variance Jacobians are checked against
coordinate-scaled central differences. Dimensions, expected and tolerance-
ladder ranks, constraint residuals, derivative differences, natural-coordinate
ranges, and conditioning are retained. This is explicitly not a response-
likelihood/design Jacobian: transformation full rank cannot classify structural
identification, weak information, readiness, or external eligibility.

Draft.27 adds the retained JML GPCM adjacent-logit response-kernel Jacobian in
the full optimizer coordinate order. It combines constrained additive columns
with the sum-zero log-slope chain rule, records a local rank ladder and bounded
null explanations, verifies analytic derivatives against central differences,
and checks unit-slope reduction plus row-order invariance. The corresponding
MML field fails closed with an explicit person-integrated-pattern requirement;
the conditional JML kernel is not relabelled as marginal evidence. Neither the
JML local rank nor the MML boundary changes readiness or freezes a threshold.

Draft.28 adds the observed-Person-pattern MML marginal score decomposition for
bounded nonlinear fits. Person log-marginal contributions reconstruct the full
objective, analytic score rows reconstruct the full score, and each row is
checked against central differences in the exact optimizer coordinate order.
The record omits Person identifiers and the score matrix, imposes explicit
execution caps, and keeps the observed-pattern rank diagnostic separate from
an all-possible-pattern structural map, fitted information, weak-information
classification, and readiness.

Draft.29 adds bounded exhaustive response-pattern enumeration for every
retained Person observation design under unit row weights. It verifies marginal
probability normalization and the zero expected-score identity, accumulates
the score-outer-product expected information in optimizer coordinates, and
checks selected response patterns against independent central differences.
Missing rows shorten the retained design rather than being imputed; nonunit
weights and combinatorial or dimensional overflow receive explicit
non-evaluated states. Workload and numeric summaries are retained without
storing Person identifiers, pattern rows, score rows, or the information
matrix. The resulting rank is local to the retained parameter vector,
quadrature rule, and observed designs and does not classify structural or weak
identification or alter readiness.

Draft.30 canonicalizes the retained row layout and evaluates exhaustive
patterns once for each exact Person-design signature. The signature includes
facet, step, slope, and interaction indices and, when applicable, the aligned
latent-regression population-design row; it excludes Person identifiers and
observed scores. Expected information is reconstructed by exact group
multiplicity. A forced non-reuse path must agree numerically, while missing
layouts and distinct population-design rows must remain separate. The fitted
record exposes only conceptual and evaluated workload summaries and group
counts. This exact reuse reduces duplicate computation but does not solve the
exponential response space of a unique long design or establish a target-size
capacity claim.

- Replication counts are selected from a frozen Monte Carlo-precision target,
  not convenience or elapsed time alone.
- A pooled mean cannot rescue a failed sample-size, family, facet, category,
  slope, or link-density cell.
- Connected sparse, weak-link, bridge, articulation, disconnected,
  empty-category, and extreme-score cases retain separate labels.
- Two-rater panels retain complete, shared-Person sparse, and zero-common-
  Person labels. The audit records rater-panel size, the Person-sharing graph,
  common-Person counts, bridge strength, articulation, component balance, and
  local information; binary connectivity alone cannot confer readiness.
- Before optimization, the constrained free-coordinate design must be checked
  for exact rank. A connected graph cannot confer readiness when a rater
  contrast is aliased with nested Person locations. After full rank is
  established, singular values and fitted information diagnose weak rather
  than exact identification; no universal condition-number cutoff is frozen
  in this draft.
- The constrained audit is estimator-specific. JML includes free Person
  coordinates; MML integrates them and must identify when a rater link rests
  on a common latent-population assumption rather than shared Persons. Main-
  effect rank cannot certify an interaction fit.
- Category support records counts, maximum proportion, normalized entropy,
  local facet support, and threshold information by model. A consecutive
  global category range cannot confer readiness when a category is unused or
  almost all responses occupy one category.
- External parameter tolerances are applied only after declared and retained
  category maps and free step dimensions agree within every relevant
  step-facet level. A category-dropping FACETS fit is an edge-policy control,
  not a numerical comparison to mfrmr's retained rectangular PCM steps.
- Extreme JML Person results are compared by low/high/unbounded status. A
  FACETS adjusted display measure is not compared to an optimizer-dependent
  finite proxy as though both were the primary JML estimand; nonextreme Person
  measures remain a separate numeric stratum.
- For independently free extreme Persons, an extended profile-limit structural
  estimate is a third identity: it is neither the raw finite trace nor a finite
  adjusted Person display, and it does not correct incidental-parameter bias.
  Constraint-coupled extremes remain outside this reduction.
- The same boundary contract applies to other JML element classes when their
  sufficient score is extreme. MML/EAP Person results remain governed by the
  population/prior model and are not relabelled as JML-unbounded.
- `DES-DISCONNECTED` and other unidentified negative controls must not become
  inferentially ready. A false-ready result is a blocker.
- The deterministic CRAN smoke tier demonstrates execution only. Release
  recovery decisions come from the non-CRAN confirmation tier; extended
  sensitivity cannot hide a failed core cell.

Bias, RMSE, coverage, failure-rate, and Monte Carlo-precision thresholds are
`pilot_required`. Existing helper defaults are not silently adopted as
release thresholds.

The draft.19 one-seed extension and draft.20 divergence audit are calibration
evidence for these rules. The constrained main-effect audit found nullity one
for the zero-common-Person two-rater design. The severe PCM row was reported as
ready despite unsupported mfrmr steps, while FACETS dropped categories and
fit a smaller per-Criterion step dimension. Its raw parameter MAE is therefore
not a common-estimand discrepancy. Large weak-overlap Person maxima were
concentrated in FACETS-adjusted extreme scores; nonextreme MAE was 0.1465 for
RSM and 0.1190 for PCM. These outcomes define fail-closed targets and
comparison strata; they do not set a numeric support boundary.

## G3: information-criterion contract

### Common MML panel

For the current fixed-facet marginal MML likelihood, Persons are the
independent likelihood units after the latent Person parameter is integrated
out. Let `D = -2 logLik`, `k` be the dimension of the free optimization vector
after constraints, and `N_person` be the number of independent Persons. The
common panel is:

```text
AIC   = D + 2 k
BIC   = D + log(N_person) k
SABIC = D + log((N_person + 2) / 24) k
```

The SABIC formula identifier is `sclove_n_plus_2_over_24`.

Every fitted MML object and comparison row must expose `Deviance`, `LogLik`,
`Npar`, `ResponseRows`, `WeightedResponseTotal`, `Persons`, `ICSampleSize`,
`ICSampleSizeBasis`, `AIC`, `BIC`, `SABIC`, the formula identifiers, and the
integration-evaluation identity.

### Migration and free dimension

0.2.2 uses response rows, or summed observation weights, in the BIC penalty.
0.2.3 corrects that basis without silently changing the meaning of the legacy
summary `N`: response rows, weighted response total, Persons, and IC sample
size remain separate fields, and NEWS identifies the behavior change.

`k` excludes MML EAP Person estimates and includes every estimated free
latent-regression, population variance/covariance, facet, step, slope, and
interaction coordinate. It must equal the retained optimization-vector
dimension after anchors and constraints.

`Npar` is the canonical 0.2.3 field. The existing lower-case `npar` comparison
column remains a compatibility alias during the 0.2.x series and must equal
`Npar`; it is never counted as a second source of truth.

### Comparability rules

| Fit type | AIC/BIC/SABIC ranking |
| --- | --- |
| Unweighted inference-ready MML on the same observations and likelihood basis | Eligible after all identity checks. |
| MML with an explicitly supplied all-unit weight column | Same as unweighted MML. |
| MML with any non-unit observation weight, including a value constant within Person | Suppressed with `ICComparable = FALSE` in 0.2.3. |
| JML | No primary AIC/BIC/SABIC ranking; any raw value is descriptive. |
| Cross-engine MML | Eligible only after likelihood constants, observations, constraints, `k`, `N_person`, and integration evaluation are matched. |

The non-unit-weight rule is structural rather than pilot-calibrated. In the
current marginal likelihood, multiplying every conditional response
contribution for a Person by a common weight is not generally equivalent to
replicating that Person as independent latent draws. Therefore the label
"Person-frequency weight" does not by itself justify an effective-Person BIC
sample size. A later weighting contract may introduce a different likelihood
with explicit sampling semantics; 0.2.3 does not infer one from the supplied
row weights.

External comparisons recompute the common panel from comparable `D`, `k`, and
`N_person`. Native fields remain separately named and retain their formula.
In the audited TAM 4.3-25/official-source snapshot, native `aBIC` uses
`log((n - 2) / 24)` and must not be relabelled as the common Sclove SABIC.
Every external run records and rechecks its installed version and native
formula.

### Stored-value and object-version rules

Every newly fitted 0.2.3 MML object records `ICContractVersion`, `Deviance`,
`LogLik`, `Npar`, `ResponseRows`, `WeightedResponseTotal`, `Persons`,
`ICSampleSize`, `ICSampleSizeBasis`, `WeightPolicy`, `ICEligible`,
`ICSelectable`, `ICStatus`, `AIC`, `BIC`, `SABIC`, `SABICSelectable`, formula
identifiers, the integration-evaluation identity, `ICQuadraturePoints`,
`ICIntegrationTier`, `ICIntegrationStatus`, and
`ICIntegrationSelectable`.
`N` retains its 0.2.2 response-row or summed-weight meaning for compatibility;
it is not an IC input.

When `ICEligible = FALSE`, the canonical `AIC`, `BIC`, and `SABIC` fields are
`NA`; a backward-compatible raw quantity, if retained, must use an explicitly
legacy/descriptive field name. This keeps a displayed value from bypassing the
comparison guard.

`compare_mfrm()` recomputes the common panel from canonical `LogLik`, `Npar`,
and `Persons`, and verifies any stored criterion against that recomputation.
It does not inherit a stale summary value as the comparison value. A mismatch
between stored and recomputed values is a concern and suppresses ranking.

An object without the 0.2.3 contract identity is classified
`legacy_or_unknown`. Its stored 0.2.2 AIC/BIC may be displayed as explicitly
legacy descriptive output, but it cannot be mixed into a 0.2.3 ranking or
silently reinterpreted with Person-based BIC. Refit under the current package
to obtain comparable criteria.

### Numerical and interpretive guards

- ICs are evaluated at a common locked integration setting. Their ordering and
  delta values must be stable over the frozen quadrature/QMC ladder within a
  pilot-calibrated numerical tolerance.
- When QMC/deviance drift is not small relative to a model difference, the IC
  comparison is unresolved.
- At `N_person <= 22`, the Sclove SABIC penalty is non-positive; SABIC may be
  displayed with a warning but cannot block or select a model.
- Delta ICs are relative evidence within a candidate set. Neither Akaike nor
  BIC-derived weights are described as assumption-free literal model
  probabilities.
- BIC/SABIC do not repair boundary/singularity or make a tiny large-N gain
  practically important. No IC is a standalone dimensionality gate.

### Current integration-pilot state

The first working-tree `IC-INTEGRATION` pilot was run under `0.2.3-draft.2`,
then extended to a deterministic six-scenario matrix under draft.3. Draft.4
adds an independent-refit layer and a fail-closed public integration tier.
All three development layers are recorded in
`ic-integration-pilot-record-0.2.3.md`; none is candidate-linked confirmation.
The policy `fixed_retained_vector_common_ghq_v1` holds each candidate's free
parameter vector fixed and changes only the common GHQ evaluation grid. This
isolates integration approximation from optimization and verifies that the
source-grid evaluation reproduces the retained objective first.

The development pilot ladder uses q = 7, 15, 31, 61, 91, and 121, with q=121 as the
reference ceiling. q=7 and q=15 are diagnostic stress points. q=31 through
q=121 are candidate core points, not a frozen release ladder. The following
are candidate rules for calibration, not `frozen_numeric` criteria:

- source-grid objective reproduction within
  `1e-10 * max(1, abs(stored objective))`;
- unchanged criterion ordering over the candidate core ladder;
- maximum absolute raw-deviance drift no larger than 0.10;
- maximum absolute pairwise criterion-gap drift no larger than 0.10 and no
  larger than 10% of the corresponding non-tied reference gap; and
- no automatic preference when the reference gap is within the declared
  numerical tie tolerance.

The absolute 0.10 candidate is one twentieth of the conventional delta-IC 2
screening scale already described in the package help; it is a calibration
anchor rather than a result-tailored pass line. The broader pilot must still
challenge it with further weak-link, boundary-adjacent, and deliberately
near-tied candidates. TAM product-quadrature and QMC ladders require their own
evaluator and cannot inherit the mfrmr GHQ result. `IC-INTEGRATION-TOL`
therefore remains `pilot_required`, and confirmation remains unauthorized.

The six retained draft.3 cells had no fit failure or warning. Across the
candidate q=31--121 core, every criterion ordering was stable; the largest
raw drift was 0.0397363, pairwise-gap drift was 0.00924359, and relative gap
drift was 0.0160086. These observed values sit inside all three candidate
rules but do not freeze them. The wide-latent cell, whose estimated latent
variance was approximately 9, changed AIC/SABIC ordering when q=7 and q=15
were included. This is a direct warning against treating equal coarse-grid
identities as sufficient IC evidence.

The draft.4 policy `independent_refit_then_common_ghq_v1` separately refitted
all six matrix scenarios at q = 7, 15, 31, 61, 91, and 121 from ordinary
deterministic starts, then reevaluated every retained solution at common
q=121. All fits remained warning-free and the q=31--121 ordering remained
stable. Across the six scenarios, the largest core native criterion-gap
drift was 0.00944565, the largest common-q=121 gap drift was 0.000202065, the
largest parameter drift was 0.00484794, and the largest common-reference
deviance excess was 0.000734096. In the wide-latent cell, the native q=7
AIC/SABIC ordering reversed, but reevaluating both q=7 solutions at q=121
restored the q=121 preference. The q=15 native gap was already order-stable
but differed from the reference AIC gap by about 10.5%. These results locate
the observed coarse-grid problem primarily in integration evaluation and do
not justify automatic ranking at q=7 or q=15.

Accordingly, contract `mfrmr_ic_person_v2` separates formula eligibility from
selection readiness:

| GHQ points | Integration tier | Public comparison policy |
| ---: | --- | --- |
| below 15 | `coarse_screening` | Raw canonical ICs may be inspected, but automatic ranking is screening-only and suppressed. |
| 15--30 | `intermediate_review` | Raw canonical ICs are review-only; automatic ranking remains suppressed. |
| 31--60 | `standard_start` | Same-basis eligible fits may enter comparison; this is a starting grid, not proof of integration stability. |
| 61 or more | `dense_sensitivity` | Same-basis eligible fits may enter comparison and can provide denser-grid sensitivity evidence. |

Below q=31, `ICEligible` may remain true while `ICSelectable` is false;
`compare_mfrm()` suppresses delta criteria, criterion weights, preferred-model
labels, evidence ratios, and LRT. This is a fail-closed draft policy, not a
frozen declaration that every q>=31 comparison is adequate. Close,
consequential, wide-latent, or otherwise sensitive comparisons still require
a prespecified denser common-grid check. Weak-link/boundary cells,
cross-platform replication, TAM QMC evaluation, and the practical reference
ceiling remain unresolved before the ladder and tolerance can be frozen.

### Current external-IC normalization state

Draft.5 adds the repository-only contract `mfrmr_external_ic_v1`. It calls the
same internal common-panel builder used by newly fitted mfrmr objects, so the
normalized panel has one formula source:

- `AIC = D + 2k`;
- `BIC = D + log(N_person)k`; and
- `SABIC = D + log((N_person + 2) / 24)k`.

The record keeps engine-native fields and formula identifiers separately.
The TAM 4.3-25 adapter verifies native AIC/BIC arithmetic and retains native
`aBIC = D + log((n - 2) / 24)k`; it never copies that value into common
`SABIC`. Seven deterministic fixtures cover ready MML arithmetic, unchecked
integration, JML suppression, the small-N SABIC boundary, inconsistent
deviance/log-likelihood, and incomplete identity. A local TAM 4.3-25 PCM
development run also exercised the object adapter. These are pilot/unit
observations, not candidate-linked external evidence.

Arithmetic eligibility and comparison readiness are distinct. A record can
show reproducible common criteria while remaining non-comparable. Delta
criteria, weights, and preferred labels require all candidates to have:

- MML, finite consistent deviance/log-likelihood, free dimension, and Person
  count;
- one shared observation-set, likelihood-basis, constraint-basis, and
  integration-comparison identity;
- an engine-specific integration-evaluation identity;
- reviewed convergence status `pass`; and
- integration-stability status `pass`.

The public `import_tam_fit()` route now accepts only verified unidimensional
`tam.mml` objects. It rejects `tam.jml` rather than relabelling it as MML and
rejects `ndim > 1` rather than exposing a flattened one-scale imported fit.
Native TAM criteria and version provenance remain descriptive on supported 1D
imports, which cannot enter the current mfrmr IC contract automatically.
Dimension-aware TAM evidence belongs to the separate repository runner.

Draft.7 adds `mfrmr_conquest_ic_handoff_v1` to the ConQuest bundle and the
repository-only external normalizer. The generated command retains the
estimate `matrixout` history as CSV. The adapter reads its documented third
matrix column as deviance, checks the history free dimension independently
against the combined parameter/regression/covariance export rows, checks the
final history vector against those exports, and requires the case-EAP PIDs to
match the expected bundle Person IDs exactly. In ConQuest 5.47.5 the native CSV header calls that objective
column `LogLikelihood` even though its positive values are the deviance stated
in manual section 4.9.2 and match the human summary table; the audit preserves
this header discrepancy rather than silently trusting the label.
The adapter is locked to audited ConQuest 5.47.5; a later engine version must
receive a schema/objective audit before it can enter the handoff.

Draft.8 fixes the generated ConQuest benchmark controls at parameter change
`1e-8`, deviance change `1e-10`, and 2000 iterations. In the 60-Person,
six-item, one-covariate binary pilot, ConQuest's default stopping rule ended at
iteration 42 with deviance `424.739512`, approximately `0.000533` above mfrmr.
The strict run ended at iteration 132 with deviance `424.738979`, versus mfrmr
`424.738979414`; the difference `-4.14e-7` is within the six-decimal ConQuest
CSV resolution. The largest exported transformed-parameter difference was
`5.77e-6`. This removes an apparent additive likelihood-constant discrepancy
for this one pilot but does not freeze a tolerance or establish RSM/PCM and
cross-platform agreement.

Draft.9 adds repository-only contract
`mfrmr_conquest_binary_ladder_v1`. It prepares but never launches the external
program, then reviews strict runs at q=7, 15, 31, 61, 91, and 121 plus a second
same-platform q=31 run. The q=31--121 rows all passed the arithmetic handoff:
ConQuest deviance was `424.738979`, the maximum absolute cross-engine deviance
difference was `4.142e-7`, and the maximum transformed-parameter difference
was `5.762e-6`. The five native q=31 CSV outputs were byte-identical across
the two runs. The q=7 row was rejected after ConQuest retained an earlier
higher-likelihood solution and its final history/export vectors differed by
up to `0.036778`; q=15 was rejected despite deviance-criterion termination
because its final history/export vectors differed by up to `8.7e-5`.

Draft.10 adds repository-only contract
`mfrmr_conquest_polytomous_rsm_pcm_v1`. It prepares one deterministic
120-Person, five-item, four-category input and matched q=31 latent-regression
fits using ConQuest `item + step` for RSM and `item + item*step` for PCM. The
reviewer verifies complete category coverage, audited native 5.47.5 parameter
label order, history/export identity, free dimensions, item sum-zero, shared
RSM step sum-zero, and item-specific PCM step-row sum-zero constraints. It
never starts the external executable.

Both same-platform runs terminated on the deviance criterion after 63
iterations. RSM retained 9 and PCM 17 free parameters in both engines; all
reconstructed sum-zero residuals were exactly zero. The largest absolute
ConQuest-minus-mfrmr deviance difference was `1.24811e-6`, the largest free or
full transformed-parameter difference was `1.604646e-6`, and the
cross-engine difference in the RSM-minus-PCM deviance drop was `1.10628e-6`.
Every row remains `ComparisonReady = FALSE` because integration stability is
still `review` and no tolerance has been frozen.

Draft.11 replaces the one-pair pilot contract with
`mfrmr_conquest_polytomous_rsm_pcm_ladder_v1` and applies that same fixed input
to q=7, 15, 31, 61, 91, and 121 plus a fresh q=31 directory for each model.
Every q=31--121 RSM and PCM core row passed the arithmetic handoff. ConQuest
deviance was constant at six-decimal export resolution within each core; the
maximum absolute cross-engine deviance difference was `1.24811e-6`, the
maximum free or full transformed-coordinate difference was `1.674273e-6`,
and the maximum cross-engine difference in the RSM-minus-PCM deviance drop was
`1.10628e-6`. The mfrmr core deviance ranges were `5.96e-8` for RSM and
`8.7637e-7` for PCM. Within each model, all five native q=31 CSV files were
byte-identical across the two runs.

The low-node rows are deliberately retained. RSM q=7 and q=15 passed the
schema/arithmetic handoff but differed from mfrmr by `4.437555` and
`-0.227498` deviance units; PCM q=15 differed by `-0.116292`. PCM q=7 was
rejected because its terminal history and native export vectors differed by
`1e-6`. Thus `accepted_arithmetic` means that a native result can be audited,
not that cross-engine agreement or integration stability has passed.

This handoff now establishes reproducible arithmetic, one same-platform binary
repeat, a useful coarse-node diagnostic boundary, and same-platform
polytomous likelihood/constraint mapping, node-ladder, and repeat evidence
only. Independent platform/version replication, integration-tolerance freeze,
and candidate-linked mfrmr/TAM/ConQuest runs remain pending;
`mfrmr_external_ic_v1` must not be used to manufacture cross-engine
comparability by assigning identical identity strings without that evidence.

## G4: dimensionality challenge

The dimensionality challenge follows three stages and never creates a native
multidimensional mfrmr feature.

### Current draft.6 TAM runner state

Contract `mfrmr_tam_dimensionality_pilot_v1` keeps multidimensional TAM
objects in repository evidence and outside `import_tam_fit()`. Its first
binary-Rasch matrix uses one prespecified true-1D and one prespecified true-2D
control, fixed Q hashes, product grids of 15/21/31/41 nodes per dimension, and
deterministic QMC grids of 512/1024/2048/4096 nodes. All 32 fits preserved the
observation set, had finite fit/IC-consistent objectives and positive-definite
latent covariance matrices, stopped before the iteration ceiling, and emitted
no warnings or hard failures. Ten remain `review` because TAM's final reported
objective did not equal the last iteration-history value.

The true-1D 15-point product row reverses all three common-IC signs; the
21--41 point rows agree and have maximum criterion-gap drift 0.000102. The
true-2D product rows retain all signs, with maximum full-ladder gap drift
0.025685. Deterministic-QMC signs are stable, but maximum gap drift reaches
0.188133. Two independent 1024-node QMC refits for each model/control reproduce
deviances and retained parameters exactly, separating finite-node drift from
run-to-run randomness for these TAM 4.3-25 cells.

The first `QMC = FALSE` audit uses 1024 stochastic nodes and four operative
seeds. Every external integration-evaluation identity includes its seed. The
maximum criterion-gap seed difference is 3.965763 in the true-1D control and
2.865980 in the true-2D control; true-1D raw deviance-gain sign changes across
seeds. No hard failure or warning occurred, but all stochastic results remain
review-only. A single stochastic run or a retrospectively favored seed cannot
enter release evidence.

These are single-seed calibration observations. Every normalized record keeps
integration stability unchecked, selection is unauthorized, no ordinary
chi-square LRT p-value is produced, and no dimension score is returned.
Replicated truth cells, multi-node/platform stochastic integration and its
seed-aggregation rule, platform review, boundary bootstrap, empirical
partitioning, interaction attribution, and consequence criteria remain
unresolved.

### Explore

Substantive theory, design review, residual PCAR/parallel evidence, and
Q3-style residual patterns generate candidate clusters, local-dependence
pairs, rater effects, and Q matrices. The discovery output is a versioned
hypothesis set only.

The current `q3_statistic()` uses standardized residuals aggregated to
Person-by-facet-level cells. It is not relabelled as raw-residual Yen Q3 or the
published maximum-relative-to-mean Q3*. Fixed residual cutoffs remain
exploratory. A formal blocking Q3* route requires an explicit residual unit,
multiplicity policy, and design-specific parametric bootstrap.

`analyze_residual_pca()` is likewise an exploratory hypothesis generator.
Before confirmation, `DIAG-PCAR-NULL` and `DIAG-PCAR-LOCAL` freeze the residual
unit, missing-pair correlation and smoothing rules, permutation unit, retained
quantile precision, topology/missingness grid, and failed-replicate policy.
The draft.19 pilot detected planted local dependence but also flagged one
balanced single-seed row, and weak-overlap PC1 differed between mfrmr and
FACETS despite row-residual correlations above 0.996. External raw PCA and
mfrmr permutation PCAR are therefore separate sensitivity definitions, not a
pooled statistic.

Fixed `Rater:Criterion` interaction recovery and `estimate_bias()` screening
also remain separate. `DIAG-BIAS-NULL` and `DIAG-BIAS-NONNULL` estimate
conditional false-positive and detection behavior across effect size, cell
information, category support, topology, multiplicity, and numerical-
readiness state. A conventional p-value/t screen is not a frozen automatic
decision rule. FACETS Table 14 enters only through a separately parsed `?B`
control after its estimand and uncertainty definitions are matched.

### Confirm

- Freeze Q matrices, labels, variance/covariance constraints, response family,
  partition, integration policy, starts, metrics, and failure policy.
- Establish matched mfrmr-versus-TAM 1D overlap before using TAM alternatives.
- Compare TAM 1D with prespecified TAM multidimensional models on untouched
  Persons or an external sample.
- Use the four-model attribution grid where identified: 1D additive; 1D plus
  rater-by-criterion interaction; multidimensional additive; and
  multidimensional plus interaction.
- A deliberately confounded design is classified unidentified; fit gain is
  not assigned to either explanation.
- Do not use a direct cross-engine LRT. Within-engine nesting must be proved.
  When 1D lies on a variance/correlation boundary, use a frozen parametric-
  bootstrap deviance-difference reference rather than the ordinary chi-square
  p-value.
- Report deviance gain per Person and response, normalized AIC/BIC/SABIC,
  held-out predictive gain where feasible, residual change, dimension
  correlations, parameter stability, failed/singular replicate rates, and
  numerical integration uncertainty.

True-1D false-selection control, true-2D detection power, bootstrap size,
smallest practically relevant gain, and QMC-stability thresholds are
`pilot_required`. The pilot and confirmation seed/Person registries must be
disjoint.

### Test consequences

A better-fitting multidimensional model does not by itself justify subscores.
The consequence stage checks incremental prediction beyond the total score,
conditional precision/information, replication stability, classification and
rank changes, invariance, and an external criterion only where defensible.

The final classification is one of:

1. `total_score_only`: no confirmed material departure requiring a reporting
   change;
2. `multidimensionality_as_sensitivity`: structural improvement without
   defensible individual subscore value;
3. `future_subscore_research`: replicated practical value that motivates a
   later native model but does not add scores in 0.2.3; or
4. `unresolved`: identification, integration, replication, or consequence
   evidence is insufficient.

`unresolved` cannot support an unqualified unidimensionality claim.

## G5: external comparison

ConQuest is the required 0.2.3 external MML core for matched unidimensional
binary, RSM, and PCM cases. Each row matches observations, missingness,
categories, model, constraints, quadrature, starts, convergence review,
orientation, and parameter transformation as far as the programs permit.
Correlations are descriptive; blocker tolerances apply to signed/absolute
differences on named estimands.

TAM supplies a second MML reference where its unidimensional likelihood,
population, design matrix, constraints, and integration evaluation can be
matched. This row remains distinct from TAM's within-engine 1D/2D
dimensionality challenge and from every TAM JML mode; none may borrow another
row's likelihood or recovery conclusion.

FACETS 4.5.0 is the selected 0.2.3 external JML stress implementation for RSM
and PCM. The environment record in `facets-jml-stress-plan-0.2.3.md` captures
orientation, constraints, parser behavior, and executable/report identity but
does not stop pilot execution when the current upstream release differs. The
binary microcase does not by itself validate polytomous recovery. Mandatory
core families cover connected recovery, current element/group anchors,
sparse/weak-link topology, and edge cases. Threshold anchors remain 0.2.4
scope. Fit and DFF/DIF rows remain optional promotions until definitions,
null/non-null generators, multiplicity, and tolerances are frozen.

FACETS is not ground truth. For every paired replicate, mfrmr and FACETS are
first evaluated separately against the common generating truth, then compared
on explicitly transformed common estimands. Agreement between two biased
solutions cannot pass recovery. FACETS JML Person measures are not targets for
MML EAP scores, and no JML-versus-MML equality criterion is permitted. No
proprietary program is launched by package tests, and no proprietary binary,
license material, or identifier-bearing external case file is committed.

Before a FACETS row reaches a numeric tolerance, its comparison contract must
verify the category map, retained scale/step dimension, active facets,
constraints/anchors, parameter orientation, estimability, and extreme-score
policy. A failed contract is retained as definition or failure-behavior
evidence and is never averaged into an engine-agreement statistic.
Eligibility is metric-specific: rejected extreme Person rows need not suppress
a valid nonextreme comparison, while an unmatched PCM step dimension rejects
parameter MAE even if row predictions are retained as descriptive sensitivity.
Every aggregate reports expected, eligible, rejected-by-reason, missing, and
failed denominators.

FACETS evidence is generated in isolated synthetic run directories with exact
replicate accounting, timeouts, exit/stderr capture, one process by default,
and fail-closed stale-output detection. The official-site version, executable
file metadata, report-header version, executable SHA-256, controls, inputs,
outputs, parser/generator versions, and candidate manifest are retained as
distinct fields. Different versions are analyzed as separate sensitivity
strata rather than silently pooled; the batch continues.

TAM and immer add estimator-convention evidence, not a vote among programs.
The primary identities are CRAN TAM 4.3-25 and CRAN immer 1.5-13. Audited
development snapshots TAM 4.4-2 and immer 1.6-1 may be executed only as
separately labelled sensitivity strata. Package version, source identity, R and
dependency versions, function/default arguments, response/design-matrix hash,
output hash, and normalizer version are mandatory provenance. A change in a
default adjustment creates a different method-mode identity even if the
package label otherwise appears unchanged.

The JML grid includes mfrmr uncorrected JML and, where supported, TAM and immer
unadjusted, extreme-score-adjusted, and bias-corrected modes. Each mode is
declared before pilot results are inspected. Comparisons reuse the same
observations, weights, category map, active facets, design-matrix columns,
constraint/free-coordinate map, parameter orientation, and generating truth.
Unadjusted and adjusted/corrected results are never pooled. Extreme and
nonextreme Person rows receive separate denominators; any finite display for a
theoretically unbounded extreme measure is not treated as ordinary parameter
recovery.

Truth recovery, interval coverage or explicit SE unavailability, false-ready
and failed-run behavior, and transformed between-program differences are
reported separately by model, parameter class, design cell, and method mode.
The grid must include balanced exposure, unequal workload, two raters,
sparse/weak-link assignment, planned and unplanned missingness, category
imbalance, extreme scores, and an incidental-parameter sequence in which the
number of Persons grows while observations per Person remain fixed. Agreement
between two biased modes cannot pass, and the best-agreeing mode cannot be
selected post hoc.

A correction such as `(I - 1) / I` is reference evidence, not a native mfrmr
specification. With arbitrary facets, missingness, unequal exposure, and
design-matrix pseudoitems, the effective `I` must be defined before transport.
A future native correction needs a separate proposal, balanced-case reduction,
identification/anchor proof, and prespecified improvement in bias/RMSE without
unacceptable coverage, boundary, or failure-rate cost.

immer CML and CCML rows are eligible only for matched Rasch-family structural
parameters that remain after conditioning. Their contract verifies the
sufficient statistic, category support, design rank, constraint basis,
missingness treatment, and parameter transformation. Person measures,
bounded-GPCM quantities, latent-regression parameters, and quantities removed
by conditioning are ineligible. These rows are external references and do not
create native `fit_mfrm()` methods.

The immer HRM lane is an alternative-data-generating-model stress test. It
requires a frozen latent true-rating, rater severity/variability, prior, MCMC
diagnostic, and replication contract. It evaluates the behavior of additive
mfrmr readiness, bias, and residual diagnostics under local dependence; it is
not included in parameter/objective engine tolerances, cannot be exposed as
`method = "HRM"`, and cannot establish model preference from one fitted sample.

External numerical tolerances are `pilot_required`; the mandatory scenario
scope and fail-closed provenance rules are structural blockers now.

## G6: public contract

Before release, code, help, README, vignettes, capability tables, reports,
exports, and runtime guards must agree that:

- the current latent trait is unidimensional;
- PCAR/Q3-style output is exploratory hypothesis-generation evidence;
- AIC/BIC/SABIC apply only under the recorded MML comparability contract;
- IC-derived weights are not unconditional model probabilities;
- `JML` is canonical and `JMLE` is only an input alias;
- a common MML/JML interface does not imply equal estimator maturity, and JML
  correction/uncertainty limitations remain visible wherever relevant;
- CML/CCML and HRM are not current `fit_mfrm()` estimator choices, and HRM is a
  distinct latent-data/local-dependence model rather than a backend;
- threshold anchors, frozen calibration, multiple scales, scale-specific PCM,
  native multidimensional scores, and unrestricted GPCM remain later work;
- external comparisons cover matched rows rather than blanket equivalence; and
- repository planning, local paths, development decisions, and private
  evidence language do not leak into installed first-screen output.

Unsupported routes fail closed or display an unavoidable caveat before the
affected quantity is interpreted.

## G7: engineering release

The exact candidate must pass:

- full regression outside CRAN and the required macOS/Windows/Linux R matrix;
- `R CMD check --as-cran --run-donttest` on the exact upload tarball;
- manuals, URLs, examples, package-content, source-truth, and terminology
  audits;
- official Win-builder or the then-current CRAN-equivalent Windows check; and
- a package-controlled CRAN workload below 600 seconds.

The 600-second gate sums ordinary examples, `donttest` examples, tests, and
vignette rebuilding. Dependency installation, manuals, and check
infrastructure remain diagnostic context rather than package-controlled time.
Heavy recovery, bootstrap, and external gates run outside CRAN and remain
candidate-linked and reproducible.

## Numeric criteria still required before freeze

The following criterion IDs are intentionally unresolved in the current
draft:

| Criterion ID | Required frozen content |
| --- | --- |
| `NUM-SCORE-TOL` | Absolute/scaled canonical-score tolerance by model and parameter class. |
| `NUM-OBJECTIVE-TOL` | Cross-engine objective tolerance at common evaluation. |
| `NUM-PARAMETER-TOL` | Transformed parameter tolerance by estimand. |
| `REC-GRID` | Core sample sizes, facet counts, category support, link density, and slope cells. |
| `REC-MCSE` | Replication rule and maximum Monte Carlo uncertainty. |
| `REC-BIAS-RMSE` | Practical bias and RMSE limits by parameter class. |
| `REC-COVERAGE` | Supported-interval coverage limits and SE-availability rule. |
| `REC-FAILURE` | Maximum failed-fit and false-ready rates. |
| `IC-INTEGRATION-TOL` | Maximum acceptable IC/deviance drift across the integration ladder. |
| `DIM-TYPE1` | Maximum false multidimensional selection rate with Monte Carlo margin. |
| `DIM-POWER` | Minimum detection rate for each prespecified true-2D core. |
| `DIM-PRACTICAL-GAIN` | Smallest practical predictive/score consequence. |
| `DIM-BOOTSTRAP` | Bootstrap replication and failed/singular replicate policy. |
| `EXT-CQ-TOL` | ConQuest signed/absolute tolerances by common estimand. |
| `EXT-FACETS-IDENTITY` | Recorded executable SHA-256/file metadata, report-header version, parser/generator identities, and separate-stratum policy for version or stale-output differences. |
| `EXT-FACETS-GRID` | Mandatory RSM/PCM core, anchor, sparse, and edge cells plus optional fit/DFF promotions and explicit exclusions. |
| `EXT-FACETS-TOL` | Truth-recovery and transformed between-program tolerances by model, parameter/statistic, design cell, and estimand. |
| `EXT-FACETS-MCSE` | Replication, complete-case accounting, Monte Carlo uncertainty, failed-run, and escalation rules. |
| `EXT-JML-MODE-GRID` | Frozen TAM/immer function arguments, raw/adjusted/bias-corrected method identities, balanced and adversarial design cells, and explicit exclusions. |
| `EXT-JML-TOL` | Transformed between-program tolerances by structural parameter, nonextreme Person estimand, design cell, and JML convention. |
| `EXT-JML-RECOVERY` | Truth-bias/RMSE and incidental-parameter trend limits by method mode; agreement alone cannot satisfy this criterion. |
| `EXT-JML-COVERAGE` | Supported SE/interval availability, coverage, width, and boundary policy by method mode and parameter class. |
| `EXT-CML-TOL` | CML/CCML structural-parameter transformation, recovery, and matched-overlap tolerance with explicit ineligible quantities. |
| `EXT-EST-MCSE` | Replication, failed-run, complete-case denominator, Monte Carlo precision, and no-post-hoc-mode-selection rules for TAM/immer lanes. |

No placeholder above may be filled after its confirmatory result is viewed.
If pilot evidence cannot support a defensible threshold, the corresponding
claim is reduced or deferred rather than decided ad hoc.

## Revision record

| Specification | Change |
| --- | --- |
| `0.2.3-draft.1` | Initial roadmap-to-gate translation. |
| `0.2.3-draft.2` | Recorded the M1 source audit; made all non-unit-weight IC ranking fail closed; added legacy-object, canonical-recomputation, and `Npar` compatibility rules; and linked the fixed IC fixture contract. |
| `0.2.3-draft.3` | Added the fixed-vector common-GHQ pilot policy, recorded the first RSM-versus-PCM run and six-scenario development matrix, and named candidate ladder/drift rules while leaving `IC-INTEGRATION-TOL` unfrozen. |
| `0.2.3-draft.4` | Added independent refit-at-grid/common-reference evidence, introduced `mfrmr_ic_person_v2`, and made q<31 comparison-derived selection output fail closed while leaving the ladder and `IC-INTEGRATION-TOL` unfrozen. |
| `0.2.3-draft.5` | Centralized the common IC formula panel, added fail-closed external normalization with native TAM aBIC preservation, and made public TAM imports reject JML and multidimensional objects rather than relabelling or flattening them. |
| `0.2.3-draft.6` | Added a separate dimension-aware TAM 1D/2D runner, recorded the first product-quadrature, deterministic-QMC, and four-seed stochastic-integration truth controls, made stochastic seeds part of integration identity, distinguished final-history review from objective inconsistency, and verified exact same-node QMC replay while leaving every dimensionality criterion unfrozen. |
| `0.2.3-draft.7` | Added explicit ConQuest stopping controls, a matrixout-history export, and a repository-only adapter that cross-check deviance, free dimension, exported parameter vectors, unit weights, Persons, convergence evidence identity, run metadata, and output fingerprints without parsing the free-form summary report; recorded the 5.47.5 objective-header discrepancy and left likelihood-constant, constraint, integration, RSM/PCM, and candidate-linked comparisons unresolved. |
| `0.2.3-draft.8` | Made ConQuest benchmark stopping controls explicit and strict, reran the binary 31-node pilot, and showed that the apparent `5.33e-4` objective discrepancy under ConQuest defaults shrinks to `4.14e-7` at strict convergence, while leaving tolerance calibration, replication, RSM/PCM, platform, and candidate-linked evidence unresolved. |
| `0.2.3-draft.9` | Added a repository-only ConQuest binary node-ladder preparer/reviewer, repeated q=31 with byte-identical native outputs, retained arithmetic agreement over q=31--121, and demonstrated fail-closed q=7/q=15 rejection from final-history/native-export disagreement; independent-platform replication, RSM/PCM, tolerance freeze, and candidate-linked evidence remain unresolved. |
| `0.2.3-draft.10` | Added a repository-only four-category ConQuest RSM/PCM preparer/reviewer, matched q=31 objective/free dimensions and explicit sum-zero coordinate reconstruction on one same-platform input, and retained all rows as pilot-only; independent-platform replication, a polytomous node ladder, tolerance freeze, and candidate-linked evidence remain unresolved. |
| `0.2.3-draft.11` | Extended the fixed four-category ConQuest RSM/PCM pilot to q=7/15/31/61/91/121 ladders and fresh same-platform q=31 repeats; retained objective and transformed-coordinate agreement across both q=31--121 cores, byte-identical native q=31 outputs within each model, and diagnostic low-node instability/fail-closed evidence while leaving independent-platform replication, tolerance freeze, and candidate-linked evidence unresolved. |
| `0.2.3-draft.12` | Added the fixed-fixture canonical free-score pilot, independently implemented three-step central differences at retained and nonzero-score points, the explicit free-log-slope to sum-zero-log to positive-slope GPCM Jacobian contract, and exact binary/unit-slope reductions; corrected the earlier box-constraint/projected-gradient wording while leaving `NUM-SCORE-TOL`, engine parity, expanded boundary-regime calibration, and confirmation unresolved. |
| `0.2.3-draft.13` | Added the fixed-fixture additive RSM/PCM engine-path pilot, exact hashed raw-EM-to-common-direct-polish handoff, common-vector objective/score evaluation through direct/EM/hybrid contexts, free and expanded parameter comparisons, and an explicit GPCM/interaction/latent-regression fallback boundary; left `NUM-OBJECTIVE-TOL`, `NUM-PARAMETER-TOL`, broader grid/platform calibration, and confirmation unresolved. |
| `0.2.3-draft.14` | Bound release-readiness output to an exact candidate-manifest schema, tarball/check-log/specification/checklist SHA-256 identities, a frozen specification ID, explicit confirmation authorization, and machine-checked current-versus-future public API truth; retained missing manifests, draft specifications, unfrozen blocker criteria, and every identity mismatch as concerns, so M3 and confirmation remain unauthorized. |
| `0.2.3-draft.15` | Added a release-readiness guard against carrying numeric test/check pass counts forward in current README, NEWS, or cran-comments prose; historical NEWS sections remain untouched, while current engineering status must come from the exact candidate-linked logs and hashes. |
| `0.2.3-draft.16` | Added fail-closed loading of candidate-linked gate-result rows, exact commit/specification identity checks, retained evidence-path SHA-256 verification, checklist item/scenario completeness, and blocker/roadmap/caveat status propagation into the release decision. |
| `0.2.3-draft.17` | Promoted FACETS from a conditional supplied-output row to a mandatory JML RSM/PCM stress lane; pinned local 4.5.0 with a non-blocking identity record and separate-version strata; separated truth recovery from program agreement; added anchor, topology, missingness, edge, optional DFF, Monte Carlo, stale-output, and support-envelope requirements; and retained all new numeric criteria as pilot-required. |
| `0.2.3-draft.18` | Added the first paired FACETS 4.5.0 RSM/PCM stress driver and one-seed 44-run pilot; expanded missingness, sparse, topology, duplicate, category-support, extreme-score, and contamination cells; verified fail-closed disconnected-design reporting; identified binary connectivity as insufficient for a single-bridge design; recorded 4.5.1 reporting/test sensitivity; and retained every external numeric threshold and confirmation claim as unresolved. |
| `0.2.3-draft.19` | Added two-rater, severe category-imbalance, checkerboard-interaction, and residual-local-dependence FACETS 4.5.0 extension cells plus a separate interaction/bias/PCAR runner; diagnosed false-ready zero-common-Person and category-support states, weak-interaction screening limits, and weak-overlap PCA-definition sensitivity; added scenario IDs and required quantitative design/category/diagnostic gates while retaining all results as one-seed pilot evidence. |
| `0.2.3-draft.20` | Added a repository divergence audit before FACETS tolerances; established exact rank deficiency in the zero-common-Person and other sparse controls, reclassified the severe PCM parameter gap as a retained-category/step-dimension mismatch plus false readiness rather than a demonstrated kernel defect, separated nonextreme Person agreement from FACETS adjusted extreme displays, and required estimability, category-contract, typed-extreme, and diagnostic-definition guards before confirmation. |
| `0.2.3-draft.21` | Converted the divergence diagnosis into eight dependent work packages; separated fit-, parameter-, and metric-level readiness; made JML/MML estimability rules distinct; specified sparse rank and category/step records, generalized typed JML boundary states, required one-source cross-surface propagation and legacy-object handling, and made external eligibility metric-specific with complete rejection accounting before the new-seed repilot. |
| `0.2.3-draft.22` | Completed the structural WP0 readiness contract: froze the internal v1 state vocabulary, deterministic fit precedence, conservative `InferenceReady` mapping, parameter and comparison states, controlled reason codes, typed condition/runtime policy, fail-closed 0.2.2-object mapping, and a validated 27-row adversarial fixture registry. Runtime implementation and every statistical threshold remain pending. |
| `0.2.3-draft.23` | Began WP1 runtime implementation with a sparse adjacent-category-logit rank audit in the actual RSM/PCM free-coordinate basis; added typed pre-optimization errors for exact aliases, counterfactual-JML classification of MML population-assumption linkage, bounded null explanations, and row-order, relabelling, anchor, PCM-step, interaction, and constraint-Jacobian controls. QR tolerance disagreement is diagnostic only and cannot become `weak_information` before the fitted-information rule is piloted and frozen. Nonlinear and fitted-information completion remains pending. |
| `0.2.3-draft.24` | Separated workflow integration from estimator maturity; added the TAM/immer estimator stress plan with raw, extreme-adjusted, bias-corrected, and combined-default JML modes as distinct truth-first comparison strata; limited immer CML/CCML to eligible Rasch-family structural estimands; classified HRM as an alternative latent/local-dependence model; prohibited automatic transport of finite-item correction and ambiguous `GMFRM` naming; and left every new numerical threshold and pilot result unresolved. |
| `0.2.3-draft.25` | Added the first bounded fitted-information instrument for stationary nonlinear fits: a dense numerical observed-information Hessian from the direct objective and analytical gradient, a diagnostic eigenvalue tolerance ladder, nonlinear-block summaries for GPCM log slopes and latent-regression residual variance, and explicit nonstationary/dimension-limit/unavailable states. The 80-coordinate cap is computational; no result classifies weak information, changes readiness, completes nonlinear structural identification, or freezes a release threshold. |
| `0.2.3-draft.26` | Added a separate retained-vector nonlinear transformation audit for GPCM log slopes and latent residual variance; recorded analytic and central-difference Jacobian agreement, dimensions, rank ladders, invariants, ranges, and conditioning; and made the result explicitly parameterization-only. Response-likelihood/design Jacobians, structural or weak-information classification, readiness effects, and frozen thresholds remain pending. |
| `0.2.3-draft.27` | Added the retained JML GPCM adjacent-category-logit Jacobian over the full constrained optimizer vector, joining additive and sum-zero log-slope coordinates; verified analytic derivatives, unit-slope additive reduction, row-order invariance, and nonbinding execution limits; and made MML fail closed with an explicit person-integrated response-pattern requirement. Local structural/weak-information classification, readiness effects, MML response geometry, latent-variance response geometry, and frozen thresholds remain pending. |
| `0.2.3-draft.28` | Added a bounded observed-Person-pattern marginal score decomposition for nonlinear MML fits; verified Person contribution sums against the full objective and analytic gradient, checked each pattern score against central differences, retained observation-row invariance and privacy controls, and recorded an explicit rank-deficient-but-derivative-correct negative control. All-possible-pattern structural geometry, expected-information rules, readiness effects, scale benchmarks, and frozen thresholds remain pending. |
| `0.2.3-draft.29` | Added bounded exhaustive response-pattern expected-information instrumentation for nonlinear MML fits under unit weights; verified probability mass, zero expected score, score-outer-product information, selected central differences, row-order invariance, missing-row semantics, latent-regression coordinates, nonunit-weight rejection, privacy, and execution limits. A degenerate one-node control demonstrates that exhaustive-pattern rank remains local and integration-rule dependent. Global structural arguments, scalable target-size methods, readiness effects, and frozen thresholds remain pending. |
| `0.2.3-draft.30` | Added exact Person-design reuse to bounded MML all-pattern expected information; canonicalized within-Person layouts, included active population-design rows in the signature, reconstructed information by group multiplicity, recorded conceptual versus evaluated workloads, matched a forced non-reuse reference, separated missing and distinct-covariate designs, and retained privacy. Unique long-design exponential cost, target-size benchmarks, readiness effects, and frozen thresholds remain pending. |
| `0.2.3-draft.31` | Began WP2 with a model-scoped category and step-support preflight; separated declared, observed, retained, free, fixed, derived, weak, and unsupported coordinates; derived the exact adjacent-step recession direction for zero-count internal categories under the sum-zero parameterization; kept boundary gaps in separate weak/element-boundary review; distinguished RSM shared-ladder from PCM/GPCM step-scope support; preserved binary no-free-step reduction; retained typed failure provenance and zero-row schemas in design evaluation; and added invariance and privacy controls. Weak-information calibration, structural-zero declarations, threshold anchors, multiple scales, WP4 propagation, WP5 external eligibility, and new-seed recovery remain pending. |
| `0.2.3-draft.32` | Began WP3 with a Person-scoped sufficient-score boundary audit on retained contributing observations; replaced finite optimizer-dependent primary values for standard free JML extremes with typed negative or positive infinity while retaining the iterate as a numerical trace only; retained contributing row and positive-weight totals; separated directly or implicitly fixed, constraint-coupled review, nonextreme JML, and finite prior-regularized MML/EAP cases; removed ordinary SE/CI eligibility from typed boundaries; and made FACETS-style endpoint placement explicit without creating a finite adjusted estimate. Generalized non-Person and interaction recession directions, a named optional adjustment, complete WP4 propagation, external eligibility, and new-seed recovery remain pending. |
| `0.2.3-draft.33` | Added a bounded linear-program certificate for JML additive structural recession with Person coordinates fixed; transformed the exact adjacent-category design into retained observed-versus-alternative contrasts; reused facet signs, direct/group anchors, centering, interaction, and step Jacobians; tested every expanded target in both directions; rejected structural-null-only candidates through strict-margin post-solve checks; retained direction loadings, execution-limit and dependency states, MML non-applicability, and public-table non-propagation. Joint Person movement, GPCM slope recession, sparse target-size execution, independent solver parity, WP4 propagation, and new-seed recovery remain pending. |
| `0.2.3-draft.34` | Replaced dense positive/negative-split LP constraints with the solver's sparse triplet interface; retained an explicitly bounded dense-reference route; recorded variables, constraints, sparse nonzeros, dense-equivalent elements, structural coordinates, target directions, and representation; added independent ceilings for each workload dimension; matched sparse and dense target capacities/statuses in two-Rater and checkerboard-interaction fixtures; retained two-Rater row-order invariance; matched checkerboard interaction directions to a test-only finite-grid oracle; and verified sparse construction on a 20,000-row by 100-coordinate synthetic matrix without allocating its dense equivalent. General independent solver parity, end-to-end target-size runtime/memory, joint Person movement, GPCM slope recession, WP4 propagation, and new-seed recovery remain pending. |
| `0.2.3-draft.35` | Added a companion joint Person-structural additive recession audit over the exact retained observed-category contrast cone; screens global cone existence before target enumeration; includes all free Person coordinates but targets only constraint-coupled extreme Persons unresolved by the sufficient-score rule plus all structural expanded targets; records global and target margin certificates and direction loadings; adds separate Person, structural, and total additive-coordinate ceilings; and defers the target-direction limit until a global ray is certified. A group-constrained two-Person/two-Item fixture proves that the joint ray can exist when both restricted subspaces have none, with sparse/dense, row-order, nonseparated, finite-grid, pre-screen coordinate-limit, post-screen target-limit, and MML non-reduction controls. Public state propagation, nonlinear GPCM slope recession, general independent solver parity, target-size runtime/memory, and new-seed recovery remain pending. |
| `0.2.3-draft.36` | Added a bounded retained-additive JML GPCM log-slope boundary-path audit. It derives monotonicity from observed maximum/minimum cumulative category utilities, exhausts ordered pair rays under sum-zero expanded log slopes, independently matches the retained likelihood to the optimizer objective, stores boundary likelihoods and expanded/free direction loadings, and separates scoped completion from global nonlinear structural identification. A fixed-Person checkerboard, direct objective-path oracle, row reversal, execution limit, and MML non-reduction controls pass; an unanchored checkerboard supplies the required negative counterexample in which no retained-point slope-only ray exists but a joint Person-plus-slope path improves. Public propagation, general joint nonlinear GPCM path coverage, broad properties, target-size evidence, and new-seed recovery remain pending. |
| `0.2.3-draft.37` | Began WP4 runtime propagation with one stored five-component fit record, deterministic precedence, a conservative scalar, controlled new reason codes, and a fail-closed legacy adapter. Fit, summary, result, convergence, and plot front doors consume the record; synthetic saved pre-contract objects cannot regain readiness from an old Boolean, and the exposed contract identifier contains no internal work-package label. Boundary aggregation distinguishes genuinely unpropagated structural/slope targets from a joint cone explained entirely by Persons already typed as unbounded. Complete parameter-primary, report/export/replay, real serialized migration, comparison-eligibility, and confirmation work remains pending. |
| `0.2.3-draft.38` | Revised the readiness contract to v2 and added GPCM slope parameter rows. Certified retained-additive JML paths map to low, high, or two-sided (`unbounded_both`) typed boundaries with extended-real or undefined primary values; scoped negative JML results and free-slope MML results remain applicable-not-evaluated rather than finite claims; unit-slope reductions are fixed. Finite optimizer estimates and local covariance intervals remain explicitly named traces, ordinary SE/CI and numeric comparison eligibility fail closed, and free-slope GPCM fit readiness remains review until estimator-specific joint-JML or marginal-MML boundary work is complete. Other parameter classes, downstream enforcement, and external eligibility remain pending. |
| `0.2.3-draft.39` | Closed the GPCM summary-level primary/optimizer ambiguity and decomposed cross-software GPCM differences by kernel, active design, steps, slopes, identification, estimator, global geometry, information, support, diagnostics, and output transformation. Added the prespecified covering stress envelope spanning two-rater panels, sparse/missing/weighted designs, category imbalance, interactions, bias, residual PCA, slope regimes, and target scale. Restricted FACETS, TAM, and immer to estimand-eligible roles instead of treating similarly named discrimination output as interchangeable. Executable grid completion, estimator-specific global boundary work, numeric criterion freeze, and confirmation remain pending. |
| `0.2.3-draft.40` | Added the first bounded joint nonlinear JML GPCM path family: ordered `+1`/`-1` log-slope rates with simultaneous constrained linear additive movement, sparse LP sufficient conditions, analytic boundary likelihoods, and candidate-specific contract-v3 reasons. The unanchored checkerboard supplies a positive case missed by the slope-only check; balanced repeated outcomes, row reversal, workload limits, direct objective paths, and MML non-reuse provide negative and scope controls. Positive candidates remain without a global primary value, and negative results do not imply finite slopes. General rate vectors, curved paths, marginal-MML boundary work, target-scale evidence, and confirmation remain pending. |
| `0.2.3-draft.41` | Implemented the internal GPCM stress covering-grid runner: four estimator/lower-model modes, 12 total axes, 12 mandatory adversarial corners, 70 pilot cells, and complete coverage of all 1,330 two-axis level pairs. Added disjoint smoke/pilot/confirmation seeds, manifest and retained-data hashes, explicit one-slope-level generator non-executability, missingness/category/topology/cell/interaction/diagnostic transforms, false-ready and primary-versus-optimizer reporting, and an exploratory residual-PCA lane. Initial deterministic smoke controls show zero false-ready results for zero-shared-Person and internal-zero-category cases and reach residual PCA for an Occasion-distinguished local-dependence case. External numeric comparison, thresholds, and every current run remain ineligible, unfrozen, and calibration-only. Full replicated pilot, target-scale evidence, matched external normalizers, candidate freeze, and confirmation remain pending. |
| `0.2.3-draft.42` | Added an isolated one-axis attribution manifest around one fixed GPCM reference and four paired analysis routes per retained dataset. The five-replicate pilot has 40 arms and 800 route rows, common pilot seeds, a disjoint reserved confirmation range, parameter-class coordinate flags, separate JML/MML Person estimands, exact-unit-slope versus misspecified PCM roles, truth-aligned Person/facet/step metrics, optimizer-only slope diagnostics, and a fail-closed full-pilot resource authorization. A 24-row smoke spanning the reference, two raters, internal category zero, zero shared Persons, Person-by-rater interaction, and local dependence produced zero route-data identity violations and zero false-ready rows; two zero-shared-Person JML rows failed closed. Ready additive PCM rows under planted dependence are retained as evidence that numerical readiness is not model adequacy. All slope-primary, external-numeric, threshold, recovery, diagnostic, and confirmation decisions remain ineligible or unfrozen. |
| `0.2.3-draft.43` | Added guarded feasibility, core, and expanded replicated-attribution tiers, complete four-route/hash ledgers, Wilson intervals, numeric and Bernoulli Monte Carlo summaries, and runtime forecasting. The authoritative corrected feasibility run completed 80 of 80 routes and 20 of 20 data cells with zero identity violations, fit failures, false-ready rows, primary-slope eligible rows, or external-numeric eligible rows. Filtered-row arms exposed a common MML EAP Person-order defect; commit `655f6bf` aligns EAP and posterior-SD summaries by Person index and adds row-reversal invariance coverage. Pre-fix Person and EAP-derived diagnostic evidence is invalidated, while identical hashes show structural recovery and readiness were unchanged. A missing `lpSolve` run separately demonstrated dependency-sensitive fail-closed behavior; authoritative v4 used `lpSolve` 5.6.23. With only two replicates, wide Wilson intervals, no atomic resume, incomplete metamorphic grids, unmatched external normalizers, and open recovery/coverage and diagnostic calibration, no criterion, candidate, or confirmation state is frozen. |
| `0.2.3-draft.44` | Added atomic checkpoint/resume at the complete four-route data-cell boundary. Checkpoint-v1 identity combines selected and declared manifest hashes, actual execution controls, loaded mfrmr runtime content, the three validation runners, R/platform/RNG, numerical runtime reporting, and dependency availability/version/content. Verified same-directory temporary files are renamed into place; existing targets and unexpected RDS files fail closed. Resume validates schema, execution, cell-manifest, result payload, ScenarioId, route, DataCellId, and declared-manifest identity. Aggregate output receives a separately atomic completion marker whose artifact inventory and hashes must remain valid. Deterministic interruption, clean-run equality, mismatch, orphan-partial, path-traversal, and adulteration tests pass, and a real four-route reference cell matches the prior execution path except for elapsed runtime. The draft.43 artifacts remain historical and non-resumable. Metamorphic, target-scale, replication/precision, external, statistical-freeze, candidate, and confirmation gates remain open. |
| `0.2.3-draft.45` | Added a guarded ten-transformation by three-model MML metamorphic grid. The runner distinguishes input provenance from retained-data/result invariance, hashes declared and selected manifests separately, compares semantic output keys and quantities, and requires both fits to be numerically ready. A loose-control v1 failed five relabelling comparisons at terminal-gradient review and was retained as a superseded diagnostic rather than repaired by wider tolerances. The production-control v3 passed 30/30 with zero warnings and maximum objective, parameter, and observation differences of `3.112007e-09`, `1.862395e-05`, and `6.113984e-06`. The final runner also refuses existing output directories and deduplicates model requests; v2 retained identical numeric maxima before that storage guard. This is pilot software-property evidence only; thresholds remain unfrozen, and target-size, JML/replay/active-structure, replication/precision, external, candidate, and confirmation gates remain open. |
| `0.2.3-draft.46` | Reassessed the 87-row inventory after the MML metamorphic pass. Introduced `release_spine`, `claim_conditional`, and `deferred` portfolios so optional research does not create an accidental infinite release scope, while an unsupported callable result cannot escape through a generic caveat. Unblocked stable metric-specific WP5 fixtures, noninferential WP6 target-size work, and WP7 precision/manifest prespecification for parallel calibration; confirmation remains blocked until a machine-readable item profile, fallbacks, dependencies, criteria, and candidate identity are frozen. Rechecked official mfrmr/TAM/immer/FACETS versions without changing the local FACETS 4.5.0 execution stratum. No evidence row passed and no criterion was frozen. |
| `0.2.3-draft.47` | Added a guarded capacity-feasibility executor for all six already-declared executable 400-Person `target_sparse` cells. The run covers GPCM/PCM, JML/MML, sparse/disconnected assignment, 2--12 Raters, missingness, category imbalance/absence, weights, Occasion, interaction, bias/drift, local dependence, and residual PCA. All six cells executed with zero unexpected failures and zero false-ready rows; the mixed-adversity GPCM MML cell remained iteration-limited/blocked, two exact-rank controls failed before optimization, one PCM JML cell retained extreme-Person exclusions, and one imbalanced/missing PCM MML cell was ready but not statistically validated. The runner distinguishes one capacity replicate from five declared pilot replicates, refuses overwrite, binds runtime/manifest/artifact identity, and validates the bundle in a fresh session. Residual-PCA messages exposed a missing computability state, and R heap high-water values remain proxies rather than OS capacity evidence. No threshold, checklist row, FACETS-parity claim, candidate, or confirmation state was promoted. |
| `0.2.3-draft.48` | Added a guarded 13-cell/26-route target baseline and bridge pilot. Complete balanced and clean matched-sparse 400-Person RSM/PCM/GPCM cells use identical data across JML/MML; a PCM two-Rater gradient varies 0--40 common Persons under one truth hash. All routes executed with zero unexpected failures and zero false-ready rows. Clean sparse JML routes were iteration-limited and hundreds of seconds; MML partners were much faster but retained gradient/boundary review. Zero-overlap JML failed closed, zero-overlap MML remained population-assumption-linked, and positive-overlap traces were nonmonotone. OS process-lifetime peak memory is now recorded, PCA remains disabled pending its computability contract, the differently seeded v1 bridge output is superseded, and no recovery, overlap, runtime, memory, external, candidate, or confirmation criterion is promoted. |
| `0.2.3-draft.49` | Added a guarded 14-cell/34-route PCM JML bottleneck decomposition across Person/row growth, fixed-row Rater panel/topology, fixed-row Criterion/step growth, fixed-parameter exposure, forced extremes, paired MML controls, and explicit optimizers. P200/P400 auto L-BFGS-B blocked while explicit BFGS was ready, but R12, C12, and extreme BFGS controls remained blocked. Sparse low exposure was slower and less conditioned than a larger cell, while the largest-row cell was slowest but ready; no single monotone capacity rule survived. All routes executed with zero unexpected failure and zero false-ready state. Total elapsed/evaluation is explicitly a proxy including audits. Phase timing and replicated cross-model controls precede any optimizer change; no runtime, recovery, external, candidate, or confirmation criterion is promoted. |
| `0.2.3-draft.50` | Added opt-in, diagnostic-only 18-phase timing after all statistical and readiness decisions, with timed-versus-untimed semantic invariance tests. A fixed seven-cell/19-route subset of Draft.49 completed with all timing contracts, zero false-ready rows, and unchanged semantic hashes/readiness/optimizer states across progressively refined v1--v3 bundles. Structural and joint recession audits consumed 201.94 of 211.25 instrumented JML seconds, versus 3.70 seconds for optimization. Every structural audit enumerated 46--126 target directions and returned `none_certified`; joint target enumeration occurred only after its global cone screen certified a ray. The next corrective slice tests a structural cone prescreen under exact sparse/dense, certificate, fail-closed, and frozen-route equivalence. The optimizer-dispatch grid remains separate, and no runtime, solver, recovery, external, candidate, or confirmation rule is promoted. |
| `0.2.3-draft.51` | Implemented the versioned structural global-cone prescreen with fail-closed solver/size states and actual cone/target LP-call counters. Negative cones skip redundant target enumeration; positive cones retain it. Adversarial review identified a near-boundary false-negative risk in the ordinary target LP cutoff, added a `5e-7` counterexample, and guarded cone exclusion at objective tolerance `1e-10` versus certificate tolerance `1e-7`; v5 supersedes pre-guard v4. Integrated positive/negative, sparse/dense, row-order, anchor, interaction, retained-row, size-limit, MML, and injected solver-failure controls pass. The fixed v5 profile preserves all 19 v3/v4 semantic/readiness/boundary states and all 12 same-fit screened/unscreened structural target-status hashes. The selected negative-cone PCM routes replace 908 target LP calls with 12 cone LP calls, reduce structural phase time 90.1%, and reduce JML outer time 59.3%; all 12 JML routes are faster. All 127 package-aware test files pass, and the exact local tarball passes `R CMD check --no-manual` with `Status: OK`; this is not a complete `--as-cran` or candidate check. These one-run timings freeze no criterion. Joint cone/target attribution, target-scale positive cones, RSM/GPCM coverage, replication, capacity, recovery, external, candidate, and confirmation work remain pending. |
| `0.2.3-draft.52` | Added fail-closed joint cone/target workload counters to phase schema v6 and preserved every v5 semantic/readiness/boundary state across the fixed 19 routes. Seven negative cones used no target LPs; five positive cones consumed 48.99 of 62.52 joint-phase seconds and triggered 346 target LP calls with zero target certificates. A separate guarded three-cell attribution runner projected each cone through the full target Jacobian: all 43 nonzero coordinates were ordinary free extreme Persons already typed by the sufficient-score audit, with zero structural and zero selected-target change. The readiness guard already prevents these Person-only cones from becoming unpropagated candidates. A diagnostic row-and-coordinate quotient was negative in all three cells, but cannot become a fast path until selected-target quotient-null directions are excluded under guarded rank tolerances. No core estimator, boundary, readiness, public API, roadmap, runtime threshold, candidate, or confirmation rule changed. RSM/GPCM coverage, positive constraint-coupled controls, flat-direction counterexamples, fixed-route equivalence after correction, replication, capacity, recovery, external, candidate, and confirmation work remain pending. |
| `0.2.3-draft.53` | Added a verified ordinary-free-extreme-Person row-and-coordinate quotient plus a common-column-scaled selected-target nullspace rank screen to the joint additive JML audit. A negative quotient skips target enumeration only when base and target-augmented sparse-QR ranks are stable and equal at `1e-12`, `1e-10`, and `1e-8`; every uncertain mapping, ray, solver, rank, tolerance, or size state retains legacy work or fails closed. A flat target-changing null-direction counterexample forces fallback; RSM, row-order, target-limit, structural-positive, constraint-coupled, interaction, bounded-GPCM conditional-additive, MML, readiness, and release-readiness controls pass. Authoritative v8 preserves all 19 v6 semantic/readiness/boundary/target-status comparisons, replaces 346 joint target LPs with five quotient LPs, and reduces joint phase time 69.9% and JML outer time 50.0%; all five exclusion routes print zero rank increment across three tolerances. v7 is superseded for incomplete rank observability. The clean exact 491-entry source tarball passes `R CMD check --no-manual` with `Status: OK`; the earlier 497-entry artifact is superseded because it included six hidden change-local scripts. These one-run PCM timings freeze no criterion, and the standard local check is not an `--as-cran` or candidate gate pass; broad model/target-scale, shared-geometry, replication, capacity, recovery, external, candidate, and confirmation work remain pending. |
| `0.2.3-draft.54` | Added a guarded shared-geometry fast path for the JML structural and joint recession audits. The full adjacent design, expanded-target system, and observed contrast are built once, validated, projected exactly onto non-Person rows/columns for the structural audit, and reused by the joint audit; malformed or failed shared construction falls back to legacy work, MML bypasses the path, and no fit schema changes. Whole-audit identity tests cover direct/group anchors, interactions, RSM, criterion-step PCM, weighted/missing retained rows, bounded-GPCM conditional-additive geometry, and injected failure. A component profiler restores namespace bindings before legacy comparison and passes all 19 phase/component/equivalence contracts with zero false-ready rows. Against a same-day canonical Draft.53 replay, structural-plus-joint time falls from 33.00 to 21.07 seconds and JML outer time from 44.12 to 31.71 seconds; all 12 JML routes are faster, while one-run timing freezes no rule. Adversarial comparison also exposed that serialized object hashes could differ for `identical()` target tables whose R internal representation differs; canonical-v1 now hashes type-explicit values and normalizes CSV blank versus in-memory NA. The clean exact 492-entry source tarball passes `R CMD check --no-manual` with `Status: OK`; this standard local check is not an `--as-cran` or candidate pass. Contrast construction is the largest remaining component, ahead of LP solving. Replicated/general model and target-scale performance, independent solver parity, recovery, external, candidate, and confirmation work remain pending. |
| `0.2.3-draft.55` | Replaced repeated observed-contrast triplet-vector growth with exact stored-entry counting, score-specific transition templates, and one preallocated block fill per observation while retaining the former algorithm as an internal reference. A 977-expectation suite proves exact `dgCMatrix` and shared-geometry identity across zero/dense/sparse designs, 1--10 steps, outcome extremes and imbalance, malformed inputs, observation permutation, zero columns, two-Rater weighted/missing direct/group-anchored PCM, interaction RSM, and bounded GPCM. The final guarded five-case/seven-replicate v3 bundle passes all output, artifact, and runtime-identity contracts; v1 is superseded for staging-path row-name leakage and v2 for a distinct package-hash convention. Median constructor time falls 85.7--99.7% and cumulative R allocation 93.3--99.96%, but the latter is not peak RSS. The fixed 19-route component rerun preserves all canonical semantic/readiness/boundary comparisons with zero false-ready rows; contrast time falls from 10.28 to 0.11 seconds, combined structural/joint time from 21.07 to 10.64 seconds, and JML outer time from 31.71 to 21.34 seconds, with all 12 JML routes faster. MML is unaffected. The clean exact 493-entry source tarball passes `R CMD check --no-manual` with `Status: OK`; the standard CRAN-light path has 397 passing expectations and three skips. A hash-verified 10-shard regression covers the same tarball's complete 126-file inventory exactly once, with 1,726 tests, 11,945 passing expectations, 83 skips, 38 allowlisted warnings, and zero failures, errors, or unexpected warnings. The monolithic full-regression attempt reached 30 minutes without completing and is not a pass or failure; the sharded result does not upgrade the standard check. Candidate and `--as-cran` gates remain open. LP solver calls now consume 75.6% of measured JML component time, so Draft.56 performs attribution and independent parity before any solver intervention. No numeric criterion, candidate, or confirmation state is promoted. |
| `0.2.3-draft.56` | Added repository-only nested timing around LP-base assembly, `mfrmr_jml_recession_run_lp()`, and `lpSolve::lp()`, plus an independent sparse GLPK translation and post-solve certificate that cannot affect fit results. Authoritative v3 preserves all 19 Draft.55 fixed-route canonical comparisons with zero false-ready rows and zero MML LP events. Forty bases generate 48 production calls; base construction takes 0.06 seconds, run-LP work 8.64 seconds, solver execution 8.58 seconds, and assembly/dispatch 0.06 seconds. v2 records a 99.77% solver share versus v3's 99.31% while classifications and capacities are identical, so no sub-percentage timing claim is frozen. All 40 independent comparisons agree: 32 negative cones, eight certified directions, and maximum capacity difference `1.421085e-14`. Thirteen sparse/dense, guarded/default near-boundary, flat-direction, and forced-failure controls pass. Four added controls cover two-Rater missing/weighted RSM, interaction RSM, two-Rater strongly imbalanced bounded GPCM with protected category support, and sparse-panel bounded GPCM; every baseline/instrumented semantic hash, readiness state, and independent comparison matches. The initially overaggressive imbalance generator correctly triggered the unsupported internal-category contract before optimization and was replaced with a protected support spine. v1 is superseded as PCM-only; v2 is superseded because v3 adds runtime-content hashes for all solver capabilities. The profile is single-run and fixed-order; GLPK timing cannot select a solver, bounded-GPCM evidence is conditional-additive rather than a global nonlinear slope proof, dependencies and dispatch remain unchanged, and no criterion, candidate, or confirmation state is promoted. Draft.57 performs alternating replicated solver qualification, status mapping, generated-property parity, and isolated-process memory before any intervention. |
| `0.2.3-draft.57` | Added guarded exact reacquisition of the Draft.56 40-target ledger, balanced alternating seven-replicate comparison, eight generated transformations over positive/negative PCM/RSM/GPCM sources, actual and injected failure-status controls, and six fresh-process memory cells. All 560 timed calls and 280 pairs reproduce production classifications and capacities. GLPK is faster in this calibration but fails the frozen positive-RSM `1e-3`--`1e3` row-scaling property with status 1 while `lpSolve` retains certified capacity 147; property parity is 94/96. `lpSolve` status 0 with value `1e30` on an unbounded fixture and Rglpk's undifferentiated status 1 also prevent failure-status qualification. All unsafe states fail closed, all memory workers complete, and the 18-artifact bundle validates, but `CandidateQualified`, `SolverDispatchEligible`, runtime/memory rules, dependencies, criteria, and confirmation remain false or unfrozen. Draft.58 tests solver-neutral normalization and real child-process deadline provenance; no production or public surface changes. |
| `0.2.3-draft.58` | Added a guarded six-source by six-scale-exponent solver-normalization ladder over raw and L1 row-normalized formulations for `lpSolve` and GLPK. All 144 rows preserve source, original-scale, solver-formulation, and row-scale provenance. Raw qualification is 66/72: GLPK fails closed on one PCM, three RSM, and two bounded-GPCM positive joint-cone rows at larger exponents. Solver-only L1 normalization qualifies 72/72 while retaining the original strict objective, theoretical box bound, and original-scale certificate. The formerly failed RSM cell reproduces 12/12 parent expectations in fresh processes; 4/4 actual child-process deadline controls observe start, complete success children, and terminate forced child trees with exit 15 and no output. `NormalizationCandidateQualified` is true only for this bounded calibration. Solver candidacy, production change, dispatch, dependency, runtime/capacity criteria, and confirmation remain false or unfrozen because failure-class specificity, broader target-scale cones, nonlinear GPCM geometry, and fit-level validation remain open. The solver-local branch stops unless model-level evidence demonstrates preventable supported-fit failures; no public or production surface changes. |
| `0.2.3-draft.59` | Added three topology- and exposure-matched 400-Person RSM/bounded-GPCM designs, paired ordinary/capture fits, 16 serialized exact recession problems, 64 raw/normalized `lpSolve`/GLPK comparisons, and ten selected timeout-zero attribution references. All solver rows fail closed safely, but provenance is 61/64, raw and normalized `lpSolve` qualify 15/16 and 13/16, and only five of six fit pairs preserve semantic/readiness/boundary identity. The random-sparse RSM joint state changes from `not_evaluated_solver` to `certified_recession`; timeout zero changes three of ten selected two-second outcomes. Target normalization and observed production need are false, while native-timeout instability and replay investigation are true. The result rejects normalization rather than selecting a solver or timeout policy. Production code, dependencies, public surfaces, thresholds, criteria, and confirmation remain unchanged or unfrozen. Draft.60 must replay frozen positive, failed, and negative RSM problems in fresh processes under bounded capacity/strictness policies and parent OS deadlines. |
| `0.2.3-draft.60` | Added seven exact Draft.59 positive, failed, and negative problems under current two-second, bounded two-then-eight-second retry, bounded single-ten-second, and OS-bounded native-zero policies with four order-balanced fresh-process repetitions. All 112 workers complete, all results are safe, no parent deadline fires, and 27/28 policy-by-problem cells are stable. The current policy is unstable on problem 8 and reference-inconsistent on problems 8 and 13. Both bounded candidates are stable and reference-matched on all seven problems and preserve 32/32 negative-control outcomes. Fifteen two-second stage calls return status 1 and fail closed; seven required eight-second retries return accepted original-scale solutions. Both candidates advance only to fit-level comparison. No solver, normalization, status reinterpretation, native-zero production use, policy selection, production change, runtime freeze, blocker closure, checklist promotion, or confirmation is authorized. |
| `0.2.3-draft.61` | Added the six exact Draft.59 complete, balanced-sparse, and random-sparse RSM/GPCM fits under current two-second, bounded retry, bounded single-ten-second, and OS-bounded native-zero policies with three fresh-process repetitions. All 72 workers and fits complete safely, no parent deadline fires, optimizer hashes are invariant, and 23/24 policy-by-route cells are stable. The current policy is stable on 5/6 routes and reference-matched on 4/6; balanced-sparse RSM is a stable false negative and random-sparse RSM is unstable. Both candidates are stable and reference-matched on 6/6 routes. Their call sequences agree in 18/18 paired cells; at an equal 20-second maximum positive-target native bound, single-ten-second has no more attempts in 18/18 cells and fewer in six, totaling 66 versus 77. The prespecified consequence rule selects single-ten-second only as the Draft.62 implementation candidate. Production change, runtime/capacity freeze, replay-blocker closure, checklist promotion, and confirmation remain unauthorized; nonlinear GPCM, PCA, ADEMP, external, and release-engineering work remain open. |
| `0.2.3-draft.62` | Implemented `mfrmr-jml-recession-fit-policy-v1` for additive structural/joint audits: one ten-second attempt per stage, no retry, and a 20-second maximum native allowance for a positive target. The nonlinear GPCM joint-pair audit remains on its independently scoped two-second default. Across 18 fresh production-native target-scale fits, all workers complete safely without a parent kill; 51 native target calls and 66 solver attempts reproduce the selected Draft.61 candidate's result and call-outcome identities in every matched cell, with all six route cells stable. The final exact tarball (`7ceb2848...d92624`) passes 129/129 sharded non-CRAN regression files with zero failures/errors. Its temporary-filesystem `R CMD check --no-manual` has no WARNING and one offline optional-Suggests Rd-cross-reference NOTE; it is not `Status: OK` or a release-candidate check. The additive replay blocker is resolved; runtime/capacity criteria, checklist promotion, nonlinear GPCM, PCA, ADEMP, external evidence, candidate freeze, and confirmation remain open or unauthorized. |
| `0.2.3-draft.63` | Adopted a machine-readable generalized-MFRM identity contract without changing the implemented likelihood. The current bounded route is now explicitly the aligned single-owner relative-slope GPCM. Criterion-owned and rater-owned evidence have distinct scenario IDs; the legacy pooled `NUM-GPCM-BOUND` row is calibration-only and non-promoting. Fit-null, fit-nonnull, uniform-DFF, nonuniform-DFF negative-control, rater-category, slope-absorption, response-style, repeated-rating, decoupled-owner, multiplicative-slope, severity-interaction, and multidimensional scope rows are registered. Five checklist rows raise the current inventory from the historical Draft.46 count of 87 to 92. Every future GPCM result must retain slope owner, step owner, slope composition, latent dimension count, ability-scale contract, and actual runtime identity. Existing Draft.62 policy evidence is retained; no prior pilot is relabelled, no checklist criterion or numeric threshold is frozen, and confirmation remains unauthorized. |
| `0.2.3-draft.64` | Added a repository-only owner-specific GPCM runner and exact schema tests. The 16-cell smoke manifest crosses criterion/rater owners, JML/MML, and four support designs with the Draft.63 identity fields plus loaded-package, runner, contract, seed, retained-data, and manifest hashes. A six-cell guarded execution proves that the same aligned kernel can be dispatched with criterion-owned and rater-owned slope/step blocks and that rater-owned zero-common-Person and internal-zero-category controls cannot become evidence-ready. All requested owner identities match, all six fits return typed results, and final false-ready count is zero; observed slope RMSE and readiness states are calibration traces only. The authorization-guarded pilot has 24 owner/estimator/design cells and five disjoint seeds per cell, for 120 run rows; it adds workload and range restriction but remains unrun. No existing evidence is promoted, no public or package likelihood/readiness surface changes, and confirmation remains unauthorized. |
| `0.2.3-draft.65` | Froze the owner-specific pre-pilot execution and precision contract. The 120-row declared manifest now enters one loaded-runtime/runner/model-contract/execution-contract/control identity shared by deterministic modulo shards. Every selected row remains in Bernoulli denominators; summaries add plug-in MCSE, 95% Wilson intervals, and finite-value numeric MCSE with explicit missing, fit-failure, and evidence-ready counts. Atomic one-row checkpoints validate execution, exact row, scenario/design cell, and result hashes; resume refuses existing unapproved sets, orphan files, and any altered payload. Aggregate publication requires the exact declared scenario order, one valid checkpoint per row, and a completion marker with a relative-path/SHA-256 inventory. Unit controls cover shard union/disjointness, no ad hoc subset sharding, interruption/resume, payload adulteration, denominator retention, and completion validation. The live pilot remains authorization-guarded and unrun, no outcome-adaptive stopping or confirmation is permitted, and no numeric criterion or checklist row is promoted. |
| `0.2.3-draft.66` | Corrected and completed the owner-specific calibration pilot. The first 120-row Draft.65 execution exposed that observed 1/3/4 internal-zero data were automatically recoded to 1/2/3 before fitting; its 20 affected rows and whole promotion bundle are superseded. Draft.66 fixes `keep_original = TRUE`, assigns a new runner/contract/execution identity, and completes 120/120 rows with a validated 130-artifact completion inventory. All 20 internal-zero cells now fail closed before optimization, all ten zero-common-Person JML cells remain structurally unidentified, ten zero-common-Person MML cells remain population-assumption-linked guarded controls, and two additional JML cells retain slope-optimization failure. The other 100 rows match Draft.65 on all 52 non-identity fields. There are zero identity violations, raw/evidence inference-ready rows, and raw/final false-ready rows. Core optimizer log-slope RMSE is small in this design, while weak-bridge JML is severely degraded; five-replicate Wilson intervals and MCSE remain too wide for a threshold. The exact identities, aggregate hashes, correction provenance, cell summaries, limitations, and next lanes are retained in `gpcm-owner-specific-pilot-record-0.2.3.md`. No checklist row, numeric rule, candidate, or confirmation state is promoted. |
| `0.2.3-draft.67` | Attributed the two unexpected owner-pilot JML failures without outcome-dependent repair. A fixed 40-dataset/two-optimizer common-data panel shows that L-BFGS-B retains both failed fits but does not resolve criterion-owned weak-bridge geometry: its five criterion weak-bridge fits all fail convergence with maximum slopes near 1,462--3,168, while equal-iteration optimizer differences outside weak bridge are essentially numerical. Exact expansion traces distinguish a late weak-link boundary excursion from an early workload-imbalance line-search accident. The implementation now updates parameter caches transactionally and converts only typed non-representable GPCM slope trial proposals to a finite dominating direct-optimizer objective; other errors and invalid retained vectors still fail hard. A full 40-row BFGS recheck returns every fit, records rejection only on the two historical rows, reproduces the other 38 objectives within `4.55e-12`, restores the workload case to a finite convergence pass, and retains criterion weak bridge as convergence-failed with a maximum slope near 271,874. Exact historical/recheck identities and gate consequences are in `gpcm-owner-jml-optimizer-attribution-record-0.2.3.md`. Artifact retention improves, but no finite maximum, recovery margin, owner contrast, threshold, checklist pass, or confirmation state is established. |
| `0.2.3-draft.68` | Completed the separately identified MML integration-sensitivity lane. Forty exact owner-pilot datasets cross criterion/rater ownership, core/weak-bridge/range-restricted/zero-common-Person designs, and five replicates; each receives fixed q=31/61/91 direct-MML fits and a common q=91 candidate evaluation. All 120 arms and 40 atomic dataset checkpoints complete, 113 arms pass the gradient gate, seven code-zero arms remain numerical review, no slope proposal leaves the representable range, and no arm is inference-ready. q=31 common-grid regret reaches 0.5443; q=61 regret is at most 0.001471. Relative to q=31, q=91 changes reach 0.0914 in identified log slopes, 0.1424 in facet locations, 0.6011 in steps, 0.0745 Person-EAP RMSE, and 0.0591 posterior-SD RMSE. More nodes do not monotonically improve optimizer state, small common-grid regret does not guarantee Person-estimand invariance, and all zero-common-Person arms remain evidence-blocked under the population-assumption contract. Exact identities, summaries, and limitations are retained in `gpcm-mml-integration-sensitivity-record-0.2.3.md`. q=31 remains a selection starting grid rather than a sufficiency certificate; direct q61-to-q91 parameter tolerance, expanded replication, marginal-MML slope geometry, thresholds, checklist promotion, and confirmation remain open. |
| `0.2.3-draft.69` | Added estimator-specific marginal-MML GPCM slope-boundary instrumentation for the implemented finite quadrature objective. The audit derives the Person-pattern marginal derivative as a posterior-node-weighted conditional derivative, enumerates every ordered constant sum-zero two-level log-slope ray, requires nodewise utility-max/min compatibility, independently reconstructs the boundary likelihood, and records exact dimensions, loadings, and failure states. A constructed sparse positive control follows its direct likelihood oracle to the computed boundary; row-order invariance, ordinary none-certified behavior, likelihood reconstruction, and bounded execution-limit failure are covered. The result is expressly a sufficient fixed-node path certificate, not a continuous-integral, joint-coordinate, finite-MLE, uncertainty, recovery, fit, DFF, or sample-size result. Exact implementation and distribution-check provenance is retained in `gpcm-mml-slope-boundary-implementation-record-0.2.3.md`. Its readiness effect is none: MML optimizer slopes remain numerical traces, all free MML slope primary statuses remain `not_evaluated`, and no checklist row, threshold, default, candidate, or confirmation state is promoted. |
| `0.2.3-draft.70` | Completed the retrospective marginal-MML boundary/grid calibration on the exact 40 Draft.68 datasets. All 120 q=31/61/91 arms and 40 atomic checkpoints complete, all retained-data hashes match the source panel, and Draft.68 own-grid/common-q91 likelihoods are reproduced with maximum difference zero after adding Draft.69 instrumentation. Every arm has complete fixed-quadrature scope, exact likelihood reconstruction, and state `none_certified_fixed_quadrature_marginal`; state, certified direction set, and target status are stable across q for 40/40 datasets, but zero positive cases means positive-path sensitivity and false-negative behavior remain unknown. Direct q61-to-q91 maxima are 0.001471 common-grid regret, 0.00619 log slope, 0.0132 facet, 0.0553 step, 0.00552 EAP RMSE, and 0.00790 posterior-SD RMSE. A separately sourced validator verifies the full 49-file inventory, checkpoints, frozen manifest, Draft.68 linkage, and non-confirmation controls. Because all outcomes are retrospective and only five replicates populate each owner/design cell, no value is an equivalence threshold, q default, recovery rule, or sample-size result. Positive/negative challenge calibration and a fresh owner panel must precede any tolerance or readiness propagation. No checklist row, numeric criterion, candidate, or confirmation state is promoted. |
| `0.2.3-draft.71` | Completed the predeclared deterministic marginal-MML boundary challenge across criterion/rater ownership, forward/reverse positive patterns, mixed negatives, and zero-versus-`1e-8` discordant weights at q=31/61/91. All 30 fits, calculations, and likelihood reconstructions complete; no arm becomes evidence-ready. The 12 negative expectations pass, but all 18 positive expectations fail and every dense-grid arm is none-certified. The q=5 reference remains certified only for that exact coarse numerical objective. Gaussian--Hermite node extrema expand from about +/-2.86 at q=5 to +/-9.89, +/-14.50, and +/-18.02, breaking the individual-response all-node utility-extremum condition under finite additive coordinates. The result blocks readiness propagation and cannot be relabelled as evidence of finite slopes: it demonstrates insufficient dense-grid detection by this narrow sufficient condition, not absence of other recession paths or existence of a finite maximum. A separately sourced validator verifies the 17-file bundle, regenerated inputs, atomic checkpoints, frozen expectations, and non-promotion state. The next slice must derive a broader Person-marginal half-line certificate that permits conditionally adverse outer nodes to lose posterior mass, with continuous-normal and moving-additive claims kept separate. No threshold, q default, checklist row, candidate, or confirmation state is promoted. |
| `0.2.3-draft.72` | Derived and implemented a repository-only analytic oracle for the broader finite-q Person-marginal slope pair path. Observation-level softmax utility moments produce exact path derivatives; posterior-node averaging and the conditional-derivative variance produce exact curvature. A generalized boundary calculation removes positive-incompatible nodes, retains maximizing ties, makes the negative group uniform, and computes the leading negative-slope tail coefficient over the surviving-node posterior. On the Draft.71 criterion-forward q=31 control, 36 Person-node derivative cells are negative while all 12 Person marginals and the total derivative are positive over selected t=0--8 values. Reconstructed likelihoods match exactly, analytic first derivatives match central likelihood differences within about `3e-12`, and curvature matches central differences of the analytic derivative within about `2e-15`. Boundary improvement and the tail coefficient are both about `9.0542e-9`; t=10 behavior agrees, and q=61/91 retain 58/61 and 81/91 compatible nodes per Person rather than discarding the direction. This verifies formulas and exposes the adverse-node posterior-mass mechanism, but it is not a half-line proof: selected grids, ordinary floating point, and an unbounded tail approximation cannot certify nonnegativity. `HalfLineCertified`, `TailCertified`, and readiness effects are false. Outward-rounded compact-interval bounds, rigorous tail remainder, sign counterexamples, and broader owner/polytomous challenges remain blockers. No production code, threshold, default, checklist row, candidate, or confirmation state changes. |
| `0.2.3-draft.73` | Added a repository-only extended-JML profile-limit prototype for independently free typed extreme Persons. It proves the reduced objective identity by removing only likelihood contributions whose signed-theta limit is zero, reoptimizes the retained coordinates, and reconstructs finite original-JML vectors along signed caps without overwriting raw fits. Selected RSM/PCM/GPCM paths are monotone and end within `5.92e-12` of the reduced supremum; anchored extremes remain fixed and constraint-coupled extremes fail closed. Every result states that the original finite full-vector maximum is unattained and that the profile result is not finite-item bias correction. The prototype has no readiness effect; recovery, uncertainty, sparse/weak-link calibration, external JML convention comparison, public integration, numeric criteria, checklist promotion, candidate selection, and confirmation remain pending. |
| `0.2.3-draft.74` | Froze and completed the first paired raw-finite-JML versus extended-profile-limit structural recovery pilot. Ninety RSM/PCM/aligned Criterion-owned GPCM datasets cross two Person-exposure levels, three forced-extreme fractions, and five seeds. All fits and pairings complete, all forced Persons have the correct signed unbounded state, 68 profile paths verify, 22 no-free-extreme cases use an explicit no-op, and false-ready count is zero. The 2,700 recovery rows exclude Persons and leave profile SE/coverage unavailable. Maximum aligned raw/profile change is `5.94e-6`, and recovery RMSE agrees at reported precision: the profile operation resolves boundary non-attainment but does not become finite-item bias correction. Five replicates, narrow crossed designs, one GPCM owner, and no weighting/missingness/weak-link/external modes prevent numeric freeze or estimator selection. No public surface, threshold, checklist state, candidate, or confirmation changes. |
| `0.2.3-draft.75` | Source-audited the installed TAM 4.3-25 and immer 1.5-13 JML modes and completed the first common-estimand RSM/PCM normalization smoke. The runner retains exact loaded-function hashes, arguments, Person-extreme handling, postscale definitions, and all 36 planned method rows. Thirty-two modes return finite surfaces; the four TAM `adj = 0` rows with forced extremes fail closed and remain ineligible. All 864 successful structural coordinates are retained. No-extreme mfrmr/TAM/immer raw surfaces agree within the deterministic smoke tolerances, TAM `adj = .3` agrees with immer `jml, eps = .3` on forced extremes, and all classical postscale identities reproduce numerically. The audit prevents three semantic errors: immer `jml` is original raw only without extremes, `eps_adj` changes both Person and item sufficient statistics, and TAM's `(I - 1) / I` need not equal immer's `(Ibar - 1) / Ibar` under missing or unequal exposure. One-seed recovery values select no method. Persons, uncertainty, fit indices, GPCM, adversity, replication, missingness, candidate evidence, and confirmation remain open; no public surface, threshold, default, checklist state, candidate, or confirmation changes. |
| `0.2.3-draft.76` | Added a factor-structured TAM/immer/mfrmr JML feasibility contract over Persons, realized exposure, Raters, Criteria, 3--6 categories, assignment density, workload imbalance, endpoint response and Person rates, Gaussian-copula local dependence, Rater anchors, and none/MCAR/Rater-MAR/score-MNAR missingness. All 22 RSM/PCM datasets generate. Eighteen retain 162 mode rows; two anchor rows stop at a common-basis guard, and two no-common-Person low-information rows fail the expected structural-rank screen. Natural or forced extremes occur in 11/18 fitted datasets and account for 22 retained TAM `adj = 0` failures. Sparse and missing designs separate TAM's potential-pseudoitem `(I - 1) / I` from immer's observed-exposure `(Ibar - 1) / Ibar`; the sparse control yields `31/32` versus `7/8`. The returned marginal SE vectors do not follow the classical point-estimate postscale, so common-surface coverage is withheld. Bias, RMSE, two rank metrics, truth-SD/RMSE recovery separation, fit return, finite surface, engine-labelled convergence, and estimand eligibility are separated in 3,050 metric rows; common coverage and reported facet separation have zero eligible rows. A guarded 290-dataset/five-replicate pilot is declared but cannot freeze coverage, failure, correction, or sample-size rules. No public surface, threshold, default, checklist state, candidate, or confirmation changes. |
| `0.2.3-draft.77` | Added an execution-identity-bound atomic checkpoint and completion-marker contract, then completed the guarded 29-profile x RSM/PCM x five-replicate factor pilot. All 290 cells match their declared states: 230 fitted datasets retain 2,070 mode rows and 39,406 metrics, 40 one-Rater-per-Person cells fail the expected no-common-Person structural-rank screen, and 20 anchor cells stop before fitting. A separate process validates and resumes all 290 checkpoints without refitting and reproduces the deterministic result and marker hashes. Extreme Persons occur in 126/230 fitted datasets; original-raw eligibility is 104/230 for mfrmr raw and immer `jml`, while TAM raw/classical return on 103. mfrmr raw returns on 230 and profile on 228. Engine-labelled convergence remains definition-specific. Sparse and missing exposure expands the TAM/immer correction-factor ranges to `0.875--0.96875` versus `0.75--0.9375`; common original-raw traces show the expected finite-item contraction direction but do not select a correction or turn corrected points into original-JML maxima. Method-specific reference ratios expose score-MNAR, local dependence, high workload, sparse-load-MAR, and connected-design aliases as next priorities. Common coverage and reported facet separation still have zero eligible rows; five replicates cannot freeze coverage, rare-failure, sample-size, threshold, or confirmation rules. No public surface, default, checklist state, candidate, or confirmation changes. |
| `0.2.3-draft.78` | Added an independent connected-assignment RSM/PCM runner and completed its deterministic 36-dataset structural smoke. Thirty connected cells retain 270 method rows and six disconnected controls stop at the exact structural-rank screen before optimization. Assigned and observed graph audits retain bridge-Person count, Person degree, density, realized workload, Rater components, shared-Person edge weights, and weighted algebraic connectivity. Six bridge Persons leave the planned eight-Rater construction disconnected, while 12 connect it; holding those 12 bridges fixed gives the same cross-Rater algebraic connectivity with 60, 120, or 480 total Persons. Thus total Persons, bridge percentage, bridge count, and topology are distinct design quantities. Fixed-degree and fixed-density Rater sequences remain conditional multi-factor contrasts. Natural extremes occur in 20/30 connected cells and most TAM adjusted fits reach the smoke iteration ceiling, preserving separate return, convergence-proxy, and estimand-eligibility denominators. Common coverage and reported facet separation retain zero eligible rows. No bridge cutoff, sample-size recommendation, correction, threshold, public surface, checklist state, candidate, or confirmation changes. |
| `0.2.3-draft.79` | Added a matched path/cycle/distributed/hub topology runner with graph-removal vulnerability, adversarial one-link-Person loss, execution-bound atomic checkpoints, and a completion marker. Its 36-dataset RSM/PCM smoke retains 270 mode rows across 30 connected cells and stops six observed-disconnected controls before optimization. A separate process validates and resumes all 36 checkpoints with exact aggregate and marker hashes. At 12 bridges the hub has higher weighted algebraic connectivity than cycle or path but retains one articulation Rater and seven cut edges; losing one targeted link disconnects path and hub while cycle and distributed remain connected with new vulnerability. A 24-bridge negative control remains globally disconnected when links are concentrated within four Raters. All 30 connected cells contain natural extreme Persons, leaving original raw JML eligibility at zero and TAM raw/classical unavailable; only 10 TAM adjusted/combined fits stop before the 800-iteration ceiling. The unchanged declared 180-cell pilot is therefore not run and must be split into operational sparse/extreme, raw-eligible high-information, and stronger convergence lanes. Common coverage and reported separation remain unavailable. No bridge minimum, graph cutoff, topology preference, correction, sample-size rule, threshold, public surface, checklist state, candidate, or confirmation changes. |
| `0.2.3-draft.80` | Added a typed reconstruction roadmap for the existing observed-score G-theory and D-study helpers. The source audit distinguishes archived `gtheory` feature breadth from its unbalanced-count approximation and current maintenance state, and records local `lme4` 2.0.6 and `glmmTMB` 1.1.14 as separately diagnosed future backends. Arbitrary formula syntax is separated from a required object/facet/nesting/stratum/effect-map contract that determines universe, relative-error, absolute-error, and component-specific D-study scaling roles. Balanced ANOVA/MoM remains a negative-component-visible oracle; constrained likelihood boundary zeros retain their own identity. Full-refit bootstrap, missingness/allocation operators, and multivariate covariance/PSD/shared-facet rules are ordered future gates. The existing main-effects collapsed-residual public helper remains unchanged and caveated. No backend dependency, supported formula grammar, multivariate or joint GT-IRT claim, CI method, coefficient cutoff, optimal design, public surface, checklist promotion, candidate, or confirmation changes. |
| `0.2.3-draft.81` | Implemented the repository-only no-fit typed design parser and balanced component-wise D-study oracle. The parser retains original `/` nesting declarations separately from `reformulas` expansion, canonicalizes declared-order random-intercept terms into stable effect roles and `ScaleBy` divisors, and hashes design and result identities. Frozen p x i and p x r x i fixtures reproduce `G=5/6`, `Phi=4/5`, `G=10/13`, and `Phi=30/43` across eight tests and 46 expectations. Unresolved residual semantics or role, incomplete decompositions, unsupported nested scaling, malformed grammar, and unreplicated highest-order/residual aliasing fail before coefficient output. Raw negative inputs are retained with non-ready status; positive fixtures can be algebra-ready but every row is decision-ineligible because no fit or uncertainty is performed. The typed algebra roadmap guard moves from `not_run` to `review`, not `ok`. No fitting engine, public formula grammar, crossed/nested/multivariate support, uncertainty method, cutoff, optimum design, public surface, candidate, or confirmation changes. |
| `0.2.3-draft.82` | Added a complete-balanced univariate G-study point-estimation prototype under the Draft.81 typed map. Orthogonal marginal-effect ANOVA closes corrected total SS, retains all EMS coefficients/contributions, exactly recovers frozen p x i and p x r x i raw MoM vectors, and matches saturated base-R ANOVA terms. Local lme4 2.0.6 REML agrees on interior fixtures within `1.2e-5`; ML remains separate. A raw negative Item MoM value remains visible while matched REML returns a constrained approximately-zero singular boundary, so neither is relabelled as a correction or an unconstrained maximum. The main-effects/collapsed-residual compatibility fit reproduces current public helper components but cannot use typed D-study algebra. Seven tests and 71 expectations pass; all inference and decision-ready states remain false. Balanced-univariate and current-compatibility roadmap rows move only to `review`, not `ok`. No public estimator, formula support, preferred method, recovery/coverage/sample-size rule, interval, nested/imbalanced/missing/multivariate claim, candidate, or confirmation changes. |
| `0.2.3-draft.83a` | Added a repository-only pre-fit observed-design audit under the Draft.81 typed identity. It hashes canonical input, retained rows, omission patterns, and declared levels; applies conditional identities to nested child levels; records global/pairwise connectivity, degree and workload imbalance including zero-retained levels, full-cell replication, nesting counts, and component-specific fixed-effect-equivalent rank increments; and retains complete, sparse-connected, disconnected, nested, replicated, missingness-conflict, isolated-level, nonnumeric, and capacity controls. Seven tests and 69 expectations pass. Connectivity is necessary but insufficient, fixed-equivalent rank is not covariance/information rank, declared missingness is not verified, and observed loads are not D-study allocation operators. Estimation eligibility remains unadjudicated and all coefficient/decision-ready fields remain false. No public engine, nested/unbalanced support claim, allocation rule, interval, multivariate claim, checklist promotion, candidate, or confirmation changes. |
| `0.2.3-draft.83b` | Added a repository-only component-specific planned-weight allocation operator. Squared marginal weights for each typed `ScaleBy` map reduce exactly to all Draft.81 p x i / p x r x i divisors and coefficients. Nested Site/Rater support uses conditional identities and explicit planned structural cells; unequal units retain separate effective counts and G/Phi; shared/disjoint condition overlap retains non-object covariance multipliers; equal coefficients under disjoint support do not form a scenario scalar. Seven tests and 71 expectations pass. Components are supplied rather than estimated, raw negatives remain non-ready, and all estimation/inference/decision-ready states remain false. Object-nested facets, replicate-error operators, fixed-facet transport, fitted unbalanced backends, recovery, and intervals remain pending. No public engine, nested/unbalanced support claim, default allocation, checklist promotion, candidate, or confirmation changes. |
| `0.2.3-draft.83c1` | Added repository-only covariance-design, expected-information, and exact-retained-row lme4 diagnostics. Semantic `K_c=Z_c Z_c'` derivatives provide structural rank and null spaces; ML and REML expected-information ranks remain separate. Balanced p x r x i is 7/7 under both methods, saturated p x i exposes the exact highest-order/residual alias, and a one-Item fixture is structurally and ML full rank but REML deficient after intercept removal. Sparse disconnected incidence can retain full covariance rank and therefore still fails the independent Draft.83a gate. The lme4 route records version, likelihood, component identities, optimizer/messages/gradient/Hessian availability, singularity, default controls, backend-function hashes, and boundary regularity. Eight tests and 103 expectations pass. A finite optimizer-code-zero boundary fit remains nonregular; inference, coefficient, and decision readiness remain false. glmmTMB parity, recovery, intervals, public support, checklist promotion, candidate, and confirmation remain pending. |
| `0.2.3-draft.83c2` | Added repository-only exact-overlap Gaussian ML/REML glmmTMB/lme4 point-estimation parity. The glmmTMB route retains version/control/function, semantic component, objective/gradient/`pdHess`, boundary, information, intercept, and full-logLik identities. Interior p x i ML/REML, p x r x i ML/REML, and nested Site/Rater fixtures pass explicit smoke tolerances. A boundary fixture has glmmTMB optimizer code zero and `pdHess=TRUE` but remains near-zero/nonregular and materially disagrees with lme4, so matched parity is false. Eight tests and 93 expectations pass. Smoke tolerances are not equivalence margins; no backend selection, recovery, interval, coefficient, public support, checklist, candidate, or confirmation change is made. |
| `0.2.3-draft.83d1` | Added a repository-only pre-simulation G-theory ADEMP registry and exact-denominator contract. Twenty-four covering scenarios and 20 metrics produce 480 explicit routes; 22 executable scenarios expand to an 89-unit paired one-replicate smoke manifest. Exact Gaussian, missingness, finite observed-score projection, local-dependence reference, boundary/identification, and blocked-anchor lanes retain distinct estimands. Coverage is unavailable until Draft.84, and facet-level effect recovery is not Rasch/FACETS separation. Result stages are monotone, absent rows remain unrecorded, and fit-return, convergence, point-gate, metric-availability, false-ready, bias/RMSE, and coverage denominators are explicit. Nine tests and 77 expectations pass. No simulation, recovery evidence, replication-count freeze, interval, backend selection, coefficient, public support, checklist, candidate, or confirmation change is made. |
| `0.2.3-draft.83d1-mv1` | Added the future Draft.85a0 repository-only supplied-matrix multivariate G-theory algebra preflight without changing the ordered univariate gates. Semantic stratum covariance `Gamma_c` and explicit allocation-weight Gram `Lambda_c` form Hadamard component contributions, universe/relative/absolute covariance matrices, and weighted composite quadratic forms. Common, one-of-two shared, and independent facet supports remain distinct; unequal two-versus-three partial overlap is exactly 1/6 rather than `1/sqrt(6)`. One-stratum reduction, two-/three-stratum ordering, raw PSD/rank, component contributions, weight policies, and invalid matrix/allocation negatives pass nine tests and 66 expectations. Matrices are supplied, not estimated. No Stratum-aware estimator, interval, PSD repair, backend selection, public multivariate claim, candidate, confirmation, or checklist pass is added; Draft.83d2 and Draft.84 remain prerequisites. |
| `0.2.3-draft.83d2a` | Added the repository-only deterministic generation half of the univariate G-theory ADEMP smoke. The unchanged 24-scenario/89-fit-unit Draft.83d1 registry produces 22 generated scenario identities and two typed anchor blocks. Each generated result binds complete-potential, assigned, and post-missingness tables; nominal and finite-table projection truth where defined; generated effects; realized assignment/missingness/score audits; eleven function hashes; and one aggregate identity. Exact density/counts, workload contrasts, bounded support, MCAR/MAR/MNAR/unknown mechanisms, AR(1) residual dependence, zero/near-zero boundaries, nested incidence, and disconnected/aliased controls pass ten tests and 185 expectations. No model is fitted and no recovery, false-ready, interval, replication-count, backend, coefficient, public support, candidate, confirmation, or checklist pass is added; Draft.83d2b and Draft.84 remain pending. |
| `0.2.3-draft.83d2b0` | Added the repository-only exact scalable structural pre-fit half of the univariate G-theory ADEMP smoke. Equality-signature compression preserves covariance-component rank and null directions for the registered independent scalar random-intercept family without materializing dense derivative matrices. It matches the feasible dense Draft.83c1 oracle, evaluates the 19,200-row N=300 scenario at rank 7/7 instead of allocating 1,290,307,200 cells, and binds all generated identities to all 89 planned fit units. Nineteen scenarios/77 units are structurally eligible with likelihood information pending; three scenarios/12 units are blocked before fitting by disconnected or aliased designs. Eight tests and 71 expectations pass. No fit is authorized or attempted and no atomic result, recovery, false-ready, interval, coefficient, public support, candidate, confirmation, or checklist pass is added; Draft.83d2b1, later Draft.83d2 recovery work, and Draft.84 remain pending. |
| `0.2.3-draft.83d2b1` | Added repository-only atomic balanced-MoM, lme4 ML/REML, and glmmTMB ML/REML adapters and executed all 89 planned units. Twelve pre-fit-blocked units make no backend call; all 77 eligible attempts return. Fifty-seven point gates pass and 32 typed failures yield exact accounting with no unrecorded unit. Backend-local curvature remains explicitly narrower than full expected information, and negative raw MoM output is retained. The zero-false-ready gate fails because all four near-zero variance routes pass the current optimizer/finite/absolute-boundary/local-curvature rule; exact-zero and identification controls remain blocked. The execution hash reproduces twice. Recovery and inference remain false pending a pre-registered observable weak-information calibration with zero, near-zero, small-positive, and ordinary-positive controls; no truth-aware special case, post hoc tolerance change, interval, coefficient, estimator preference, public support, candidate, confirmation, or checklist pass is added. |
| `0.2.3-draft.83d2b2a` | Added a repository-only weak-information diagnostic registry and one-replicate covering smoke. Five information/design strata x six generating Rater-variance regions x four lme4/glmmTMB ML/REML routes produce 120/120 returned, atomically recorded fits under one registry identity. The existing whole-model point gate passes 82 units but gives 27/40 false-ready exact-zero/near-zero target controls and 3/12 false-block narrow positive controls, so it is not a target-component resolution rule. Ten truth-blind candidate observables are instrumented; reduced-model likelihood evidence and full-refit intervals remain unimplemented. These single-replicate counts are counterexamples, not operating characteristics. No threshold, replication count, Monte Carlo precision target, indeterminate zone, minimum design, sample-size rule, recovery, interval, coefficient, estimator preference, public support, candidate, confirmation, or checklist pass is added. |
| `0.2.3-draft.83d2b2b0` | Added the repository-only replicated weak-information pilot plan and authorization firewall. Scenario x replicate is the independent Monte Carlo unit and all four methods are paired. Nonoverlapping schema, feasibility, calibration, and confirmation bands contain 24, 3,000, 12,000, and 24,000 fits with worst-case cell x method Bernoulli MCSE 0.354, 0.10, 0.05, and 0.035. The complete 24-fit schema returns and atomically records every route but is ineligible as pilot evidence. Four rule architectures, fixed score grids, lexicographic selection order, and not-resolved/indeterminate/resolved/not-evaluable states are registered without selected cutpoints. Backend-specific target uncertainty and reduced-component likelihood refits remain pending. Feasibility is authorized but unrun; calibration and confirmation fail closed and no confirmation result has been viewed. No threshold, sample-size rule, recovery, interval, coefficient, estimator preference, public support, candidate, confirmation, or checklist pass is added. |
| `0.2.3-draft.83d2b2b1a` | Added a source-audited variance-component boundary-inference contract and already-viewed diagnostic refits. Self--Liang asymptotics, Crainiceanu--Ruppert's exact one-component LMM result, Greven et al.'s finite-sample work, and current lme4/glmmTMB/RLRsim contracts prohibit an ordinary chi-square, universal mixture, or nominally exact RLRsim shortcut for the registered multi-component model. The common `target_relative_se_profiled` score and its two rule families are withdrawn because lme4 profiled relative-SD and glmmTMB joint log-SD scales are noncommensurate and nonregular at zero. All 24 full/reduced pairs return, all raw ML/REML likelihood diagnostics are recorded, four tiny negative numerical differences remain untruncated, and 20 backend-coordinate local diagnostics are available; no p-value or interval is formed. The historical feasibility authorization is prospectively superseded before any reserved seed is generated. A replacement exact-design reduced-model bootstrap contract plus separate positive-control recovery and D-study stability gates is required. No threshold, recovery, interval, coefficient, estimator preference, public support, candidate, confirmation, or checklist pass is added. |
| `0.2.3-draft.83d2b2b1b` | Added repository-only exact-observed-design fitted-reduced-model parametric-bootstrap mechanics. Three already viewed baseline controls at replicate 2 x four lme4/glmmTMB ML/REML routes x `B=3` produce 12 observed and 36 bootstrap pairs, or 96 full/reduced fits. All pairs return; every generated response preserves design identity and has a distinct response/data hash; 16 small negative likelihood differences remain raw; and eight bootstrap pairs expose a non-target nuisance boundary. Plus-one lower/upper bounds retain every planned failure, while simulation/identity and refit failures remain distinct. This is a plain plug-in procedure, not finite-sample exact, not the shrinked nuisance-boundary bootstrap, and not operating-characteristic calibration. The 0.25 grid cannot select a threshold or production `B`; a naive all-cell `B=199` plan would require 1,200,000 fits and is unauthorized. Resolution feasibility, size/power, positive recovery, D-study stability, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or pending. |
| `0.2.3-draft.83d2b2b1c` | Added a replacement untouched descriptive-feasibility manifest plus viewed all-cell runtime/checkpoint authorization. Five designs x six target-variance regions x 25 replicates x four methods give 3,000 atomic rows, 750 independent datasets, and 6,000 full/reduced fits in the never-generated 101--125 band. Manifest construction calls no generator. The replicate-1 runtime schema returns all 120 pairs but retains nine unavailable common scores: six materially negative nested likelihood differences and four optimizer/likelihood failures overlap in one row. Eighteen available small negatives, 22 target boundaries, and eight nuisance boundaries remain visible. Timing-excluded execution and authorization hashes reproduce; central local serial projections are about 32.6--33.0 minutes and x4 sensitivity is about 2.2 hours without a performance guarantee. Pair/dataset atomic checkpoint gates authorize only the frozen descriptive run. Feasibility evidence, thresholds, inner bootstrap, size/power, positive recovery, D-study stability, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or pending. |
| `0.2.3-draft.83d2b2b1d` | Executed the frozen descriptive-feasibility manifest with one atomic checkpoint per full/reduced method pair and one completion marker per four-route dataset. All 3,000 pairs and 750 datasets validate; all pairs return and 2,804 common scores are available. The corrected three-state classification contains 79 optimizer/likelihood-identity unavailable rows, 126 finite materially negative raw likelihood differences with nine overlapping unavailable rows, and seven non-finite differences already in the unavailable set. Seven hundred fifty-nine available small negative differences remain raw, with 913 target and 381 nuisance boundary rows. High-information availability is 457/600 with 101 material negatives; few-level availability is 576/600 but ordering correlations are about 0.14--0.26 and 283/600 routes have a nuisance boundary. A no-refit resume reuses all 3,000 checkpoints and reproduces execution hash `04ec60ab6d4351c0d8c6416543fa8ac46e15585bbe85680f829b341beb34a22b`. Descriptive feasibility accounting is ready, but numerical sensitivity, bootstrap operating characteristics, size/power, positive recovery, D-study stability, thresholds, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or pending. |
| `0.2.3-draft.83d2b2b1e` | Executed three frozen optimizer profiles for each of the 3,000 viewed feasibility routes: all 9,000 full/reduced pairs, 18,000 backend fits, and 750 dataset markers validate, and a zero-computation resume reproduces hash `37be0b4dbac852454ced612b5f84706678f688f3f3ea7209793111ab6a706d94`. Same-algorithm strict controls reproduce every default objective. lme4 bobyqa is finite and comparison-available on all 1,500 routes and removes all 34 default-lme4 material-negative differences; glmmTMB BFGS instead yields 331 non-finite and 401 finite material-negative differences. Across original routes, 433 signs are optimizer-sensitive, 334 are incomplete, 30 are material-negative for every profile, and 69 separately maximized envelopes remain below -1e-6. All 2,993 finite defaults exactly replay b1d, but seven same-non-finite defaults fall outside the frozen finite absolute-difference replay rule. `NumericalSensitivityEvidenceReady` and all downstream gates remain false. No optimizer, practical-equivalence threshold, bootstrap method, inference, coefficient, public support, candidate, confirmation, or checklist promotion is selected. |
| `0.2.3-draft.83d2b2b1f` | Applied a prospectively typed, no-refit replay adjudicator to the exact b1d and b1e ledgers. The five-state accounting contains 2,993 `finite_match`, seven `same_typed_nonfinite_state`, and zero mismatches. All seven are `NA_real_` on both sides with identical pair-return, likelihood-unavailable, tolerance, and comparison states; none is assigned a numeric distance or promoted to availability. Contract hash `8a18d59548ab5d8523e29f7089d2ea70620f51b38e2444e133a2e78974ff0d4a` and result hash `e200a9ee7984bbc3be32ab5ef209ce2eb26c0b42c8df3ad758bab7baf559f8c1` reproduce. `TypedReplayAdjudicationReady=TRUE` closes only the missing state definition. The immutable b1e replay failure, numerical stabilization, numerical-sensitivity evidence, calibration, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false. |
| `0.2.3-draft.83d2b2b1g` | Froze a prospective glmmTMB-only stabilization DAG and exact manifest from the immutable b1e/b1f identities. All 1,500 viewed glmmTMB routes receive cold nlminb/BFGS, same-algorithm restart, and cross-algorithm warm-start profiles, producing 9,000 planned full/reduced pairs and 18,000 fits with zero duplicate routes or missing parents. All ten named start blocks, immediate `last.par.best` snapshot plus fixed-coordinate equality, same-model transfer, typed parent failure, outer/sdreport gradients, and frozen Richardson Hessian diagnostics are part of identity. Five tests and 73 expectations reproduce contract `8feb8695c655c0621d61863e00d82fffe7fd5d7b619761decefaed6e89b0c326` and manifest `92435f41b0dab7e13bf1febcf6e043fc1ae8d4a2cb7d159401dd1d78b4c9ff3e`. `ManifestReady=TRUE`, but the runner is absent and execution, numerical stabilization, numerical-sensitivity evidence, calibration, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g1` | Implemented the six-profile atomic base-route runner and executed the factor-selected viewed smoke: 10 datasets, 20 base routes, 120 pair rows, and 240 planned fits. Twenty route checkpoints and ten dataset markers validate; a no-fit resume reuses 20/20 routes and reproduces execution `c743de38ec7d5ff6606c5b1df7960caea4bca149b470063e8496db83b5ab439d`. The exact state partition is 84 diagnostic-complete, 21 finite material-negative, 11 non-finite objective/likelihood, two parent-unavailable, one full failure, and one reduced failure. Two BFGS fits return but fail the strict fixed-coordinate snapshot rule; the observed approximately 2.80e-10 difference is not adopted as a tolerance. Four tests and 59 expectations pass. `SmokeRunnerMechanicsReady=TRUE`, but full execution, deterministic alignment, numerical stabilization, numerical-sensitivity evidence, calibration, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g2` | Prospectively specified a tolerance-free fixed-coordinate alignment and re-executed the exact b1g1 120-pair denominator under a new checkpoint identity. The rule overwrites only `lfixed()` positions with `fit$fit$par` and retains raw/aligned hashes. All 240 fits return and align exactly, all 80 dependent transfers verify, and no-fit resume reuses 20/20 routes. Four b1g1 returns are recovered with no losses; common returned top-level hashes, objectives, and likelihoods have zero mismatches. Execution `e2716a4ae71784e218d15f2509ed8c15326c1b7c6bc9acf78826a81822581482` and full-denominator comparison `651b6f07cb7977b7d1245b1048e0b7b905c4999f8da43bfbcd30180d9581d435` reproduce. The 85 diagnostic-complete, 21 material-negative, and 14 non-finite rows plus adverse gradient/curvature evidence keep full execution, numerical stabilization, numerical-sensitivity evidence, calibration, inference, coefficient, public support, candidate, confirmation, and checklist promotion false or unauthorized. |
| `0.2.3-draft.83d2b2b1g3` | Applied a no-refit multi-axis adjudicator to the exact b1g2 ledger after recomputing upstream scientific hashes. All 120 full/reduced raw-objective pairs are finite; 106 reported likelihood pairs are available and exact, while five full-only, seven reduced-only, and two both-side unavailable pairs are exactly explained by the installed glmmTMB non-PD-Hessian mask. One full optimizer code is nonzero beneath an earlier primary state. Sdreport and Richardson PD signs agree throughout; one full and one reduced outer/sdreport gradient hash differ. The complete trace ordering is 75 positive, 22 small negative, and 23 material negative. Twenty best-observed six-profile envelopes are 12 positive and eight small negative, but are not global-maximum claims. Contract `934f96be6e23cd728576cfedbb44d48b68137fbbc74e84d19d7200b8ad52ccc0` and result `c7c35d5b961c578b6234a1f29f628f13dac357bc1b046e012832d28ca7f3d4de` reproduce. Stationarity, sufficient numerical eligibility, full execution, calibration, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g4` | Prospectively instrumented and replayed the exact b1g2 120-pair/240-fit denominator with content-addressed raw parameter, outer-gradient, sdreport-gradient, Richardson-Jacobian, and derived-vector sidecars. All b1g2 parameter hashes, objectives, reported likelihoods, and nested drops have zero mismatch; all repeated outer-gradient and Richardson hashes agree, and a no-fit resume reuses all 20 routes. Raw and objective-relative gradients are available for 240 fits, spectral Hessian positivity for 224, Cholesky-based lme4/Newton summaries for 221, and direct relative Newton steps for 218. The two prior gradient-surface discrepancies are quantified but not thresholded. Across 40 route/model strata, metric-minimizing and best-objective profiles incompletely agree, so no optimizer is selected. Contract `97dcdd0103a3eb9a714ac56008f801af57853bc816f42e5bc9ab33dd63f3ae32`, execution `a825ab427da7e4a8160e428a7a6b00038f364b1c15049df6d5e4bf03bbbbbade`, and result `40949ff311e6dbe1289cf6488aa2db3642a65ca64d6b794b47aeb5001d53acf1` reproduce. The measurement schema is ready, while stationarity, sufficient numerical eligibility, full execution, calibration, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g5` | Froze the independent stationarity-calibration design without generating reserved data. Finite stationarity, Hessian curvature/factorability, profiled log-SD boundary limits, and statistical component resolution have separate states and gate order. Analytic affine fixtures preserve Hessian inertia and Newton decrement while exposing coordinate dependence of raw, objective-relative, lme4-compatible, and relative-step summaries. Five candidate families, eight prespecified zones, and an eight-stage high-accuracy reference architecture are registered. The sealed 3,000-dataset reservation maps to 144,000 candidate fits and 24,000 reference problems, all unauthorized. Contract `278353d1668501d04dd3af4adc96dfcd39b232796057242418f89601b22b99ac` and manifest `0dbe9e92bed7baa27b6c5f29bed0759a789bcc02c285bd77d749a9cc9666e4d0` reproduce. Reference tolerances, stationarity, sufficient numerical eligibility, calibration execution, full execution, bootstrap, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g6` | Completed the high-accuracy reference calibration on six analytic objectives and nonreserved baseline-complete replicates 901--902. The final derivative gate selects a central-difference interval from adjacent-step stability without using AD, retains componentwise objective-scale numerical resolution, resets TMB random-effect starts before every evaluation, and requires numerical-Hessian symmetry. All four full/reduced glmmTMB REML objectives pass three-algorithm consensus, derivative, curvature, and content-addressed sidecar checks; all twelve fixed-log-SD profile points pass nuisance curvature/stationarity, and both full profiles support a finite interior. Contract `60e04706736c0e7273dfa321d0d41a3a9ed4bb8362a0b7d428f8507653ecce9a`, manifest `87b42667d3dbeb2ecd045b23b32cf23a5f9919b0d26ac75c5771baf691770d3a`, and execution `28f155c91065cb56ebe695234eab7867392e25fe413ab362717e760f5e775e72` reproduce. `ReferenceToleranceFrozen=TRUE` closes only reference mechanics. Production stationarity, sufficient numerical eligibility, reserved calibration, full execution, bootstrap, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g7` | Completed a fail-closed preauthorization audit without generating or reading calibration 201--300 or confirmation 501--700. Backend-specific registries contain six glmmTMB and three lme4 profiles, correcting the prospective candidate-fit upper bound from the b1g5 historical 144,000 to 108,000 while keeping 3,000 datasets, 12,000 method units, and 24,000 reference problems unchanged. Candidate state precedence distinguishes boundary handoff, first-order zones, curvature/factorability, indeterminate, and not-evaluable states. Profile aggregation occurs only within dataset-method-model-role by minimum finite objective plus fixed exact-tie priority; neither a diagnostic metric nor generating truth selects a profile. Audit `b293987e768ec0e998d3224a6df0689f0ab8b6f2268704ef422e333865d82765` and corrected manifest `7cce9d42faccfbbdf928c9ec4978fef25c50aa562750141fbab45a53b75885f8` reproduce in five focused tests with 71 expectations. Only glmmTMB REML has passed nonreserved high-accuracy reference mechanics; glmmTMB ML, lme4 ML/REML, acceptance policy, production boundary probe, runner, reserved calibration, stationarity, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g8` | Completed a separately identified glmmTMB ML high-accuracy replay on the same nonreserved generator hashes as b1g6. `REML=FALSE` is bound at contract, manifest, row, and sidecar levels; all four polished objectives differ from REML by more than `1e-3`, so likelihood mode is not cosmetic. Four objectives pass three-algorithm consensus, AD-independent derivative agreement, Hessian symmetry, positive curvature, and sidecar checks. Both six-point full-model profiles support finite interiors and all twelve nuisance fits pass stationarity. A second complete replay reproduces rows, sidecar hashes, and execution hash. Contract `1216ae3591fc026a61b4fb6581ebe79e33d34e4e2b6bf04a969a4c93c3e06689`, manifest `2974db4aefd07636d286b8227edb6dd50b481764e9dd7060296bd379a2688434`, and execution `46ea4be751a3c54904bac28da31f15e5e05f347b9e8f10a1194887f55557807d` reproduce in six focused tests with 64 expectations. `GlmmTMBMethodCoverageReady=TRUE` closes only glmmTMB ML/REML reference mechanics. lme4 ML/REML, complete coverage, calibration authorization, stationarity, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g9` | Completed an analytic lme4 objective-reference preflight without reading any response reservation. For zero-offset, unweighted independent random intercepts, a dense Gaussian oracle independently reproduces the theta-only profiled ML deviance and REML criterion, analytic gradients, fitted accessors, log-likelihoods, evaluation-order stability, and exact-zero full-to-reduced identities. Installed lme4 2.0.6 source hashes show that `devfun2()` forces ML and fails its local basedev control, while `deviance(..., REML=TRUE)` returns an ML rather than REML criterion; both routes are prohibited. Audit `83faaaf570bd814c000924aa21396ade00958fb8134cec553a0eaa985382ca67` and contract `20d6fb656ac2f2996e5881a07729a3e4fb2f417859f90efde7ee72784ba62092` reproduce in seven focused tests with 67 expectations. `Lme4ObjectivePreflightReady=TRUE` closes only objective identity. The lme4 solver, boundary profile, nonreserved replay, complete coverage, calibration authorization, stationarity, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g10` | Completed repeatable nonreserved lme4 ML/REML high-accuracy reference coverage. Replicates 901--902 yield eight full/reduced objectives and 72 main solver runs across three starts and three algorithms per objective. Every objective passes multialgorithm consensus, the independent sparse Gaussian objective/gradient oracle, raw and curvature-scaled KKT diagnostics, analytic Newton polishing, and positive free curvature. Four seven-point nuisance-reoptimized target-theta profiles retain finite interiors and match their reduced zero endpoints within `2.27e-11`; the sparse oracle reduces to the b1g9 dense oracle on its analytic fixture. A second complete replay reproduces atomic rows, sidecar hashes, and execution hash `b84c1d53f8653bb5329a0a165e2249b36e5d12e10c26099ab15cbdfac4281e8a`. Nine focused tests with 137 expectations pass. `ReferenceMethodCoverageComplete=TRUE` closes only all four high-accuracy reference-mechanics lanes. `BoundaryFirstOrderSufficiencyClaim=FALSE`; acceptance policy, production boundary probe and exact-resume runner, calibration authorization/execution, stationarity, inference, coefficient, public support, candidate, confirmation, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g11` | Froze a truth-blind numerical-stationarity acceptance and Monte Carlo decision policy without opening any reserved response. Four nonreserved receipts bind glmmTMB/lme4 x ML/REML reference coverage. Three primary score families crossed with eight registered zones produce 24 candidates under one global family-zone scope. Primary rates remain scenario x method x model-role specific; safety false ready, false boundary handoff, false unready, missed boundary, indeterminate, non-evaluation, and reference-unresolved states are nonexchangeable. Zero events retain one-sided Clopper--Pearson upper bounds, zero denominators remain unavailable, and bounds have no unqualified post-selection confidence interpretation. Any observed safety event rejects a candidate, while observation and correct decisive coverage of all 20 required method x role x reference classes prevents an always-indeterminate winner. If no candidate passes, the immutable outcome is a negative calibration with no threshold. Receipt `777e7550a188f89515854738e3b7e42ef418037de4c9f7166a67d61e6dfa2e9e`, policy `7962e47df285812d8c785f206d51925b44a13d02037b7b40a619cb80ce833a62`, audit `cfdaa73ddc0beb6cc6ca3fbdc2cd7c73bd899bf9e6bebaab675ccc7bd88b16f7`, and contract `1dcc877da78d3975271b33629b3d67bd9f0f48d675fb1ed62e5704baa46b8b1a` reproduce in nine focused tests with 102 expectations. The production boundary probe, exact-resume runner, calibration authorization/execution, stationarity threshold/criterion, confirmation, inference, coefficient, public support, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g12` | Implemented a coordinate-correct, truth-blind production boundary probe without opening reserved calibration or confirmation responses. lme4 follows nonnegative relative-SD theta to exact zero; glmmTMB follows six log-SD offsets toward its nonfinite zero-variance limit. Every point reoptimizes nuisance coordinates. A boundary handoff requires monotone material improvement and agreement with the separately fitted reduced objective; monotone material worsening supports a finite interior; flat, nonmonotone, mismatched, and failed paths remain inconclusive or non-evaluable. One warm-started L-BFGS-B fit plus a single recorded same-point `nlminb` failure fallback defines the production mechanics. Seven analytic controls and lme4/glmmTMB x ML/REML fixtures pass in eight tests with 120 expectations. Policy `fb7f938a0e1e5b7be598a180f7d5c06eb3176ebea7f1bfc269491befa865cb6c`, audit `272e5ee2c274c8972ecde6feb039db02e9beb937d4db910555812c35af627eab`, and contract `53a36d72388eb8b4e096ef817aaf94959aa1b3fd3257190cf5c0a8164383d9da` reproduce. `ProductionBoundaryProbeReady=TRUE` is narrow; exact-resume runner, calibration authorization/execution, threshold/criterion, confirmation, inference, coefficient, public support, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g13` | Implemented content-addressed exact-resume runner mechanics without opening reserved responses. One atomic dataset-method checkpoint binds all registered full/reduced optimizer-profile fits, two high-accuracy references, 48 candidate decisions, and typed failures. The sealed b1g7 workload independently reconstructs 3,000 dataset markers, 12,000 atomic units, 108,000 candidate-fit rows, 576,000 candidate-decision rows, and 24,000 reference rows. Nonreserved replicates 901--902 verify evaluator-error expansion, checkpoint/marker/manifest tamper rejection, three-unit interruption and resume, cold-run equality, complete reuse, and one-checkpoint repair. Nine focused tests with 129 expectations pass. Policy `c346aebac1a13770e755402b6a0b7f0e1338d8f9f8b238103b7291e01e250f38`, contract `8fb599cd4abbabb454ce416fe3470d3e0f8d23f0bc8f2662630083fb1ec388da`, fixture manifest `af13168b59d6f083bade0c66b85ff6befa48b195f1a27434c95e8acc6e18d098`, and execution `4cdbb0ed2ba69588f81e3fcbd3df634b92a4b7e1929bac387cd6a8562a18100f` reproduce. `RunnerImplementationReady=TRUE` is narrow; production evaluator adapters, reserved run manifest, calibration authorization/execution, threshold/criterion, confirmation, inference, coefficient, public support, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g14` | Froze the real four-lane production candidate/reference adapters and the exact reserved-run manifest without opening a reserved response. Runtime `94cb18393b87ef8409f231b2e62c507f43fb3294cdefeb0f3e8c19c8235e7753`, adapter contract `baf48a948b86c1769aba8a574619c6ce57be17b4b5747ae935f0e430392518a1`, reserved manifest `019fedf063ce90c3492f9eb37f6dbec43a42474ce9096e7cc2891491d7a158c8`, dry manifest `cb9fe43e82be6ed64dc08db94998114b5a3f1f420914e1e5491cee3c79f2554c`, dry execution `b9ad747a62b1e14cf1da1e0e4cee8a0a341db969596df3e2de87b25ba908caae`, and preflight `eddbe9cb3e1ab56d3389f9f896524f0bf0ae92b224b2997f4e5f6014219a31cf` reproduce. The nonreserved four-unit run retains exact 36-fit, 192-decision, and eight-reference ledgers; 35 fits return and one glmmTMB ML `start_snapshot` failure remains typed. All references resolve, candidate/reference generator and structural pre-fit hashes match, truth and metrics do not select profiles, and complete reuse is exact. One hundred shards independently reconstruct all sealed counts but retain `ExecutionAuthorized=FALSE`. Seven focused tests with 114 assertions pass. `ProductionAdapterPreflightReady=TRUE` is narrow; calibration authorization/execution/data/results, threshold/criterion, confirmation, inference, coefficient, public support, decision, D-study readiness, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g15` | Completed the response-free one-way authorization preflight without issuing an execution authorization or opening replicate 201. Contract `44a6d4e2677af111e6eeeae8b7b3143ce8521db34da1769a86b85f32c63ca551` and prospective bundle `dfd5099b84dc8ff64b605261d957173e63b8b04645b1f90cc7262b8120d54e82` reproduce. One hundred independently hashed manifests exactly partition 3,000 datasets, 12,000 atomic units, 108,000 candidate fits, 576,000 decisions, and 24,000 references, while every shard remains non-executable. The real output parent passes target-absence, actual write, checked same-directory rename, identical readback, cleanup, and site-capacity checks. Conservative planning requires 47,775,834,368 available bytes and bounds the 4x serial plan at 296.823333 hours and each shard at 2.968233 hours, with one concurrent shard and no early stopping. Eight focused tests with 149 assertions pass. `AuthorizationReadinessAuditReady=TRUE` and `AuthorizationActivationEligible=TRUE` are narrow; `ExecutionAuthorizationRecordIssued=FALSE`, and calibration execution/data/results, threshold/criterion, confirmation, inference, coefficient, public support, decision, D-study readiness, and checklist promotion remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g15a` | Completed a response-free Monte Carlo value and precision audit. Policy `f91a2b54fd766a35255097feaaf27e0be446a2e5f454c6b19f7eb85686a5e58d`, contract `9695149f99885bd4647c40466fef37e99cf321aa7786e232476586730f4fa1d8`, and audit `67987463d2fa587441714da5b6a8fc9046f6c2cc3ec604a416a264762d868f45` reproduce. It distinguishes 3,000 independent scenario-replicate datasets and at most 100 planned trials per primary cell from 108,000 dependent fits, 576,000 decisions, and 24,000 references. With a complete resolved n=100, worst-case Bernoulli MCSE is 0.05, 0/100 one-sided 95% upper is 0.029513, and a true 3% event is seen at least once with probability 0.952447. The 3% event and 95% detection values are purpose-audit benchmarks that cannot enter candidate selection. Five focused tests with 87 assertions pass. `NumericalCalibrationDesignPurposeJustified=TRUE` retains the plan only for numerical candidate calibration and a valid negative result; `CalibrationPrecisionEvidenceReady=FALSE`, and broad performance, D-study operating-characteristic, universal sample-size, authorization, calibration, confirmation, inference, and checklist claims remain false or unavailable. |
| `0.2.3-draft.83d2b2b1g16` | Completed the response-free pre-activation hardening audit and stopped before large calibration. Policy `dc3fb20ef72d64842b1fed7273ebcaa6b2bd794b8757f59ccea03a4d3e945c3c`, portable contract `9ea428bfec87509cacdadba6250d837aef5090c07550f2c0603b79164f2c58c0`, seed audit `34d69696fb17e6f3af9213113a7f629ab69ff16d2826f6412db01716009cd027`, ambient-RNG probe `b95dbfdddca4515ec70dcfb23680039de565e744c2b6e4fbc6468a95c62bc7f0`, and portable decision `e4e32bc3fc93c4469ce88ccf46e91866b6a22ef3e81d71a9a3dc483d767f1799` reproduce. The 9,756 actual phase-specific seed rows are unique, but the same nonreserved replicate-901 integer seed yields different data under Mersenne-Twister and Wichmann-Hill. Extended runtime, explicit threads, vanilla process, authorized runner, writer lock, activation/resume root, and per-shard capacity gates also block. Five focused tests with 92 assertions pass. `PreactivationHardeningAuditReady=TRUE` records a complete stop decision; `AuthorizationActivationEligible=FALSE`, `LargeSimulationMayStart=FALSE`, `Replicate201MayBeOpened=FALSE`, and all calibration, confirmation, inference, and checklist claims remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g17` | Completed a separately identified nonreserved RNG-hardened generator replay without changing the historical b1g2a function or opening reserved responses. Policy `9d54aeee4e7cf9bc3b20e8e96fa99dfa79febf070cc0d732377287ce897e20a9`, manifest `d3fe95bc5eae79dbc76e622fb2767eb3b0d14a378c767eaaf15872685ca351f7`, cross-ambient replay `39054c3ef7b78783065134ee38303d73d3c6601edfddc8e68cebb8990949f955`, contract `90869c6874e2884b7bf5bc96c1939bd95d1b41603ea76a1d2b1c617f32c700d2`, and audit `918e7da5e0ba484bbdef4251965c25ff31c5d4a39be237c3d218ed47fceae397` reproduce. All 30 scenarios at replicate 901 have identical hardened, historical-parent, and analysis-data hashes across Mersenne-Twister and Wichmann-Hill caller states; caller state is restored and reserved bands are rejected. Four focused tests with 67 assertions pass. `HardenedGeneratorReady=TRUE` and `RNG01ProspectivelyResolved=TRUE` are component-local. `RNG-ADAPTER-01` blocks, `AuthorizationRNG01Closed=FALSE`, `LargeSimulationMayStart=FALSE`, `Replicate201MayBeOpened=FALSE`, and all calibration, confirmation, inference, and checklist claims remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g18` | Completed the separately identified nonreserved hardened production-adapter rebase without changing historical b1g14 evidence or opening reserved responses. Policy `4a4d5ff4becc42e931806cc19d97449d8ec083b65b970d201230f4f4b5e19684`, contract `0373db563cd16c63693b02b968dbbd49221a77e1f666b87cc63b17cb6f786e64`, manifest `090b761a835098b037b3f65021ebe22eeae9df822628df6898685b1f84e99d15`, hardened execution `14a44a4382e50ba2819d57072288bd213789935d4ed6d78fba674a8081b62aeb`, and audit `1e8461eca060b00215240d65c506873d6f082f66b6216fb1ff30761df7dfdb63` reproduce. Historical/hardened four-lane executions have exact semantic parity over 36 candidate fits, 192 decisions, eight references, 35 returned fits, one retained `start_snapshot` failure, and zero unresolved references. Candidate/reference hardened identities match and all four checkpoints reuse. Four focused tests with 65 assertions pass. `NonreservedAdapterRebaseReady=TRUE` is narrow; reserved-manifest rebase is deferred, `AuthorizationRNG01Closed=FALSE`, `LargeSimulationMayStart=FALSE`, and calibration, confirmation, inference, and checklist claims remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g19` | Completed the response-free hardened reserved-lineage rebase without opening calibration or confirmation responses. Receipt `3e7911686df046bc5fe507ab276194e463952c45b04230facbadb42120500313`, policy `6a68e71aa5cb30c7f1460b8c81ba76e135ed42dcf2d341a8f966d2e1783ea905`, contract `5075f23a5af8edb7d77ff3cd4c4efaad4d7a624995c3ee04a62ed04a2bee49f5`, reserved manifest `da3905e9b9a605f42e877f695226a0c5ee7089cc04f68ad7ee6350de25c9cbd6`, shard bundle `634159d6d85ea04ecf9447330af122c01284644211aa2dd78d85ab34a92661df`, and audit `9bc4d4dafbeec602f7718c7249bf15f4aeb5a6dbd5862b46e4d8889dc2540d7a` reproduce. The old/new workload agrees on 3,000 datasets, 12,000 units, 108,000 fits, 576,000 decisions, 24,000 references, and 100 shard partitions. All 12,000 unit and 100 shard identities change under hardened lineage, with historical hashes retained only as provenance and zero active/historical overlap. Four focused tests with 69 assertions pass. `ReservedManifestRebaseReady=TRUE` is narrow; the reserved entry point, runtime extension, authorized runner, calibration, confirmation, inference, and checklist claims remain false or unauthorized. |
| `0.2.3-draft.83d2b2b1g20` | Consolidated the shared response-free authorization infrastructure. Receipt `4a453c9b44fd03ae456ba1fda2f8d65208c3ab63b23c6c51d1f6026dfe3e4e92`, policy `ca0b9691600d4aa1241741cb0fa8710de1ea892a8e243c323425c6b76047136c`, contract `86a6015b1eebdb4c9cf8cfa57110d24354ec5999e62f477c82506d8cc6b1edce`, isolated runtime `b32aca03814bd2ae12a7475e61caa338c464a46e4bd90209b186ccc1da9383b0`, runtime probe `53a4b57eaf4556ede168f1e686e3be87697a2b3175f464cc88090d394693715e`, and lock/root audit `ec573bb5e939e4bd711ae267c41894db89b14d576a19d4ac7ac45c268b3071cf` reproduce. Runtime/thread/process, lock/root, fresh capacity, lineage, RNG, and confirmation gates pass in four tests with 54 assertions. Runtime drift, capacity loss, worker mutation, contention, marker mutation, and unmarked roots fail closed or become typed non-ready receipts. `AuthorizationKernelReady=TRUE` is narrow; `RUNNER-01` and `AUTH-RECORD-01` block, no target or response is created, and all authorization, calibration, inference, and checklist claims remain false. |
| `0.2.3-draft.83d2b2b1g21` | Completed the guarded runner scientific reduction without opening a reserved response. Policy `ecdf55015369c04e9ec81549fe68a624d0c0a025a154f28eeedc3adbd41d86aa`, contract `f6d932f5261b3816bd16afa820cc2c36acf188d5144277b22623a9b41245f552`, fixture manifest `0a515e0977774887094321284a723e058d9b1723f3d45f505245429dc93d6db3`, runner `b179c44076107c64e0f7d030585d38dec11bffba5231dcf4a702631a11742e1c`, and worker `8a7bfbe987a4178f83351508d29defeb631acb9f3cd9c1f724ae85d68fbf2df7` reproduce. Four focused tests with 54 assertions run the actual b1g18-derived glmmTMB/lme4 x ML/REML path under the b1g20 isolated runtime, lock, and activation marker. All 36 fits, 192 decisions, and eight references retain semantic parity; 35 fits return and one typed failure remains. Exact resume computes zero and reuses four checkpoints. Job, manifest, contract, worker, and result mutations fail closed. The portable contract excludes the dynamic site-preflight hash; fresh capacity belongs to authorization. `RUNNER-01` passes; only `AUTH-RECORD-01` blocks, replicate 201 stays sealed, and all authorization, calibration, confirmation, inference, and large-simulation flags remain false. |
| `0.2.3-draft.83d2b2b1g22` | Completed the explicit response-free execution-authorization decision and refused issuance. Parent receipt `2be44c3fdda1dc455a83eecdd6c0613240050db4b4aee8cb0cb47399b8d73818`, policy `4d89c7235e9ae8537b8b9743ba356c8eaf0ad85308cd9c1cf5a4ec87cc562c04`, source audit `d0a59e573ef9c6fba7af5a308a58bc974b04c0a87ca74dc70de1aca39c506b07`, and decision `3df37fa52c9ff688bd5110d4ae097a8fed10123eb898f9967fdcb5fd791c9ab6` reproduce. Exact source inspection confirms the b1g21 nonreserved reduction while showing that b1g21 rejects reserved preparation and b1g13 remains nonreserved-only. Five gates pass; `RESERVED-ENTRY-01`, `ACTIVE-MANIFEST-01`, and `SITE-RECEIPT-01` block. Four tests with 48 assertions preserve the exact R0201 30/120/1,080/5,760/240 denominators and reject receipt, policy, audit, gate, and issuance mutations. The outcome is `no_go_refused_not_issued`; no response, fit, reserved root, authorization, inference, or large-simulation claim is created. |
| `0.2.3-draft.83d2b2b1g23` | Implemented the response-free record-bound reserved entry and exact R0201 active-manifest conversion. Policy `61ee07c4ea86087a7ad8731ef374b38ec7f12621cc8e818501f4e4bab85abd07`, contract `0dca57ff5546d49c2ef49b27e89bd571d19fbbe93ea3698dbdb7d87b69abfd3a`, reduction record `c1c08749050445743e6938357b64aed921fc6b620fdbec31c3586ea40c2f0af0`, active reduction manifest `2221cc38bf526b1cf7d0f66d59090b1af7b1c8b9cbdc6f144caaa06c57399f3a`, source `eec3a92a69aa1d1378137342094e536c4c71c3674b15c7a1260869cbcb0d1394`, and worker `a2ed1788b2ad96f3feb298beee67bff2aa99e8d97f3a874f6ed2bd32c3e799cd` reproduce. Three exact AST transforms preserve the b1g17 generator, b1g18 preparation, and b1g13 checkpoint bodies behind a validated ephemeral capability. Four tests with 75 assertions reproduce b1g21 36/192/8 rows, one typed failure, isolated initial execution, exact four-unit resume, R0201 prospective counts, and mutation rejections. No production issuance function, authorization record, active R0201 object, reserved response, root, or lock exists; fresh runtime/site six-gate issuance, replicate 201, inference, and large simulation remain false or prohibited. |
| `0.2.3-draft.83d2b2b1g24` | Completed the response-free fresh-site one-shard issuance boundary. Source `c93287f1e813bb3dd7265179f6adbe8742486b1f32ec7235297f8b8820a5700f`, policy `688b30494612169df6db370da5460b24f8606106acf695a5921aeab217eb186e`, and contract `b9af6f03b4b3f0a34d1e8e33c6feb5a8ff62ef6516b78f136772cae79eacc620` reproduce. Six exact entry/conversion/runtime/site/scope/confirmation gates are recomputed rather than trusted. One observed GO issues a parent-validated R0201 record and derives an unexecuted 30/120/1,080/5,760/240 active manifest; an occupied-target control yields an auditable NO-GO and issuance refusal. Three tests with 51 assertions reject decision, record, manifest, and audit mutations. No response, fit, checkpoint, root, or lock is created. One fresh R0201 instance may open; R0201 execution/review, inference, and every large-simulation continuation remain pending or prohibited. |

## Release decision algorithm

1. Reject evidence whose candidate/specification identity does not match.
2. Mark the release `No-Go` while any checklist item lacks a reviewed portfolio
   assignment or any `claim_conditional` item lacks an explicit fallback and
   public-surface map.
3. Mark the release `No-Go` for any `release_spine` blocker row that is
   `concern`, `review`, `not_run`, or missing.
4. Do not promote the claim controlled by a failed or unfinished
   `claim_conditional` row. Mark the release `No-Go` if that result remains
   primary, uncaveated, or inconsistently classified on any public surface.
5. Keep `deferred` rows guarded and outside the candidate decision; expanding
   public scope moves the affected row back into the spine before freeze.
6. Do not aggregate across a failed core cell or silently drop failed
   replications.
7. Permit a caveat row only when its limitation appears in the first screen,
   help, capability surface, and release notes for the affected result.
8. Keep every roadmap row guarded from ordinary use.
9. Require normalized common-likelihood evidence before cross-engine IC
   comparison and a passed model/category/constraint/estimability/reporting
   contract before parameter tolerances.
10. Require the practical-consequence classification in addition to formal
   dimensionality evidence.
11. Run engineering release checks only after statistical blockers pass for one
   frozen candidate.

## Source anchors

- Cho, S.-J., and De Boeck, P. (2018). A note on N in Bayesian information
  criterion for item response models. *Applied Psychological Measurement*,
  42, 169-172. doi:10.1177/0146621617726791.
- Sclove, S. L. (1987). Application of model-selection criteria to some
  problems in multivariate analysis. *Psychometrika*, 52, 333-343.
  doi:10.1007/BF02294360.
- Self, S. G., and Liang, K.-Y. (1987). Asymptotic properties of maximum
  likelihood estimators and likelihood ratio tests under nonstandard
  conditions. *Journal of the American Statistical Association*, 82,
  605-610. doi:10.1080/01621459.1987.10478472.
- Christensen, K. B., Makransky, G., and Horton, M. (2017). Critical values
  for Yen's Q3. *Applied Psychological Measurement*, 41, 178-194.
  doi:10.1177/0146621616677520.
- Morris, T. P., White, I. R., and Crowther, M. J. (2019). Using simulation
  studies to evaluate statistical methods. *Statistics in Medicine*, 38,
  2074-2102. doi:10.1002/sim.8086.
- TAM source function `tam_mml_ic_criteria()` in the official repository,
  audited at commit `8fc1c216ff8bfd8a354ae760662a4897ae46a291`, records the
  native `aBIC` formula that must remain distinct from the common Sclove SABIC:
  https://github.com/alexanderrobitzsch/TAM/blob/8fc1c216ff8bfd8a354ae760662a4897ae46a291/R/tam_mml_ic_criteria.R
- TAM 4.3-25 manual, including `tam.jml()` adjustment, bias-reduction, and
  fixed-parameter arguments:
  https://cran.r-project.org/web/packages/TAM/TAM.pdf
- immer CRAN package record and current development manual, including
  CML/CCML, JML convention, and HRM surfaces:
  https://cran.r-project.org/web/packages/immer/index.html and
  https://alexanderrobitzsch.r-universe.dev/immer/doc/manual.html
