on:
  workflow_call:
    inputs:
      os:
        description: Runner image for the required platform cell.
        required: true
        type: string
      r:
        description: R version selector understood by setup-r.
        required: true
        type: string
      cell-id:
        description: Package-check platform cell identifier.
        required: true
        type: string
      full:
        description: Whether to enable the full NOT_CRAN validation layer.
        required: false
        default: false
        type: boolean
      http-user-agent:
        description: Optional setup-r HTTP user agent override.
        required: false
        default: ''
        type: string

jobs:
  check:
    runs-on: ${{ inputs.os }}
    name: ${{ inputs.os }} (${{ inputs.r }})

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      NOT_CRAN: ${{ inputs.full && 'true' || 'false' }}

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ inputs.r }}
          http-user-agent: ${{ inputs.http-user-agent }}
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          # Invalidate compiled-package caches after the runner TBB ABI change.
          cache-version: "2"
          extra-packages: any::digest, any::pkgbuild, any::rcmdcheck, any::testthat
          needs: check

      - name: Exact source-tarball R CMD check (G4 confirmation held)
        env:
          MFRMR_CHECK_ERROR_ON: warning
        run: |
          Rscript --vanilla \
            inst/validation/release-check-runner-0.2.4.R \
            . validation-results/release-check
        shell: bash

      - name: Upload check results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: r-cmd-check-${{ inputs.cell-id }}
          path: |
            validation-results/release-check/check
            validation-results/release-check/release-check-receipt.rds
          if-no-files-found: ignore

      - name: Repository validation review
        run: |
          source("inst/validation/release-readiness.R")
          maintenance <- new.env(parent = globalenv())
          sys.source(
            paste0(
              "inst/validation/",
              "fixed-calibration-g4-maintenance-admission-0.2.4.R"
            ),
            envir = maintenance
          )
          tarball <- list.files(
            "validation-results/release-check",
            pattern = "^mfrmr_[0-9.]+[.]tar[.]gz$", full.names = TRUE
          )
          if (length(tarball) != 1L) {
            stop("The checked source tarball is unavailable.", call. = FALSE)
          }
          readiness <- mfrmr_release_readiness_review(
            pkg_dir = ".",
            check_log = file.path(
              "validation-results/release-check/check/",
              "mfrmr.Rcheck/00check.log"
            ),
            tarball = tarball
          )
          print(summary(readiness))
          admission <- maintenance$mfrmr_fc_g4m_review(".")
          stopifnot(
            isTRUE(readiness$source_truth_status$SourceTruthOK[[1L]]),
            isTRUE(readiness$check_status$CheckPassed[[1L]]),
            isTRUE(readiness$ci_workflow_status$CIWorkflowOK[[1L]]),
            isTRUE(admission$MaintenanceBridgeComplete),
            isTRUE(admission$LegacyV5AutomaticIssuanceDisabled),
            !isTRUE(admission$V6ConfirmationContractFrozen),
            !isTRUE(admission$PostMaintenanceG4Complete)
          )
          cat(
            "Package check complete; no G4 receipt issued; G6 and public ",
            "API remain unauthorized.\n",
            sep = ""
          )
        shell: Rscript {0}
