## Development status

mfrmr 0.2.3 is under development and is not ready for CRAN submission. CRAN
0.2.2, published on 2026-07-27, is the immutable public baseline. Historical
0.2.2 submission claims and test counts are retained in release evidence, not
reused as evidence for 0.2.3.

The maintainer and license are unchanged. The 0.2.3 line is limited to
numerical trust, external-overlap evidence, fail-closed interpretation, and
release-evidence identity for the existing single-scale model contract. It
does not add imported frozen-calibration scoring, general threshold anchors,
multiple observed scales, native multidimensional estimation, or unrestricted
GPCM.

## Candidate and R CMD check status

No 0.2.3 candidate result is claimed. Before this file becomes submission
material, the exact candidate commit, source tarball, `R CMD check` log, frozen
gate specification, evidence checklist, external-tool identities, and
candidate-linked gate results must be recorded and hash-bound in the 0.2.3
candidate manifest. The final check summary must be generated from those
retained artifacts rather than copied from prose or an earlier release.

## Current CRAN status

As reviewed on 2026-08-03, CRAN distributes mfrmr 0.2.2 and provides source,
Windows, and macOS binaries. The populated check matrix reports 12 `OK`
flavors and one r-devel Debian GCC `NOTE` concerning residual
`~/tmp/scratch/Rtmp*` directories. Attribution remains open; 0.2.3 must not
describe the current matrix as all-OK until that result is resolved or shown
to be external to the package.
